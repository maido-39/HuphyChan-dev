# 실시간 브리핑 — Pygmalion 보행 로봇

*마지막 갱신 2026-09-04 01:15 (KST). 이 페이지는 자동 생성됩니다 — 손으로 고치지 말고 `tools/briefing/briefing.py`로 바꾸세요.*

> **이 프로젝트가 뭘 하는 건가**: 두 발 로봇을 시뮬레이션 안에서 걷도록 학습시키고, 걸을 때 관절과 뼈대에 걸리는 **힘을 측정해서 실제 하드웨어를 어떤 크기로 만들지 정하는 것**이 목표입니다. 로봇을 실제로 만들기 전에, 모터가 얼마나 세야 하는지·부품이 얼마나 튼튼해야 하는지를 숫자로 알아내려는 겁니다.

## 지금 하고 있는 일

**하체 본학습 2단계 6,849/18,099회(랜덤화 28%, 보상 105.8, 넘어짐 0, 속도오차 0.89 개선) 계속. Sim-Real 비교 웹뷰어 P0~P4 전 단계 완료·검증(테스트 198, 접속 192.168.20.177:8094) — 실물 연결 전까지는 더미 송신기로 검증 완료 상태**

*시작 2026-09-04 01:15*

## 다음에 할 일

### 본 학습 시작 (처음부터, 새 모델로)

**왜 하는가** — 지금까지는 기존 학습에 800회씩 덧붙이는 시험이었습니다. 확정된 설정으로 처음부터 제대로 학습시켜야 최종 하중 데이터를 얻습니다.

*2026-08-27 15:09*

### 본학습 완주 → 하중 측정 + 정식 분석

**왜 하는가** — 본학습이 끝나면 걸을 때 관절·뼈대에 걸리는 힘을 정밀 측정합니다 — 이 프로젝트의 최종 목적(실제 하드웨어 설계값)입니다.

*2026-08-27 23:43*

### 저속 걷기 수정 재설계

**왜 하는가** — 이번 수정(deadband)은 착지를 딱딱하게 만들어 뺐습니다. 부작용 없는 형태 — 측정 주기를 물리 단위(초당 200회)로 올린 항 — 를 리워드 연구 후 다시 시도합니다.

*2026-08-27 23:43*

### 착지 충격 측정 결함 수정 (전역)

**왜 하는가** — 조사 중 발견: 발이 땅에 닿을 때의 힘을 재는 규칙들이 전부 초당 50회로만 봐서, 0.02초 만에 끝나는 착지 충격을 놓치고 있었습니다. 초당 200회로 보도록 배선만 바꾸면 됩니다 — 이미 다른 부위(자기충돌)는 그렇게 되어 있고 발만 빠져 있었습니다. 본학습 완주 후 별도 실험으로.

*2026-08-28 12:05*

### 허리·어깨 자유도 학습 (상체 비교)

**왜 하는가** — 지금까지는 팔·허리를 고정하고 다리만 학습했습니다. 상체를 풀어서 학습시키면 걸음걸이와 하중이 어떻게 달라지는지 비교하려는 것입니다.

*2026-08-28 15:36*

### 상체 자유도 P1→P2 예행 완주

**왜 하는가** — 보고를 먼저 드리기 위해 120 iteration에서 멈췄습니다. 본학습 전에는 자동 단계전환과 종료까지 한 번 완주해 확인합니다.

*2026-09-01 14:54*


## 막혀 있는 일

### 어깨 3D 형상 갱신

**왜 하는가** — 로봇의 무게는 새 설계 값으로 바꿨지만, 화면에 보이는 어깨와 팔의 생김새는 아직 예전 설계 그대로입니다. 설계 프로그램에서 새 형상을 받아와 맞춰야 합니다.

**결과** — 형상을 못 받아온 진짜 이유를 찾았습니다. 설계 프로그램에 붙어 있는 연결 도구가 멈춰 있어서, 아무 내용도 없는 빈 명령조차 똑같은 오류로 되돌아옵니다. 원격에서 되살릴 방법은 없고, 그 PC에서 연결 도구를 껐다 켜야 합니다. 멈춘 원인은 어제 쓴 받아오기 도구가 설계 트리를 스스로를 다시 부르는 방식으로 훑어 프로그램 내부 메모리 한도를 넘긴 것으로 보입니다 — 처음 짐작했던 '형상이 너무 커서 한 번에 못 보낸다'는 원인이 아니었습니다. 기다리는 동안 도구 자체는 고쳤습니다: 훑는 방식을 바꿨고, 형상이 커도 잘라서 받아오며, 무엇보다 전체가 온전히 다 받아지기 전에는 기존 파일을 절대 덮어쓰지 않습니다. 어제는 이 보호장치가 없어서 팔 형상 파일이 반쪽짜리로 덮어써졌습니다.

**근거 숫자** — 빈 명령까지 포함해 시도한 모든 명령이 같은 오류로 실패(성공 0건), 고친 도구는 실패 시나리오 4가지 전부 통과, 로봇 무게는 31.316 kg 그대로

**풀리려면** — 설계 프로그램이 켜져 있는 PC에서 연결 도구를 껐다가 다시 켜 주는 것. 그 뒤에는 명령 한 줄로 형상이 갱신되고, 실패해도 기존 파일은 그대로 남습니다.

*2026-08-27 18:33*

### 설계 프로그램(Fusion) 연결 복구

**풀리려면** — 부품 사진을 뽑으려면 Fusion에서 형상을 받아와야 하는데, 스크립트 실행 통로가 고장났습니다(빈 스크립트도 같은 오류). 통신 자체는 정상이라 Fusion 쪽 애드인 재시작이 필요합니다. 복구되면 조사표의 사진 칸이 자동으로 채워집니다.

*2026-08-27 15:25*

### 어깨 3D 형상 갱신 — 보류함으로 이동 (재촉 없음)

**풀리려면** — 설계 PC의 연결 도구가 사용할 때마다 조금씩 소모되다 멈추는 구조라, 재시작 3회로도 완주하지 못했습니다. 판단을 정정합니다: 이 형상은 학습에 사실상 쓰이지 않고(팔은 고정, 무게는 이미 반영됨) 가치가 낮아 보류합니다. 사용자가 언제든 그 PC를 쓸 일이 있을 때 팔 부분만 파일로 내보내 주면 연결 도구 없이 처리합니다.

*2026-08-27 23:36*

### 알루미늄 부품 scope 최종 확정

**왜 하는가** — 임시 학습용 질량은 기존 표 D열로 적용했지만 과거 양쪽 10개와 최신 한쪽 CAD 9개 바디가 모순

**결과** — prototype-tempmass 32.470 kg 모델과 스모크는 완료

**풀리려면** — 한쪽 다리 4개 연결부품과 AnkleA2B 6개 CAD body/Arm_A/Arm_B의 실제 assembly 대응표

*2026-09-01 20:12*

### Teacher 정책 의미 확정

**왜 하는가** — 현재 구현은 privileged critic PPO이며 별도 action-producing Teacher와 distillation은 아님

**풀리려면** — privileged critic 유지 또는 Teacher actor→Student distillation 중 하나를 사용자 확정

*2026-09-01 20:12*

### 착지 reward 물리 substep 연결

**왜 하는가** — 발 센서 4-substep history는 기록하지만 active force reward가 아직 마지막 substep만 읽어 50 Hz aliasing이 남음

**풀리려면** — max-over-4 peak 연결 후 weight/threshold ablation과 air-time 퇴행 검사

*2026-09-01 20:13*

### 크랭크 A(L)/B(R) 부호 비대칭 미해결

**풀리려면** — 저전류 실기 검증(모터 1개씩 저전류 구동하며 양쪽 크랭크 엔코더+재구성 발목 pitch/roll 로깅, docs/112 남은 blocker 3) 또는 CAD상 A/B 라벨링 자체 재확인. 좌우 대칭이 아니라(L_crank_A만 반대, R_crank_B만 반대) 코드의 전역 미러 규칙으로 못 고침 — 잘못 추측하면 실제 발목 모터가 기구 스토퍼 반대로 명령받을 위험

*2026-09-02 21:25*


## 끝난 일 (최신순)

### Sim-Real 웹뷰어 P4(비교 모드) 완료

**왜 하는가** — 시뮬레이션과 실물 로봇을 같은 화면에서 비교하는 마지막 단계 — 정책이 실물 센서값을 볼 때 얼마나 다르게 움직이는지, 통신 지연을 얼마나 정확히 재는지 확인

**결과** — 시뮬레이션 기록과 '가짜 실물(더미)' 기록을 비교하는 도구를 완성해 실측했다. 일부러 30밀리초(1000분의 30초) 늦게 보낸 신호를 도구가 32밀리초로 정확히 추정했다(오차 2밀리초). 실물 로봇의 기울어진 자세 센서값을 흉내낸 신호를 정책에 흘려보내면 로봇 관절 명령이 확실히 달라진다는 것도 숫자로 확인했다(평균 변화폭 0.27라디안, 약 15도 수준 — 우연한 잡음이 아니라 실제로 반응한다는 뜻). 사람이 확인해야 하는 항목(실제 로봇의 좌우/부호 확인, 발목 기구 확인, IMU 기울임 확인)은 절차와 통과 기준만 자동으로 안내하고 '통과했다'고 거짓 표시하지 않도록 만들었다. 자동으로 돌릴 수 있는 8단계 중 4단계는 실제로 실행해 전부 통과했다. 회귀 테스트 198개 전부 통과, 메모리 사용량도 예산(300MB) 이내(147~226MB)로 확인됐다.

**근거 숫자** — 지연 추정 32ms(주입 30ms, 오차 2ms) · 정책 행동변화 평균 0.27rad(약 15도) · 자동검증 4/4 통과 · 테스트 198개 전부 통과 · 메모리 147~226MB(예산 300MB)

![[compare_p4item6_step_dummy30ms_L_knee.png]]

*2026-09-04 01:12*

### 시뮬↔실물 비교 웹뷰어 3단계 완성 — 실물 로봇 데이터를 받아 기록·재생하는 기능

**왜 하는가** — 시뮬레이션 화면과 실제 로봇을 나란히 비교하려면, 로봇이 보내는 신호를 받아서 화면에 그대로 반영하고, 나중에 다시 돌려볼 수 있어야 합니다.

**결과** — 받은 데이터로 시뮬레이션 관절을 그대로 움직이는 기능(리플레이)과, 로봇이 보내는 신호를 웹 화면 형식으로 바꿔주는 변환기, 10초짜리 기록을 저장했다가 재생하는 기능을 모두 만들고 실제로 작동을 확인했습니다. 만드는 중 두 가지 숨은 버그(코드가 자기 자신과 얽혀 멈추는 문제, 데이터가 안 온 관절이 힘이 빠져 축 처지는 문제)를 새로 만든 자동검사가 잡아내 그 자리에서 고쳤습니다. 아직 실제 로봇은 없어서 가짜 신호(더미)로만 확인했고, 로봇을 붙였을 때 좌우 다리가 맞게 연결됐는지는 아직 미검증(화면에 경고 배지 표시) 상태입니다.

**근거 숫자** — 실시간 전송 속도 초당 49.4개(목표 50개 근접) · 10초 기록해도 메모리 증가 0.3 MB뿐 · 자동검사 130개→161개로 늘렸고 전부 통과 · 무릎을 실제 각도 30도 굽히면 화면에도 정확히 30도(오차 0.0001도 미만)로 표시됨

*2026-09-04 00:11*

### 학습 중인 걸음 정책을 시뮬레이션 화면에서 그대로 돌려보는 기능 완성

**왜 하는가** — 로봇이 실제로 어떻게 걷는지 확인하려면, 학습이 만들어낸 '걸음 방법'(정책)을 아무 때나 불러와 화면 속 로봇에게 실행시켜 볼 수 있어야 한다. 1단계(완료분)는 로봇을 손으로 움직여보는 것까지였고, 이번에 학습된 걸음 정책을 불러와 실제로 걷게 하는 기능을 완성했다.

**결과** — 이어받아 확인해보니 이전 작업자가 코드 대부분을 이미 완성해 두었고, 남은 일은 검증과 버그 발견이었다. 검증 중 '걷기 모드로 전환' 명령이 옛날 버전 규칙에 걸려 항상 거부되는 버그를 찾았다 — 화면의 조작 버튼은 이 규칙을 우회해서 작동했기 때문에 눈으로만 봐서는 몰랐고, 외부 프로그램이 명령을 보내는 방식(실물 로봇과 연결할 때 쓰는 방식)으로 시도해서야 드러났다. 고쳐서 재확인했다. 정지 자세와 걷기 속도 모두 학습 시스템이 직접 낸 기준값과 비교했다.

**근거 숫자** — 학습이 내놓은 두 시점(700번째/5200번째 갱신)의 걸음 정책을 각각 불러와 확인. 정지 상태(10초 이상 유지)에서 몸통 높이 오차 1.4 mm(허용 기준 20 mm), 양 무릎 각도 오차 0.5도/0.2도(허용 기준 2도). 초속 0.6 m로 걷기 명령 시 15초 동안 넘어지지 않고 7.25 m 이동, 속도 오차 초속 0.055 m(허용 기준 0.1 m). 자동 검사 130개 전부 통과(이전 122개 + 이번에 새로 만든 8개, 13초 소요). 정책 파일을 직접 불러오는 방식은 로딩에 약 11초, 메모리 1.3 GB까지 증가(사전 예상치와 일치).

**풀리려면** — 다음 단계는 실물 로봇에서 실제 관절 데이터를 받아오는 부분(3단계)이다. 그 다음에야 시뮬레이션과 실물을 겹쳐서 비교하는 화면(4단계)을 만들 수 있다.

*2026-09-03 23:02*

### 모든 학습 기록 문서에 설정 명세 표 부착 완료 — 앞으로는 자동

**왜 하는가** — 지금까지는 실험 기록마다 설정 정보가 제각각이라, 나중에 결과를 다시 확인하려면 원본 설정 파일을 뒤져야 했다. 로봇 관절이 실제로 얼마나 움직일 수 있었는지, 모터 힘 한계가 얼마였는지 같은 기본 정보가 문서에 없었다.

**결과** — 학습 실험 기록 111건 전부에 '이 실험의 설정' 표 4종(보상 항목과 가중치 / 관절 모터의 힘·속도·마찰 한계 / 관절이 실제로 움직일 수 있던 각도 범위 / 켜져 있던 옵션 목록)을 붙였다. 표는 사람이 옮겨 적은 것이 아니라 그 실험이 남긴 설정 파일에서 프로그램이 직접 읽어 만든다. 앞으로 새 학습이 끝나면 학습 실행기가 같은 표를 자동으로 넣는다. 작업 중 기존 기준 문서의 '관절 안전 한계' 계산식이 시뮬레이터의 실제 계산과 다르다는 것을 발견해 바로잡았다 — 좌우가 비대칭인 관절(예: 무릎)에서 값이 어긋났고, 무릎의 안전 범위는 0~108도가 아니라 6~114도가 맞다.

**근거 숫자** — 학습 기록 111건 중 실제 학습 문서 103건에 표 삽입(나머지 8건은 여러 실험을 묶은 종합 문서라 각 실험 문서를 가리키는 안내문). 로봇 모델을 확정하지 못해 비워둔 건수 0건, 설정 파일이 사라져 채우지 못한 건수 0건. 모델 출처는 실험이 남긴 사본에서 확정 40건, 나머지 63건은 근거 문장을 문서에 함께 적어 귀속. 실험 당시 이후에 로봇 파일이 수정된 32건에는 '이 값은 현재 파일 기준'이라는 주의 문구를 자동으로 달았다.

*2026-09-03 21:23*

### 시뮬↔실물 비교 웹뷰어 1단계 완성 — 로봇 6종을 브라우저에서 조작·계측

**왜 하는가** — 실물 로봇과 시뮬레이션의 거동을 같은 화면·같은 숫자 형식으로 비교하려면, 먼저 시뮬레이션 쪽이 '학습에 쓰는 것과 똑같은 로봇'을 브라우저에서 조작·관측할 수 있어야 한다. 그 기반을 만들었다.

**결과** — 발목 명령 변환표가 구형 로봇용이라 그대로 쓰면 발이 명령보다 20.7도 어긋난다(구모델에서 좌우 축 부호가 바뀜). 굽는 방향을 자동으로 실측·교정하도록 만들어 오차 0.45도로 줄였다. 또 학습이 매번 '발바닥이 바닥 속 3.9 cm에 박힌 자세'로 시작하고 있음을 확인했다 — 시작 높이 값이 구형 로봇 검증파일에서 오고 있다. 뷰어는 학습과 똑같이 재현만 하고 기록해 뒀다(모델은 손대지 않음).

**근거 숫자** — 로봇 6종(다리만/반전신/전신 × 발목 폐루프AB·직결RP) 모두 준비 완료. 시뮬 속도 199.8 Hz(목표 195 이상, 실시간과 정확히 일치, 끊김 0회), 메모리 218 MB(상한 600). 자동 검사 98개 전부 통과(11.7초). 몸통 고정 정확도: 2초간 흔들림 0.0000000000002 m(사람 머리카락 굵기의 1억분의 1).

**풀리려면** — 다음 단계: 학습된 정책 불러와 돌리기(P2), 실물 로봇 데이터 수신(P3), 겹쳐 보기 비교(P4)

![[pygviewer_p1_verification.png]]

*2026-09-03 21:55*

### 현재 학습(legonly_ab_v2)의 보상·모터게인·토크한계·관절범위 명세를 노트에 추가

**왜 하는가** — 사용자 요청 — 감사자/실물 이식 담당이 학습 설정을 한 표에서 볼 수 있게

**결과** — 학습 노트에 4개 표를 추가했습니다: 보상 22항목(가중치·이유·계산법), 모터별 위치제어 게인(엉덩이 Kp150/무릎 220/발목 크랭크 22.3, Kd 6/1.41)과 토크 한계(RS04 120 N·m, RS03 60 N·m, 실측 속도-토크 곡선 적용), 관절 가동범위·안전한계(×0.9)·명령 허용 창(좌우 12관절 전부 열림 확인), 이번 런의 설정 플래그.

**근거 숫자** — 보상 22항, 액추에이터 그룹 4, 관절 12+수동 4, 프리플라이트 PASS 12/12

*2026-09-03 20:05*

### 정지(속도 0) 자세 측정 — 설계 기본자세 vs 학습 정책

**왜 하는가** — 사용자 요청: 가만히 서 있을 때의 관절값과 자세(실물 대조용)

**결과** — 설계 기본자세는 양쪽 고관절 굴곡 10°·무릎 굴곡 20°의 약간 웅크린 자세이고 골반 높이 0.907 m입니다. 재학습 1시간 시점 정책은 속도 0 명령에서 완전히 정지해 서며(골반 높이 0.903 m, 속도 0.001 m/s) 양쪽 무릎을 5.8°씩 대칭으로 굽힙니다 — 수정 전에는 왼무릎이 0°에 잠겨 있었으므로 수정이 실제 동작에서도 확인된 것입니다. 다만 양발이 같은 쪽으로 20~26° 돌아간 비틀린 자세는 초기 정책의 습관이라 학습 진행에 따라 사라지는지 매 점검마다 추적합니다. 부수 발견: 초기자세 키프레임의 발바닥이 수평 대비 10.6° 기울어져 있어(구모델 값 잔존) 재산출을 백로그에 올렸습니다.

**근거 숫자** — 골반 높이 설계 0.907 / 정책 0.903 m, 무릎 굴곡 좌우 5.81/5.88°(대칭), 엉덩이 회전 −20/−26°(감시), 키프레임 발바닥 기울기 10.6°

*2026-09-03 14:30*

### 문서 사이트 호스팅 완료

**왜 하는가** — 연구 문서 428편(마크다운)을 팀·감사자가 브라우저로 바로 읽을 수 있게, 사내망 웹사이트로 띄운다. 지금은 서버에 SSH로 들어가야만 읽을 수 있다.

**결과** — 연구 문서 폴더 전체를 사내망 웹사이트로 띄웠다. 같은 네트워크의 아무 컴퓨터에서 브라우저로 http://192.168.20.177:8300 을 열면 된다(로그인 없음). 첫 화면은 이 실시간 브리핑이고, 왼쪽 목록과 위쪽 검색창으로 문서 425편을 훑을 수 있다. 문서끼리 걸어 둔 링크·삽입 이미지·수식·다이어그램이 그대로 보인다. 원본 문서 폴더는 '읽기 전용'으로 연결해 두어 웹사이트 쪽에서 문서를 고치는 일은 구조적으로 불가능하다. 문서를 고치면 웹사이트가 약 1분 안에 따라 바뀐다(실측 43초).

**근거 숫자** — 웹 주소 192.168.20.177:8300 · 문서 425편 → 웹페이지 539장 · 사이트 재생성 20~23초 · 문서 수정 후 반영까지 43초(실측, 목표 5분) · 동영상 189개(3.3GB)는 복사하지 않고 원본에서 바로 재생 → 디스크 추가 사용량 3.7GB 대신 0.3GB · 문서 간 링크 822개 중 연결 안 되는 링크 5.2%(모두 원본 문서가 없는 링크로, 웹사이트가 아니라 원문 쪽 문제)

*2026-09-03 14:03*

- ~~좌우 부호 버그 수정 + 짧은 시험학습 통과~~ — 수정 후 짧은 시험학습(1024개 환경, 17분)이 넘어짐 0회로 완주했고, 왼쪽 무릎이 처음으로 실제 움직임을 명령받았다. *(요약 보관)*
- ~~걸음걸이 품질 정량 측정 (무릎·발목·발끝 밀기)~~ — 사용자 관찰 3개 중 '무릎을 안 쓴다'는 확정, '발목을 안 쓴다'는 반박, '발끝으로 안 민다'는 왼발만 사실. 그리고 원인이 리워드가 아니라 로봇 모델과 학습 설정의 좌우 부호 불일치임이 드러났습니다. *(요약 보관)*
- ~~발 충돌모델 검토~~ — 발이 서로 뚫고 지나가는 것처럼 보이는 것은 물리 버그가 아니라 그림과 실제 부딪히는 몸통의 크기가 다르기 때문이다. 발의 겉모습은 폭 115mm로 그려지지만, 부딪힘 계산에 쓰이는 상자는 폭 100mm라서 한쪽당 7.5mm씩 겉모습이 더 넓다. 게다가 화면(viser)은 이 부딪힘 상자를 기본적으로 숨긴다. 그래서 두 발이 최대 15mm까지 겹쳐 보여도 실제로는 아직 닿지 않은 정상 상태다. 발목 링크 자체에는 부딪힘 도형이 없지만, 정강이 도형과 발바닥 상자가 발목 높이에서 29.5mm 겹쳐 감싸므로 빈 구멍은 없다. *(요약 보관)*
- ~~세션 공백기(8/31~9/3) 작업 전부 인수 — 종합 문서 작성~~ — 공백기에 로봇 모델이 최신 CAD(v30)로 전면 재구축되고(부품 25건 수정) 12관절 하체 본학습이 시작됐습니다. 문서에 없던 발견 4건을 복구해 기록했습니다: 실물 모터 제어기의 설정 인코딩이 제조사 최신판으로 고쳐진 새 커밋(두 모터 기종은 범위가 10배 다름), 자세센서는 앞뒤축 가속도만 부호가 반대(제어코드는 결백), CAD 원격조회가 죽던 진짜 원인(조회 도구 자신의 예외 축적), 학습 자동복구 장치가 8일간 죽어 있던 것(재가동). *(요약 보관)*
- ~~세션 공백기(8/31~9/3) 작업 전부 인수 — 종합 문서 작성~~ — 공백기에 로봇 모델이 최신 CAD(v30)로 전면 재구축되고(부품 25건 수정) 12관절 하체 본학습이 시작됐습니다. 문서에 없던 발견 4건을 복구해 기록했습니다: 실물 모터 제어기의 설정 인코딩이 제조사 최신판으로 고쳐진 새 커밋(두 모터 기종은 범위가 10배 다름), 자세센서는 앞뒤축 가속도만 부호가 반대(제어코드는 결백), CAD 원격조회가 죽던 진짜 원인(조회 도구 자신의 예외 축적), 학습 자동복구 장치가 8일간 죽어 있던 것(재가동). *(요약 보관)*
- ~~질량 무작위화 범위: 몸체귀속 수정 + 나사무게 불확실성 합치기~~ — 두 보정을 합친 새 파일을 만들었습니다(전신 10개 부위 질량·무게중심·관성 범위 전부 갱신). 이제부터 이 파일이 기준이고, 앞선 두 파일은 낡은 것으로 표시했습니다. 다리전용 모델용 축소판도 같이 만들었습니다. *(요약 보관)*
- ~~LegOnly(12-DOF) 학습 오케스트레이션 스모크 launch~~ — 정상 launch 확인(iter 19 진행 중, 낙상 0). 기존 질량 무작위화 설정파일을 그대로 쓰면 상체가 없는 이 모델에서 오류가 날 것을 코드 검토로 미리 찾아, 다리 부위만 남긴 새 설정파일을 만들어 회피했습니다. *(요약 보관)*
- ~~6차: 어깨 ROM 방향 재반전(pitch 양쪽 + roll R만)~~ — shoulder_pitch: 직전 라운드에 사용자 제공 표 값(-60,170)을 L에 그대로 적용했으나, 그 표의 부호규약 자체가 현재 축과 반대였음 — negate-swap 한 번 더 적용(L=(-170,60), R은 기존 메커니즘으로 자동 대응(-60,170), negate-swap이 대합(involution)이라 공식 재사용만으로 양쪽 다 해결). shoulder_roll: L은 정상 확인, R만 별도 반전 필요 — SHOULDER_ROLL_R_CAP=(-130,15) 신설(hip_roll과 동일 패턴: 축 불변·range만 R전용) *(요약 보관)*
- ~~뷰어 축 색상 충돌 발견 + 축 정면뷰 버튼 추가~~ — 실제 회전 중 213개 지점을 캡처해 데이터 자체(오른손좌표계 성립 여부)를 매 순간 검산 — 오차 0.0003 이내로 항상 정상, 데이터/변환 코드는 결백. 대신 화면에 안 움직이는 기준축(고정, 빨강/초록/파랑)과 실제로 움직이는 센서축(회전, 똑같이 빨강/초록/파랑)이 같은 색으로 원점에 겹쳐 그려지고 있던 걸 발견 — 착시의 유력한 원인. 기준축은 회색으로 바꾸고, 각 축을 원근왜곡 없이 정면으로 보는 버튼(-X/-Y/-Z) 3개를 추가해 눈으로 직접, 애매함 없이 재확인할 수 있게 했습니다. 미완성으로 방치돼 있던 '카메라 추적' 체크박스(아무 동작 안 함)도 제거. *(요약 보관)*
- ~~IMU 실제 메시 확보 완료(Fusion 재연결)~~ — Fusion MCP 브리지 도달 방법 확정: FUSION_MCP=http://192.168.20.161:27182/mcp FUSION_MCP_HOST=127.0.0.1:27182 (윈도우 portproxy, Host 헤더로 loopback 인증 우회). meshes_fusion_full.py의 LIST_SCRIPT 하드코드 block에서 E2Box-IMU 제거 후 라이브 재fetch: 91/91 바디(기존 86 캐시+신규 IMU 5개), pelvis 11 parts로 증가. 렌더로 실제 보드 형상 확인(작은 사각 PCB+커넥터 노치). 프로덕션 반영·컴파일 확인·8098 재기동 완료 *(요약 보관)*
- ~~웹뷰어 화면표시 버그 발견·수정 — 회전방향이 거꾸로 보이던 원인~~ — 제 3D 뷰어 코드 안에 진짜 버그가 있었습니다 — Z축이 위인 센서 좌표를 화면용(Y축이 위)으로 바꾸면서 단순히 두 축을 맞바꿨는데, 이건 회전이 아니라 거울에 비친 것처럼 좌우가 뒤집히는 변환이었습니다(행렬식 -1). 그 결과 화면에서 회전 방향만 거꾸로 보였습니다. 부호 하나를 추가해 진짜 회전(행렬식 +1)으로 고쳤습니다. 단, 이건 화면표시 코드에만 있던 문제라 앞서 확정한 X축 데이터 불일치(가속도계 vs 자세)는 이 코드를 거치지 않고 확인한 것이라 그대로 유효합니다. *(요약 보관)*
- ~~X축 보정 적용 + 동작중 오차 분리 확인~~ — 확인해보니 두 가지가 섞여 있었습니다: (1) 가만히 있을 때도 있는 작은 고정 오차(X축, 이미 확정됨) (2) 빠르게 돌리는 동안에는 가속도계가 중력 외의 실제 회전운동도 같이 재서 생기는 노이즈 — 이게 19배 더 큽니다(정지 0.03 vs 회전중 0.60). 실제 정책이 쓰는 'Projected Gravity' 값 자체는 가속도계와 무관하게 쿼터니언에서만 계산되어(코드 확인), 이번 X축 보정과는 별개입니다. 뷰어에 X축 보정을 반영하고, 화면에 지금이 '정지 판정 가능 구간'인지 '운동중이라 판정 불가'인지 표시를 추가했습니다. *(요약 보관)*
- ~~EBIMU 축 불일치 원인 확정 — X축 가속도만 부호 반대, HUPHY 코드는 결백~~ — 90초 동안 센서를 여러 방향으로 돌리며 2053개 지점을 기록했습니다. 앞뒤(X)축만 정확히 부호가 반대로 나오는 관계가 전체의 94.6%에서 그대로 유지됐습니다(좌우/상하 축은 오차 0.001 수준으로 정상). GitHub의 HUPHY 원본 코드와 이 프로젝트가 쓰는 사본을 한 글자도 다르지 않게 diff로 대조했고, 가속도 값에는 축 변환/부호처리가 아예 없습니다(쿼터니언만 순서를 바꿔 받음) — 즉 원인은 HUPHY 소프트웨어가 아니라 센서 자체(펌웨어의 쿼터니언 기준축과 가속도 원본축이 다르거나, 실장 방향)입니다. *(요약 보관)*
- ~~팀원 huphy_mjcf 저장소용 별도 관절뷰어(8099)~~ — tools/viewer/mjcf_joint_viewer.py에 --xml 제네릭 모드 추가(폐루프 없는 임의 flat-hinge MJCF용, L_/R_ 접두사로 자동 그룹핑). huphy_mjcf를 /home/syaro/external_repos/huphy_mjcf에 클론(스크래치패드 아님, 세션 간 보존). 8099에 별도 기동, 8098(자사 Pygmalion FullDoF)과 동시 열람 가능. .claude/launch.json에 huphy-mjcf-joint-viewer로 등록. huphy.xml은 다리 전용 12관절(폐루프 없음) + 자체 actuator gear 부호(예: R_hip_pitch gear=-1, L_hip_pitch gear=1)를 이미 포함 — 우리 motor_sign_convention.json과는 별개 감사이므로 배지는 대부분 '감사없음'으로 뜸(정상) *(요약 보관)*
- ~~EBIMU 실시간 3D 웹 뷰어 제작 — 축 불일치를 눈으로 보여줌~~ — 브라우저에서 여는 실시간 3D 화면을 만들었습니다: IMU 자세(빨강/초록/파랑 화살표), 가속도계가 가리키는 '아래' 방향(노랑 화살표, 기준선인 회색 점선과 겹쳐야 정상), 센서 자체가 계산한 이동경로(청록 선). 테스트로 켜 보니 가만히 있는데도 노랑 화살표가 기준선에서 16도나 벗어나 있어 — 앞서 발견한 축 불일치가 화면에서도 그대로 보입니다. *(요약 보관)*
- ~~EBIMU 단일 reader 2분 정지 측정 — 클린 완료, 가동률 100%~~ — 실제 로봇 제어에 쓰는 코드(HUPHY의 배포용 IMU 클래스)를 그대로 써서, 리더 하나만으로 2분을 측정했습니다. 100Hz로 와야 할 패킷이 12,000개 중 12,000개 전부 왔고(빠진 것 0개), 잘못된 줄도 0개였습니다. 다만 자세(쿼터니언에서 계산한 중력 방향)와 가속도계가 직접 잰 중력 방향을 비교했더니, 앞뒤(X) 축만 부호가 반대로 나옵니다 — 좌우/상하(Y,Z) 축은 완전히 일치합니다. *(요약 보관)*
- ~~MJCF 관절 뷰어(viser) 신규 작성~~ — tools/viewer/mjcf_joint_viewer.py 신규 작성. FullDoF 정본=_loop(AB, 2-RSU 폐루프) — v2s1_AB 본학습과 09-02 스모크테스트가 이 파일을 씀. 크랭크A/B는 실제 구동 슬라이더, ankle_pitch/roll은 폐루프 등식구속으로 물리 settle(mj_step, 크랭크 PD서보 Kp22.3/Kd1.41±60N·m, loop_ankle_verify.py와 동일 수치)해 유도되는 읽기전용 값으로 표시(단순 mj_forward로는 등식구속이 위치레벨에서 안 풀림을 확인 후 결정). loop_ankle_verify.json 잼그리드와 결과 수치 일치 검증(cA=cB=-40° → pitch31.89/roll0.38, closure 0.002mm 동일). motor_sign_convention.json 대조: waist_yaw만 확정 INVERTED(matches=false), 나머지 8관절(hip×3·knee·crank×2·shoulder×2)은 CAD전용 미검증(matches=null), R측·ankle_pitch/roll은 감사기록 자체가 없음. viser 서버 0.0.0.0:8098 기동, .claude/launch.json에 pygmalion-fulldof-joint-viewer로 등록 *(요약 보관)*
- ~~Policy→HUPHY 배포 어댑테이션 설계~~ — 실제 로봇 제어 프로그램은 지금 다리 하나(모터 6개)만 보고 목표 속도 없이 균형을 잡는 훨씬 단순한 프로그램입니다. 우리가 학습 중인 정책은 양다리 12개 모터와 목표 속도를 함께 씁니다 — 목표 속도를 전달할 통로 자체가 실행 프로그램에 없습니다. IMU(자세 센서) 쪽은 중력 방향을 계산하는 두 수식(시뮬레이터용/실물용)을 나란히 풀어봤더니 수학적으로 완전히 같았습니다 — 이전에 보고된 '자세 센서와 가속도 센서 방향이 안 맞는다'는 문제는 코드 버그가 아니라, 가속도 센서가 힘의 방향을 재는 방식이 제조사마다 달라 생기는 흔한 부호 혼동일 가능성이 높고, 실물 제어 프로그램은 이미 그 혼동을 자동으로 잡아내는 코드를 갖고 있었습니다. *(요약 보관)*
- ~~45입력 Student·안전 관절 제한·점진 관성 무작위화 재시험~~ — 두 단계 재시험이 정상 종료됐고, 관성 무작위화는 시작부터 전량 적용되지 않고 지정 구간에서 0에서 1로 증가했다. 실행 노트·재현 파일·수치 그래프·누적 영상도 복구했다. *(요약 보관)*
- ~~prototype-tempmass 12-DOF GPU 스모크~~ — actor 45D, critic 82D, action 12D; P1 model_200, P2 model_399 생성 *(요약 보관)*
- ~~Pygmalion 12-DOF Sim2Real Skill 설치~~ — 자동 호출 가능한 pygmalion-12dof-sim2real Skill 검증·설치 완료 *(요약 보관)*
- ~~학습 전 자동 안전검사~~ — GPU가 아니라 실행 전 설정검사가 병목임을 확인했습니다. 상체 보상표·모터 정격·초기자세 오류를 수정하고 자원 검사를 자동화했습니다. 1환경 실제 step과 1,024환경 GPU 예행을 통과했으며, 본학습은 아직 시작하지 않았습니다. *(요약 보관)*
- ~~전 관절 가동범위 정리 + 상체 학습용 조정~~ — 다리·상체·목 전 관절의 가동범위를 실측 스톱과 설계 한계로 구분해 표로 정리했습니다. 상체 학습용으로 어깨 앞뒤 회전을 보행에 맞게 좁히고(뒤로 완전회전 -180도 -> -90도), 허리 좌우 회전을 60도->45도로 조정했습니다. 이 조정은 상체를 푼 학습에만 적용되고 지금 돌고 있는 측정에는 영향 없음을 확인했습니다. *(요약 보관)*
- ~~두 시뮬레이터 정성 비교 — 영상+플롯 완성~~ — 같은 정책·같은 명령을 두 엔진에 돌린 실시간 나란히 영상(5단계 설명 캡션)과 플롯 4종을 만들었습니다. 걸음걸이는 두 엔진이 거의 같고(케이던스 9% 이내), 착지 순간 힘만 2배 차이 — 그것도 버그가 아니라 계산 정밀도 설정 하나 때문임을 파형으로 보였습니다. *(요약 보관)*
- ~~발목 미사용 의혹 — 버그 아님 확인~~ — 버그 아닙니다. 토크 0은 '0이어야 하는 계산 항목' 딱 하나였고, 발목은 실제로 60~71도씩 움직이며 밀어차는 일을 합니다. 링크(크랭크) 모터가 힘을 내고, 그 힘이 발목으로 100% 전달되며(상관 0.999), 직렬식 발목과 ±10%로 같습니다. 다음 상체-자유 학습을 이 위에 얹어도 됩니다. *(요약 보관)*
- ~~MuJoCo·IsaacSim 두 엔진 걸음 비교 — 그림과 영상~~ — 완료: 비교 그림 4장 + 두 엔진을 나란히 붙인 15초 실시간 영상(왼쪽 MuJoCo·오른쪽 IsaacSim, 자막이 다섯 박자를 지나가고 각 패널에 그 순간 발에 걸리는 힘을 표시). 같은 로봇·같은 '앞으로 1.6 m/s' 명령을 두 물리엔진에서 걷게 한 결과, 걸음의 리듬(박자·접지시간·보폭·충격량)은 10% 안에서 같다. 착지 순간의 최대 힘과 힘이 차오르는 속도만 약 2배 차이인데, 로봇이 다르게 걸어서가 아니라 엔진의 접촉 계산 정밀도 설정 탓이며, 자동 변환 때 가장 나쁜 설정(32/1)이 소리 없이 들어가 있었다. 4/8로 바꾸면 최대 힘 차이가 2배에서 1.1배, 상승률이 2.9배에서 1.4배로 줄어든다. 영상은 진짜 로봇을 렌더한 것(데이터 애니메이션 폴백 아님) — 단 두 패널 모두 MuJoCo 렌더러로 그렸고 오른쪽은 IsaacSim이 실제로 만든 관절 궤적을 재생한 것이다. *(요약 보관)*
- ~~본학습 완주 — 확정 스택 첫 정책 완성~~ — 2단계 자동 학습이 끝까지 완주했습니다. 1단계(평지, 성적 기반 자동 진급)가 4,100회에 최고 난도까지 도달, 2단계(교란 서서히 증가)가 18,099회에 종료. 넘어짐 없이 진행됐습니다. 이제 이 정책으로 하중 측정을 시작합니다. *(요약 보관)*
- ~~정책 정직성 자동검증 완성~~ — 만들어서 실제 데이터로 검증했습니다: 정상 정책은 통과, 발목 모터가 한계를 넘는 옛 정책은 자동으로 걸러냈습니다(게이트가 2-RSU 링크 발목이 왜 필요한지를 스스로 재발견). 앞으로 모든 하중 측정에 자동으로 붙습니다. *(요약 보관)*
- ~~하중값 적대검증 체크리스트 완성~~ — 7개 주장 중 3개는 방어됨(측정주기 규율·엔진 피크불일치가 구조적임·낙하보호 업계관행), 4개는 열린 갭으로 확인: 단일정책 사이징·불변량 자동검증 부재·안전계수 근거·DR 보수성. 이 중 불변량 자동검증(정책이 힘을 정직하게 냈는지 물리법칙으로 체크)은 지금 만들어 첫 측정부터 붙입니다. *(요약 보관)*
- ~~두 시뮬레이터 하중값 신뢰경계 확정 + 외부 문헌 검증~~ — 걸음걸이·충격량·박자는 두 엔진이 일치(설계에 사용 가능), 순간 최대힘은 접촉 계산 방식이 근본적으로 달라 불일치(계산 정밀도 명시 시에만 사용). 이 불일치가 버그가 아니라 물리적으로 당연한 것임을 외부 논문으로 확인했습니다. 폐루프 발목까지 두 엔진에서 검증 완료. *(요약 보관)*
- ~~느린 걸음 수정 — 원인 확정, 본학습에서 제외~~ — 난수만 바꾸면 착지가 거의 안 변하는데(±5%), 수정을 넣으면 항상 2배 이상 딱딱해집니다 — 수정 탓으로 확정. 본학습에서는 빼고, 부작용 없는 형태의 저속 수정을 다음 과제로 등록했습니다. *(요약 보관)*
- ~~느린 걸음 무시 문제 — 고쳐졌습니다~~ — 문턱값 하나를 관례값으로 내리는 단일 변경으로 해결됐습니다. 느린 명령 달성률이 2.3배로 올랐고, 멈춰 서라는 명령 준수와 빠른 걷기는 그대로입니다. 마지막 확인 하나(순간 충격이 커지지 않았는지, 정밀 측정)가 통과하면 본학습 구성에 들어갑니다. *(요약 보관)*
- ~~본 학습을 자동으로 돌려 주는 진행 장치~~ — 만들었고, 축소판으로 처음부터 끝까지 돌려 검증했습니다. 1단계는 로봇 성적이 기준을 넘는 순간 스스로 끝났고, 장치가 그 지점을 읽어 2단계의 방해 강도 일정을 자동으로 계산했습니다. 방해는 계획한 구간에서 0에서 1까지 정확히 직선으로 올라간 뒤 유지됐습니다. 학습을 도중에 강제로 죽이고 장치를 다시 불렀더니 처음부터 다시 하지 않고 마지막 저장점에서 이어 갔습니다. 검증 중에 실제 결함 두 개도 잡았습니다 — 진행 상황을 잘못 읽던 문제와, 다른 작업자와 공유하는 순번표 형식을 잘못 안 문제입니다. *(요약 보관)*
- ~~폐루프 발목 로봇도 다른 시뮬레이터에서 걷는다 — 단, 계산 정밀도 설정이 직렬 발목과 정반대~~ — 학습된 걸음 정책을 옮겨 45초 걷게 했고, 세 설정 모두 한 번도 넘어지지 않았습니다. 그런데 지난번 직렬 발목에서 '이게 정답'으로 확정한 계산 정밀도 설정(위치 4회)이 고리 구조에서는 최악이었습니다. 그 설정에서는 연결봉 끝이 최대 12.9 mm(손가락 굵기)나 벌어지고, 모터가 아예 없는 발목 관절에 58.6 N·m라는 있을 수 없는 힘이 잡힙니다. 정밀도를 32회로 올리면 벌어짐이 평균 0.043 mm로, 원래 시뮬레이터 자신의 0.055 mm와 같은 수준까지 돌아옵니다. ★부수 발견: 원래 시뮬레이터도 걷는 동안 최대 0.63 mm 벌어집니다 — 이전에 인용하던 0.0003 mm는 '가만히 서 있을 때' 값이었습니다. 판정 기준을 0이 아니라 0.63 mm로 바꿔야 합니다. *(요약 보관)*
- ~~다음 세대 학습을 위한 장치 3개를 만들어 붙였습니다 (성적표 진급 · 탐색 노이즈 줄이기 · 심판에게 조건 알려주기)~~ — 세 장치 모두 구현했고, 셋을 한꺼번에 켠 300회 예행 학습에서 전부 실제로 작동하는 것을 확인했습니다. 기본값은 모두 꺼짐이라 기존 학습은 한 글자도 달라지지 않습니다(설정을 켠 것과 끈 것을 기계로 비교해 완전히 동일함을 확인). *(요약 보관)*
- ~~착지 힘 2배 차이의 범인 — 모델 변환기가 써 넣은 설정 한 줄~~ — 의심 가는 설정 다섯 가지를 **한 번에 하나씩만** 바꿔 45초씩 22번 다시 걷게 했습니다. 먼저 재현성부터 확인했습니다 — 아무것도 바꾸지 않은 재실행이 소수점 17자리까지 이전 결과와 똑같이 나왔으므로, 표에 보이는 차이는 전부 우연이 아니라 그 설정의 효과입니다. 결과, 유력 후보 네 가지는 헛다리였고(그중 둘은 결과가 한 비트도 바뀌지 않았습니다), 진짜 원인은 **로봇 모델 파일을 변환할 때 자동으로 써 넣어진 '계산 반복 횟수' 32/1** 이었습니다. 이 값은 NVIDIA가 자사 휴머노이드에 쓰는 값(8/4, 4/4)과 다르며, 이번 조사에서 **가장 나쁜 설정**이었습니다. 4/8로 바꾸자 최대 힘 격차가 2배→1.10배, 힘이 솟구치는 속도의 격차가 3배→1.14배로 닫혔고, 덤으로 걷는 속도 오차까지 줄었습니다. 학계 문헌이 가장 유력하다고 지목했던 '바닥을 무르게 만들기'(접촉 컴플라이언스)는 힘은 맞췄지만 로봇이 오히려 더 빨라져서 채택하지 않았습니다. **결론: 충격량·접지 시간은 처음부터 두 엔진이 일치했고, 이제 최대 힘과 상승률도 설정만 맞추면 10~15% 안에서 비교할 수 있습니다.** 설계 하중 숫자 자체는 계속 기존 엔진 쪽을 쓰되, 교차검증이 성립하는 조건을 확정한 것입니다. *(요약 보관)*
- ~~폐루프(AB) 발목을 IsaacSim에서 쓸 수 있는 형식으로 옮기고, 원래 시뮬레이터와 같은 답을 내는지 확인했습니다. 발목을 붙잡는 두 개의 연결봉이 만드는 '고리' 구조는 표준 로봇 파일 형식으로는 표현이 안 돼서, 고리를 닫는 관절 4개를 직접 만들어 넣었습니다. 같은 자세로 세워 놓고 각 모터가 중력을 버티는 힘을 재 보니 두 시뮬레이터가 최대 0.0078 N·m 차이(가장 큰 부하의 0.24%)로 일치했고, 고리 이음매가 벌어진 거리는 0.0003 mm(머리카락 굵기의 30만분의 1 수준)였습니다. 모터를 잡지 않고 발목을 자유롭게 놔뒀을 때도 두 시뮬레이터가 발목을 같은 각도(2 마이크로라디안 이내)에 놓았습니다.~~ —  *(요약 보관)*
- ~~CAD 원격 조회를 먹통으로 만들던 마지막 원인 3건 제거~~ — CAD 프로그램(Fusion)에 원격으로 보내는 조회 스크립트 중 3개가 부품 트리를 '함수가 자기 자신을 다시 부르는 방식(재귀)'으로 탐색하고 있었습니다. 원격 실행 환경은 이름을 하나 찾을 때마다 호출이 겹겹이 쌓이는 구조라, 재귀 탐색이 CAD 쪽 파이썬의 호출 한도(약 1 MB)를 넘겨 원격 실행 통로 전체가 먹통이 됐고(8/26·8/27 두 번, 복구는 CAD PC에서 수동 재시작뿐) — 세 스크립트 모두 '할 일을 목록에 쌓아 두고 하나씩 꺼내는 방식'으로 바꿨습니다. 이제 원격으로 보내는 스크립트 중 재귀는 남아 있지 않고, 먹통이면 시작 전에 감지해 즉시 멈추고 이미 받은 데이터는 저장합니다. *(요약 보관)*
- ~~두 시뮬레이터의 보행 비교 — 걸음은 같고 충격 숫자만 다르다~~ — 로봇의 걸음걸이(딛는 박자, 접지 시간, 충격량 총합)는 두 엔진에서 10% 이내로 같았습니다. 그러나 순간 최대 충격력은 2배 차이 — 두 엔진이 충돌을 계산하는 방식 자체가 달라서입니다. 결론: 충격량과 타이밍은 설계에 써도 되고, 순간 피크값은 아직 안 됩니다. 또 하나 — 이전에 '속도가 18% 빠르다'던 문제는 로봇 모델을 잘못 얹은 것이 태반이어서, 맞는 모델로 바꾸니 원래 엔진과 같은 수준(오차 0.14)이 됐습니다. *(요약 보관)*
- ~~두 시뮬레이터의 착지 힘 비교 — 걸음은 같은데 충격은 두 배~~ — 같은 정책·같은 로봇(35.35 kg)·같은 판정 규칙으로 45초를 걷게 하고, 발밑에 걸리는 힘을 초당 200번 기록했습니다. **걸음 자체는 두 엔진이 사실상 같습니다** — 발이 땅에 닿아 있는 시간 비율 0.528 대 0.533, 발 딛는 횟수 초당 2.41 대 2.21, 착지 후 60 ms 동안 발이 받는 총 충격량도 10% 이내로 같습니다. **그런데 착지 순간의 최대 힘은 두 배, 힘이 솟구치는 속도는 세 배 차이가 납니다.** 원인은 로봇이 아니라 바닥이 얼마나 딱딱한가의 설정입니다: 기존 엔진은 충돌을 20 ms에 걸쳐 부드럽게 퍼뜨리도록 되어 있고, 새 엔진은 딱딱한 충돌이라 5 ms 한 칸에 전부 몰립니다 — 그림 (a)에서 뾰족한 봉우리 하나가 그것입니다. 계측기 자체는 세 가지로 검증했습니다(가장 확실한 것: 걷는 동안 발밑 힘의 시간 평균이 정확히 몸무게의 1.000배). **결론: 두 엔진의 최대 착지력 수치를 그대로 비교하면 안 됩니다.** 설계 하중으로 쓸 때는 어느 바닥 설정이 실제 바닥에 가까운지를 먼저 정해야 하고, 그 전까지는 충격량·접지 시간처럼 바닥 설정에 둔감한 값만 엔진 간에 비교할 수 있습니다. *(요약 보관)*
- ~~학습된 걷기가 두 번째 시뮬레이터에서도 걷는다~~ — 옮겨 심은 정책이 14초 동안 넘어지지 않고 23.5 m를 걸었습니다. 세 번 실패했고 세 번 다 원인을 찾았습니다 — 모터 내부 관성이 파일 형식상 전달이 안 되던 것이 결정적이었고, 바닥이 기본값으로 '탱탱볼'이었던 것도 있었습니다. 속도는 아직 명령보다 18% 빠릅니다. *(요약 보관)*
- ~~두 시뮬레이터가 같은 로봇을 갖게 됨 — 변환·검증 완료~~ — 모델을 변환해 두 엔진에 같은 자세를 취하게 하고, 각 관절이 중력을 버티는 데 필요한 힘을 비교했습니다. 17개 관절 전부 소수점 넷째 자리까지 일치합니다. 무게도 그램 단위까지 같습니다. *(요약 보관)*
- ~~IsaacSim 설치 완료~~ — 설치하고 실제로 실행되는 것까지 확인했습니다. 물리 장면 생성도 됩니다. 처음 실행은 5분 걸렸지만 이후에는 7초입니다. *(요약 보관)*
- ~~어깨 출력물 무게 조사표 작성~~ — 10개 부품 표를 만들었습니다. 조사 중에 두 가지가 드러났습니다 — (1) 어깨 브래킷 한 개가 도면상 아직 '금속'으로 잡혀 있어 출력물일 때보다 9배 무겁게(543 g vs 61 g) 계산되고 있었고, (2) 그 부품을 포함해 **부품 11개(합계 873 g)가 로봇 모델에서 통째로 빠져 있습니다** — 특히 IMU 조립체가 통째로(165 g) 빠져 있습니다. *(요약 보관)*
- ~~실시간 브리핑 자동화~~ — 작업이 움직였는데 이 페이지가 그대로면 세션이 종료되지 않도록 막아뒀습니다. 학습·측정·영상·커밋 직후에는 즉시 알림이 뜹니다. *(요약 보관)*
- ~~새 모델로 다시 학습 — 통과~~ — 잘 걷습니다. 32번 시험 전부 성공, 넘어짐 0회. 다만 예상 밖의 결과가 하나 나왔습니다 — 4 kg을 덜어냈는데 착지할 때 발에 걸리는 힘은 그대로였습니다. 몸이 가벼워진 만큼 몸무게 대비 부담은 오히려 15% 커졌습니다. **부품을 가볍게 만든다고 충격이 줄지는 않는다**는 뜻입니다. *(요약 보관)*
- ~~착지 충격 벌점 실험 — 실패~~ — 역효과였습니다. 충격을 깎았더니 실제 충격이 2.6배 늘었습니다. 이유는 규칙이 충격을 초당 50번만 들여다보는데, 실제 충격은 0.02초 만에 끝나서 대부분 놓치기 때문입니다. 로봇은 충격을 줄이는 대신 '측정을 피하는' 요령을 배웠습니다. *(요약 보관)*
- ~~전 관절 동작범위 점검 영상~~ — 17개 관절 전부 최소~최대까지 움직이고 정확히 원위치로 돌아옵니다. 모터 방향은 7개 중 5개가 설계와 일치, 2개(엉덩이·어깨의 앞뒤 회전)가 반대입니다. *(요약 보관)*
- ~~로봇 모델 v4 생성 (35.3 kg → 31.3 kg)~~ — 새 무게로 모델을 다시 만들었고 치수 검증을 통과했습니다. 예전 모델은 그대로 두어 기존 학습 결과가 깨지지 않게 했습니다. *(요약 보관)*

## 정정된 결론

### 모터 회전 방향 기준

**어떻게 발견했나** — 사용자가 영상을 보고 방향이 반대인 것 같다고 지적해 확인한 결과입니다.

**처음 결론** — 모터 면을 마주보고 시계방향을 +로 계산했습니다.

**바로잡은 결론** — 면을 따라 내려다보며 시계방향이 +입니다. 결과가 완전히 뒤집혀서, 7개 관절 중 5개는 원래 맞았고 2개만 반대인 것으로 정정됐습니다.

*2026-08-27 15:09*

### 가벼운 모델의 충격 수치

**어떻게 발견했나** — 무게가 바뀐 모델을 재면서 발견했습니다. 옛 모델 수치는 전부 유효합니다.

**처음 결론** — 새 모델이 더 부드럽게 착지한다 (1.249배).

**바로잡은 결론** — 실제로는 더 세게 착지합니다 (1.412배). 측정 도구가 몸무게를 옛 모델 기준으로 고정해 두어 12.9% 낮게 나왔습니다.

*2026-08-27 15:09*

### 두 번째 시뮬레이터의 속도 오차 — 절반은 로봇 모형을 잘못 넣은 탓

**어떻게 발견했나** — 착지 힘을 재려고 준비하다가, 두 번째 시뮬레이터에 넣은 로봇이 그 정책이 배운 로봇과 다른 것임을 발견했습니다. 최근에 만든 4 kg 가벼운 모형이 들어가 있었습니다.

**근거 숫자** — 속도 오차 0.29 → 0.14 (원래 엔진 0.14와 동일). 모형 무게 31.32 kg → 35.35 kg

**처음 결론** — 옮겨 심은 걷기가 명령보다 18% 빠르게 걷는다 (속도 오차 0.29).

**바로잡은 결론** — 그 절반 이상이 로봇 모형을 잘못 넣은 탓이었습니다. 정책이 실제로 배운 35.35 kg 모형으로 바꿔 다시 재니 오차 0.14, 명령 대비 8%로 줄었습니다. 나머지 8%는 아직 설명되지 않았습니다.

*2026-08-27 18:40*

### 모델에서 빠진 부품 무게

**어떻게 발견했나** — 사용자가 다시 확인하라고 해서 분류를 처음부터 다시 돌린 결과입니다.

**처음 결론** — 살아있는 부품 11개, 합계 965 g이 모델에서 빠져 있다고 보고했습니다.

**바로잡은 결론** — 실제로는 **11개 873 g**입니다. 앞 숫자에는 이름부터 '시뮬레이션 제외(NoSim)'라고 붙어 있어 일부러 뺀 부품 2개(97 g)가 잘못 섞여 있었습니다. 빠진 873 g의 내역은 어깨 브래킷 543 g, IMU 조립체 전체 165 g(센서보드·케이스·어댑터), 프레임 압출재 160 g, 나사 2개 6 g입니다.

*2026-08-27 19:06*

### 두 시뮬레이터의 충격 수치 차이 — 원인 확정, 갭 해소

**어떻게 발견했나** — 그 22번의 측정 전부가 로봇 팔을 잘못된 자세로 두고 돌아갔습니다. 학습할 때 로봇은 팔을 15도 벌리고 걷는데, 옮기는 과정에서 팔을 몸에 붙인 자세로 굳혀 버렸습니다 — 팔 한쪽이 2.8 kg이고 무게중심이 66.8 mm 안쪽으로 들어가 있었습니다. 고리형 발목 작업을 하다가 발견했고, 팔을 원래 자세로 되돌려 다시 쟀습니다. 먼저 '팔을 예전처럼 붙인 채' 다시 돌려 이전 결과와 소수점 17자리까지 똑같이 나오는지 확인했으므로, 아래 차이는 전부 팔 자세 하나의 효과입니다.

**결과** — 원인은 변환 도구가 몰래 써넣은 계산 정밀도 설정(32/1)이었습니다. 표준값(4/8)으로 바꾸니 피크 차이가 2배에서 10%로 줄었습니다. 22개 조건을 하나씩 바꿔가며 측정했고, 이제 '어떤 숫자를 어떤 조건에서 믿어도 되는가'가 확정됐습니다: 충격량·박자는 무조건, 피크·상승률은 정밀도 설정 병기 시.

**근거 숫자** — 피크 ×1.97→×1.10, 하중률 ×2.92→×1.14, 추종 0.087(원본 0.14보다 좋음)

**처음 결론** — 직렬 발목 로봇에서 계산 정밀도를 4/8로 맞추면 '착지 힘이 실리는 속도'가 원래 시뮬레이터의 1.14배까지 붙는다고 보고했습니다.

**바로잡은 결론** — 1.41배가 맞습니다. 힘의 크기(1.08배)·충격량·걸음 박자는 그대로 유효하고, '실리는 속도' 한 항목만 바뀝니다. 4/8이 8/4보다 낫다는 순위도 그대로입니다(1.41 대 1.50).

*2026-08-27 19:57*

### 착지 힘 2배 차이의 원인 — 내가 지목한 범인이 틀렸다

**어떻게 발견했나** — 원인을 '바닥의 물렁함 설정'으로 지목하고 그 근거로 기존 엔진의 설정값(20 ms)을 들었습니다. 그 뒤 설정을 하나씩 22가지로 바꿔가며 재본 결과, 그 가설이 직접 반증됐습니다 — 실제로 물렁한 바닥 설정을 새 엔진에 넣어 봤더니 최대 힘은 맞춰졌지만 걷는 속도 정확도가 오히려 나빠졌습니다(오차 0.139 → 0.195).

**근거 숫자** — 최대 힘 비 1.97배 → 1.10배, 상승률 비 2.92배 → 1.14배, 속도 오차 0.139 → 0.118. 반증된 가설(물렁한 바닥)을 실제로 넣은 결과: 최대 힘은 맞았으나 속도 오차 0.195로 악화. 측정한 설정 22가지, 기준 재현은 소수점 17자리까지 일치

**처음 결론** — 두 엔진의 착지 최대 힘이 2배 다른 것은 바닥이 얼마나 물렁한가의 설정 차이 때문이다. 기존 엔진은 충돌을 20 ms에 걸쳐 퍼뜨리고, 새 엔진은 딱딱해서 5 ms 한 칸에 몰린다.

**바로잡은 결론** — 원인은 바닥이 아니라 **모델 변환기가 말없이 써 넣은 계산 반복 횟수** 한 줄이었습니다. 변환기가 넣은 값(32/1)은 업계 표준 설정(8/4, 4/4)과 동떨어진, 측정한 것 중 가장 나쁜 값이었습니다. 표준에 가까운 값으로 고치니 두 엔진의 차이가 거의 사라집니다: 최대 힘 1.97배 → 1.10배, 힘 상승률 2.92배 → 1.14배. 걷는 속도 정확도도 함께 좋아졌습니다(0.139 → 0.118).

![[xengine_contact_sweep.png]]

*2026-08-27 19:41*

### 느린 명령 무시 문제 해결 — 문턱값 하나 고쳐서 2.3배 개선

**어떻게 발견했나** — 처음엔 초당 50번 재는 계측기 값(21% 악화)만 있었는데, 이 로봇의 착지 충격은 15~25밀리초 만에 끝나서 초당 50번으로는 대부분 놓칩니다. 초당 200번 재는 계측기로 다시 재니 21%가 아니라 2.2~2.8배였습니다. 비교 대상이 하루 전 측정값이라 계측 코드가 바뀌었을 가능성을 의심해, 기존 로봇도 오늘 코드로 다시 재고 양쪽 모두 2번씩 반복했습니다 — 기존 로봇 값은 어제와 같았습니다. 이 프로젝트의 목적이 하드웨어에 걸리는 힘을 재는 것이고, 베어링과 감속기 수명은 최대 힘보다 '얼마나 급하게 실리느냐'에 민감하기 때문에 넘어갈 수 없는 항목입니다. 실제로 지난주 다른 수정안이 같은 지표 2.6배 악화로 기각됐습니다.

**결과** — '초속 0.25 m로 천천히 걸어라'는 명령을 로봇이 사실상 무시하던 문제를 고쳤습니다. 원인은 점수 규칙의 문턱값 하나였습니다: 느린 명령에서는 '가만히 서 있으면 감점'이라는 규칙이 아예 꺼지도록 되어 있어서, 걷는 쪽이 손해가 되게 산수가 짜여 있었습니다. 문턱값을 낮춘 뒤 32번씩 20초 동안 시켜보니 명령을 따르는 정도가 39%에서 89%로 올랐고, 제자리에 멈춰 있던 시간도 45%에서 19%로 줄었습니다. 걱정했던 부작용 — '멈춰'라고 했을 때 계속 발을 구르는 것 — 은 생기지 않았습니다(멈춤 상태 흔들림 초속 0.191 → 0.189 m로 오히려 미세하게 안정). 빠른 속도(초속 1.6 m)도 그대로였고, 넘어진 횟수는 양쪽 모두 0입니다.

**근거 숫자** — 착지 힘 상승률: 기존 20.6 → 수정판 46~58 (몸무게배/초), 2.2~2.8배 / 최대 착지력 1.22배 몸무게로 변화 없음 / 발 딛는 횟수 초당 2.2회로 변화 없음 / 양쪽 각 2회 반복, 기존 로봇 재측정값이 어제 기록과 일치(21.0 vs 19.6·21.7)

**처음 결론** — 느린 명령 수정이 부작용 없이 통과했다고 적었습니다. 부작용을 '멈춤 명령에서 계속 발을 구르는가'와 '빠른 걸음이 느려지는가' 두 가지로만 확인했고, 둘 다 괜찮았습니다.

**바로잡은 결론** — 착지 부담을 200배 빠른 계측기로 다시 재보니, 발이 땅에 닿을 때 힘이 실리는 '속도'가 기존 로봇의 2.2~2.8배로 나빴습니다. 최대 힘 자체와 발을 딛는 횟수는 그대로인데, 같은 힘을 더 급하게 싣습니다 — 발을 더 딱딱하게 내딛는다는 뜻입니다. 느린 명령 개선은 그대로 유효하지만, 그 대가로 착지가 거칠어졌으므로 **그대로 채택할지는 사람이 판단할 문제**입니다.

*2026-08-27 20:55*

### 연결 한도 측정이 연결을 다시 멈추게 함

**어떻게 발견했나** — 재시작 직후 첫 측정은 성공했는데 곧바로 같은 멈춤 증상이 재발해, 측정 도구의 초과 요청이 원인으로 특정됐습니다.

**처음 결론** — 재시작된 연결에서 먼저 한도를 재고 나서 형상을 받아오면 된다.

**바로잡은 결론** — 한도 측정 자체가 일부러 초과 요청을 던지는 방식이라, 갓 살아난 연결을 다시 멈추게 했습니다. 기본 전송 크기가 이미 한도의 1/6이라 측정이 애초에 불필요했습니다 — 측정 도구는 봉인하고 측정값만 기록했습니다.

*2026-08-27 20:24*

### 느린 걸음 수정 — '부작용 없음' 판정 정정

**어떻게 발견했나** — 덜 정밀한(초당 50회) 채점기는 이 차이를 21%로 축소해 보여줬습니다 — 이전에 문서화된 측정 한계 그대로입니다.

**처음 결론** — 느린 명령 달성률 +128%, 다른 지표 무변화로 채택.

**바로잡은 결론** — 정밀(초당 200회) 측정에서 착지 충격 상승률이 2.2~2.8배로 나타났습니다. 순간 최대 힘과 걸음 박자는 그대로이고 힘이 차오르는 속도만 빨라진 것 — 베어링·감속기 수명에 관련된 값입니다. 시드 3개 복제로 원인을 가리는 중입니다.

*2026-08-27 20:57*

### Fusion MCP 재시작 = 데이터 위험 판정 정정

**어떻게 발견했나** — 사용자가 스크린샷으로 내장 기능임을 보여주고 조사를 지시. netstat 소유자만 보고 애드인이라 단정한 것이 오류였음.

**처음 결론** — MCP가 Fusion 애드인이라 Scripts and Add-Ins에서 stop/start, 또는 Fusion을 종료해야 하며 마스터 문서 손실 위험.

**바로잡은 결론** — 둘 다 틀림. Autodesk 공식 내장 기능(Preferences→API 체크박스)이고, 체크 해제→다시 체크만으로 서버가 in-process 재시작됩니다 — Fusion 안 꺼지고 문서 손실 0. 웹 조사로 확인(공식 문서화된 복구 절차).

*2026-08-28 13:30*

### 관절 부호·부품귀속 수정 프로덕션 반영

**어떻게 발견했나** — 사용자가 8098 뷰어로 재확인 후 직접 지적: 'ROM이 관절 반전하면서 ROM까지 반전되어 틀려짐' + 'Hip Pitch 모터 위치 잘못됨'

**결과** — massprops_fusion.py classify() 3+1건 수정: HipPitchFlange(hip_pitch_link→pelvis), HipYaw2Knee_outer(thigh→hip_roll_link), Shoulder_Stopper_P(shoulder_pitch_link→torso) — 스테이터/스토퍼는 정지측에 붙어야 함. Shoulder-Roll2Yaw-dummy(폐기, 이름 자체가 'dummy')/Shoulder-Pitch2Roll(shoulder_pitch_link→arm)은 2026-09-01 결정을 사용자가 직접 번복 확인 후 반전. build_robot.py JOINT dict 축 5건 반전: hip_roll(1,0,0→-1,0,0), hip_yaw(0,0,-1→0,0,1), knee(0,-1,0→0,1,0), waist_yaw(0,0,1→0,0,-1), R_shoulder_pitch만 단독 반전(L은 정상 확인됨, R쪽에 신규 R-only 음수화 추가). range(lo,hi)는 축 반전과 독립임을 증명(물리 스톱 거리는 부호규약과 무관) 후 변경 없이 그대로 둠 — 재생성 결과 hip_roll[-85,25]·hip_yaw[-45,45]·knee[-120,0] 등 사용자 표와 정확히 일치, assert 통과. 전체질량 35.7070→35.2640kg(재분배+Shoulder-Roll2Yaw-dummy 폐기분 감소). FullDoF loop/proxyfix 프로덕션 3파일씩(mjlab xmls .xml, pygmalion_locomotion .xml/.urdf) 덮어씀, 컴파일 검증+제로포즈 렌더 확인. 뷰어(8098) 재기동해 새 모델 반영

**근거 숫자** — 질량 35.7070→35.2640 kg(−0.443kg, 순수 재귀속+더미폐기). ROM 전부 사용자 표와 정확히 일치(hip_pitch -120/+25·hip_roll -85/+25·hip_yaw ±45·knee -120/0·ankle_pitch -50/+30·ankle_roll ±20)

**처음 결론** — 축만 반전하면 ROM (lo,hi)는 그대로 둬도 된다고 판단(물리 스톱 거리는 부호규약 무관 이라는 이론적 유도) — 실제로는 틀렸음

**바로잡은 결론** — 사용자가 뷰어로 재확인해 ROM도 반전축과 함께 negate+swap 필요함을 확인(hip_roll (-85,25)→(-25,85), knee (-120,0)→(0,120), hip_yaw/waist_yaw는 대칭이라 그대로, R_shoulder_pitch는 L과 분리된 전용 range(-60,90) 신설). 추가로 Hip Pitch 모터(RS04 Hip_R) 원통 placeholder 위치가 8/22 구형 motor_proxies_fusion.json(스테일)을 쓰고 있어 틀렸던 것 확인 — 오늘 날짜 motor_proxies_live_20260902.json(v30 CAD 기준 live fetch, 로컬 캐시로 발견)으로 교체

![[fulldof_zero_pose.png]]

*2026-09-02 21:47*

### 4차 뷰어 재검토 수정: 비대칭 부호 6건 + 브라켓 누락 + IMU + 숄더 재분할

**어떻게 발견했나** — 사용자 지시: 'IMU 메시만큼은 격리 풀고 동료 mjcf에서 가져와' 처리 중 huphy_mjcf에 이름 있는 IMU 서브파츠가 없어 신뢰성 있는 추출 불가 확인 → 대안 제시(질문) → 사용자가 답변 중 Fusion 재연결 증거를 직접 제공

**결과** — 원인=단측(L 또는 R) 만 틀렸는데 코드가 좌우 공유/미러 공식으로 양쪽을 같이 뒤집던 구조적 결함. hip_pitch(R만 반전+range negate-swap 신설), hip_roll(R 미러 제거→L값 그대로 공유), knee(R만 반전 유지+range negate-swap), shoulder_roll(L만 반전+R 미러 제거), crank_A(L만 반전)/crank_B(R만 반전, 태그별 비대칭 확인 — RS03 A/B가 실제로 다른 방향 장착이라 태그마다 다른 게 맞음). R_hip_pitch 브라켓 누락 원인: HipPitchFlange를 pelvis(비-미러 단일바디)로 옮기면서 미러 복제 로직(MIRROR_TO_PELVIS)이 모터바디 전용이라 걸리지 않음 → 질량+메시 양쪽에 명시적 R미러 추가. IMU 누락: E2Box-IMU CAD그룹이 classify()에 아예 매핑 안 돼있어 조용히 드롭됨 → pelvis 귀속(질량은 해결, 메시는 89개 캐시에 IMU바디가 원래 없어서 Fusion 재연결 전까지 원통/투명 표시 불가, 정직하게 보고). shoulder_roll 재분할: Shoulder-Pitch2Roll을 통째로 arm 취급한 게 과했음 — ShoulderR_holderA/B·ShoulderP2R(=roll모터 스테이터 하우징)만 shoulder_pitch_link로 복귀, Arm_Connector*·베어링캡은 arm 유지. 오프라인 재검증: massprops_fusion.py의 classify()/mirror 로직만 캐시데이터로 재실행(Fusion 미연결) 가능함을 확인해 메시까지 재생성(89바디 캐시 재사용, IMU 5바디만 제외). 전체질량 35.264→35.528kg(+IMU 0.264kg 순수 추가)

**처음 결론** — IMU 메시는 Fusion 재연결 전까지 불가(89개 캐시에 원래 없었음, huphy_mjcf도 이름 없는 병합 메시라 신뢰성 있게 추출 불가)

**바로잡은 결론** — 사용자가 직접 Fusion MCP 브리지 LAN 주소(192.168.20.161:27182) 도달성을 curl로 확인해줌 — 재확인해보니 실제로 살아있었음(mcp_client.py 기본값 127.0.0.1:27182는 리버스터널 미가동 상태라 refused였을 뿐). FUSION_MCP=http://192.168.20.161:27182/mcp + FUSION_MCP_HOST=127.0.0.1:27182(윈도우 portproxy가 Host 헤더로 loopback authority를 검증)로 접속 성공. 추가로 meshes_fusion_full.py의 LIST_SCRIPT 자체가 'E2Box-IMU'를 하드코드된 block 리스트에 넣어 라이브 조회 단계에서부터 걸러내고 있던 것 발견(classify() 수정과 별개 문제) — 제거 후 실제 IMU 5바디 fetch 성공, pelvis에 실장

![[fulldof_zero_pose.png]]

*2026-09-02 22:33*

### 5차: R_hip_roll ROM 재분리 + Torso2ShoulderP 복원 + 어깨 ROM 사용자표 반영

**어떻게 발견했나** — 사용자가 8098에서 재확인: 'Torso2ShoulderP 오른쪽도 누락되어있다고'

**결과** — R_hip_roll: 방향(axis)은 이전 라운드에 이미 올바르게 고쳐졌으나 range를 L과 공유하고 있어서 틀림 — HIP_ROLL_R_CAP=(-85,25) 신설(축 불변, range만 R 전용, hip_pitch/knee와 동일 패턴이나 축변화 없이 range만 다른 첫 사례). Torso2ShoulderP: classify()가 '2026-08-26 torso rework 제외' 결정으로 None 반환하던 것을 torso로 라우팅(사용자가 결정 번복 확인). shoulder_pitch/shoulder_roll: 사용자 제공 기계설계표로 DESIGN_CAP 교체(pitch -60~170·roll -15~130, 기존 CAD sweep 최대치(90/30)를 크게 초과) — rom_measured.json이 09월 어깨 리워크(홀더/스토퍼 재분류) 이전 측정이라 stale 판정, DESIGN_CAP_SUPERSEDES_MEASURED 세트 신설해 해당 두 관절만 assert 우회. 질량 35.528→35.601kg(Torso2ShoulderP 73g 순수 추가). 메시도 Fusion 라이브 재fetch(92/92 바디)로 torso.stl 갱신

**근거 숫자** — R_hip_roll range -25/85→-85/25(L과 분리). shoulder_pitch -90/60→-60/170(L), 대응 R=-170/60. shoulder_roll -32/30(CAD sweep)→-15/130(설계표, 양쪽 공유)

**처음 결론** — Torso2ShoulderP를 classify()에서 torso로 라우팅하면 양쪽 다 나오는 줄 알았음

**바로잡은 결론** — torso도 pelvis와 같은 '좌우공유 단일바디'라 한쪽 CAD 데이터를 그대로 넣으면 한쪽만 생김(HipPitchFlange와 동일 함정). Shoulder_Pitch 모터에 이미 있던 torso 미러 규칙에 Torso2ShoulderP 추가(질량), 메시도 동일한 post-link_mesh 스플라이스 기법 재사용(HipPitchFlange 때 만든 splice_mirror 헬퍼로 일반화). 원본/미러 사본 중심좌표 Y=∓88.8mm로 대칭 수학적 확인 후 반영

![[fulldof_zero_pose.png]]

*2026-09-02 22:45*

### 설계 vs 실측 무게 대조 (하체 23.25 kg 실측)

**어떻게 발견했나** — 시뮬레이션 모델의 무게가 실물과 다르면 학습된 걸음을 실물에 옮길 때 오차가 됨

**결과** — 하체는 모델 23.63 vs 실측 23.25 kg — 오차 +0.38 kg(+1.6%)로 허용범위 안이며, 사용자가 확인해준 '나사 수량부족' 하나로 정확히 설명됩니다(하체 나사 전체 1.9 kg의 20% 미장착에 해당). 상체는 모델 12.04 vs 실측 10 kg — +2 kg(+20%) 초과인데, 원인은 목·팔 부품이 CAD에서 임시 재질(철·ABS)로 남아 있어서입니다(실물은 가벼운 3D 출력물). 부수 발견: 학습의 무게 흔들기(랜덤화)가 '실물이 모델보다 가벼운' 방향을 안 감싸고 있어 다음 세대에서 교정 권고.

**근거 숫자** — 하체 +0.38 kg(허용 ±0.5 안), 상체 +2.04 kg(허용 밖), 목 어셈블리만 철 재질 1.61 kg

**처음 결론** — 상체 +2 kg 초과의 원인을 목·팔 부품의 임시 재질(철·ABS)로 판정

**바로잡은 결론** — 사용자 확인 결과 전신 계량 때 전원공급장치(PSU, 1.95 kg)가 빠져 있었음. 이를 반영하면 상체는 모델 10.09 vs 실측 10.0 kg(+0.9%), 전신 33.72 vs 33.25 kg(+1.4%)로 모델이 실물과 1~2% 안에서 일치. 하체 +0.38 kg은 나사 일부 미장착으로 설명(사용자 확인). 목·팔 재질 의혹은 무게 데이터로는 근거 없음 — 부품 재질만 별도 확인.

![[mass_audit_v30_vs_measured.png]]

*2026-09-03 13:37*

- ~~진행 중인 학습이 '왼쪽 무릎이 굳은' 로봇 모델로 돌고 있음 (보행 측정으로 확정)~~ — 본학습을 약 1/3 지점에서 의도적으로 중단했습니다. 걸음걸이 정밀측정 결과, 로봇 모델의 왼무릎이 '명령을 내릴 수 있는 범위'와 '실제로 굽힐 수 있는 범위'가 전혀 겹치지 않게 설정돼 있었습니다 — 왼무릎은 아예 굽힐 수 없는 상태로 학습되고 있었고, 모터는 멈춤턱을 상시 21.8 N·m로 밀며 낭비하고 있었습니다. 사용자가 눈으로 본 '뻣뻣한 걸음'의 주원인입니다. 하중 측정용 데이터로는 무효라 중단이 손실을 줄입니다. 수정(양쪽 다리에 각자 맞는 설정) 후 재학습을 시작합니다. *(요약 보관)*
