# Closed-loop (2-RSU) ankle in RL training — verified claims (raw)

Workflow `closed-loop-ankle-rl-research` 2026-08-23: 6 search lenses (haiku) → 70 claims → 1 adversarial refuter each (haiku) → synthesis (sonnet). 50 kept / 20 dropped. Summary and decision: [docs/91](../91_closed_loop_ankle_rl.md).

## Kept claims

### K1. BRUCE (Humanoids 2025) is the clearest precedent for training directly on closed loops: all three parallel mechanisms (differential pulley, five-bar, four-bar) are simulated natively in GPU MuJoCo (MJX) as soft equality constraints, the policy acts in hardware actuator space, the constraint overhead was only 3.4% per step at 8,192 envs, and policies transferred zero-shot.
- source: https://arxiv.org/html/2507.00273v2
- excerpt: > Unlike prior approaches that rely on simplified serial approximations, we simulate all closed-chain constraints natively using GPU-accelerated MuJoCo (MJX). ... the inclusion of all three parallel mechanism constraints introduced only a 3.4% increase in per-step simulation time relative to the unconstrained model.
- relevance: Direct answer to 'can we': MJX/MuJoCo equality constraints are cheap enough for RL at scale, and a loop-native policy reached hardware zero-shot. Same paper states the competing practice explicitly: 'Researchers have resorted to strategies such as adopting a kinematic rather than actuator joint space or approximating the parallel mechanism as a serial chain' and 'In MuJoCo, all equality constraint
- verifier: ["The BRUCE paper (2507.00273v2) directly supports all four claims: (1) native GPU MuJoCo simulation of all three parallel mechanisms as soft equality constraints is confirmed; (2) the action space mapping directly to hardware actuators is explicitly stated; (3) the 3.4% per-step overhead for all th

### K2. LiPS (arXiv 2503.08349, Tien Kung humanoid, Isaac Gym 4096 envs, legged_gym codebase) trains on a serial ankle but closes the loop inside the training step: the policy outputs parallel-motor targets, a PD runs in motor space, and torque is mapped to the serial joints with a transposed Jacobian; a passive-ankle variant (kp=0) caused the robot to tilt severely and be damaged.
- source: https://arxiv.org/pdf/2503.08349
- excerpt: > many reinforcement learning based humanoid robot control algorithms currently employ open-loop topologies during training, deferring the conversion to series-parallel structures until the sim2real phase. This approach is primarily due to the limitations of physics engines, as current GPU-based physics engines often only support open-loop topologies
- relevance: Middle path between full loop closure and naive serial mapping: do the Jacobian-transpose torque mapping and motor-space PD inside the simulator loop so the policy sees actuator coordinates (Algorithm IV.1: 'τsd ← ComputeSeriesDesiredTorque(Jtranspose τpd); Apply τSeries to the environment'). Also warns that analytic serial-to-parallel conversion at deploy time 'consumes a lot of computational res
- verifier: ['The claim is fully supported by arXiv 2503.08349. The paper explicitly describes LiPS training with a serial ankle model while closing the parallel loop during training via: (1) RL policy outputting parallel motor targets, (2) PD controller in motor space, (3) transposed Jacobian mapping to series

### K3. Booster Gym (arXiv 2506.15132, Booster T1) explicitly chose NOT to simulate the parallel ankle: it trains on a virtual serial structure in Isaac Gym and moves the series-to-parallel conversion (kinematic feedback + transposed-Jacobian torque + PD) into the deployment SDK, citing PhysX's lack of closed-chain support.
- source: https://arxiv.org/html/2506.15132v1
- excerpt: > the module calculates position and velocity feedback for the virtual series joints using the robot's kinematic model and dynamically converts the policy outputs from the series model to the parallel structure using a transposed Jacobian matrix and a PD controller
- relevance: Commercial-vendor precedent for the serial approach; justification given is simulator support, not fidelity: 'the PhysX physics engine used by Isaac Gym has certain limitations, such as the lack of support for closed kinematic chains and less accurate contact force estimation.' Unitree does the same at firmware level — G1 docs: PR mode 'Controls the Pitch (P) and Roll (R) motors of the ankle joint
- verifier: ["The arXiv 2506.15132 paper explicitly supports the claim. It directly states that Booster chose not to simulate the parallel ankle in Isaac Gym (citing PhysX's lack of closed-chain support), trains using a virtual serial structure, and implements series-to-parallel conversion in the deployment SDK

### K4. Digit (Agility) RL work from Berkeley (arXiv 2410.03654, 'Learning Humanoid Locomotion over Challenging Terrain') simulates the four-bar loops with MuJoCo equality constraints on CPU, accepting a slower simulator (~100M samples/day) in exchange for loop fidelity; policies transferred zero-shot.
- source: https://arxiv.org/html/2410.03654v1
- excerpt: > Digit uses four-bar linkages that introduce loops in the kinematic tree. We use the MuJoCo simulator which is able to simulate this using equality constraints. ... While MuJoCo is considerably slower than GPU-based simulators, like IsaacGym, we can afford to use a slower simulator thanks to the sample efficiency of our method.
- relevance: Precedent that constraint-based loop closure in MuJoCo is production-proven for a commercial biped with passive springs; the stated trade-off was only throughput (CPU MuJoCo vs Isaac Gym), which MuJoCo-Warp/mjlab now removes. Same paper notes the passive joints 'are connected via springs and a four-bar linkage mechanism, making them difficult to simulate accurately.'
- verifier: ['The arXiv paper 2410.03654 directly supports all elements of the claim: (1) Digit\'s four-bar linkages are simulated with MuJoCo equality constraints; (2) MuJoCo is ~100M samples/day (exact quote: "~100M samples for fine-tuning, which we are able to acquire in MuJoCo in about a day"); (3) they acc

### K5. The earlier Digit paper from the same group (Science Robotics 2024 / arXiv 2303.03381) trained in Isaac Gym and, lacking loop support, replaced the rods with a high-stiffness 'virtual spring' penalty plus an alternating sub-step length-correction — a third modeling option (penalty/tendon-like) between constraints and serial mapping.
- source: https://arxiv.org/html/2303.03381v2
- excerpt: > Isaac Gym is unable to effectively model these dynamics. To address this limitation, we introduce a 'virtual spring' model with high stiffness to represent the rods.
- relevance: Shows the penalty-spring workaround used when the engine cannot close loops; the authors later moved to MuJoCo equality constraints (finding 5), which is itself evidence about which approach they preferred once sample efficiency allowed it. In MuJoCo terms, this is roughly what a soft 'connect' constraint already does, but with user-tuned stiffness and explicit sub-stepping.
- verifier: ['The arXiv paper (2303.03381v2) directly supports the claim. It explicitly states that Isaac Gym cannot natively model closed kinematic chains and describes their solution: "To address this limitation, we introduce a \'virtual spring\' model with high stiffness to represent the rods. We apply force

### K6. XBot-L (DWL, arXiv 2408.14472, RSS 2024) has a closed-kinematic-chain 2-DoF ankle; because Isaac Gym cannot model it, the authors added two virtual (serial) motors in simulation and remapped targets to the real motors at deployment. Active ankle control (Kp=20, Kd=5) was necessary: passive ankle (Kp=0, Kd=10) failed on hard terrain.
- source: https://arxiv.org/html/2408.14472v1
- excerpt: > its lack of support for the closed kinematic chain employed in our ankle control necessitates the addition of two virtual motors within the simulator. We then remap the joint targets to the actual motors for deployment.
- relevance: Another serial-surrogate precedent with a successful sim-to-real result, showing the approach is workable for a 2-DoF parallel ankle like Pygmalion's; but no fidelity analysis of the mapping is given, and LiPS (finding 3) reports that copying this passive-ankle setting damaged their robot.
- verifier: ["The researcher's claim about XBot-L's closed-kinematic-chain ankle control is well-supported by arXiv 2408.14472. All major assertions are confirmed: the ankle mechanism description, Isaac Gym's inability to model it, the virtual motor workaround with remapping, the specific control gains (Kp=20/K

### K7. LAAS-Gepetto (arXiv 2503.22459, Bipetto robot with Cassie-style knee/ankle loops) argue loop-closure constraints are too costly for control/learning, so they derive an exact analytic non-linear transmission model (differentiable to 2nd order) and train locomotion in Isaac Lab on serial joints, transferring the policy's impedance gains into actuator space; constant-ratio approximations diverge on demanding motions.
- source: https://arxiv.org/html/2503.22459
- excerpt: > the associated loop-closure constraints greatly increase computational cost and limit their use in control and learning. As a result, the non-linear transmission is often approximated by a constant reduction ratio, preventing exploitation of the mechanism's full capabilities.
- relevance: The principled version of the serial route: keep the serial tree but use the exact configuration-dependent transmission (q, qdot, torque, and apparent impedance) rather than a constant lever ratio. Their RL deployment: 'uses the impedance transfer described in section II-D to transfer the impedance gains from the serial-space to the actuator-space.' Companion work (arXiv 2504.00642) states serial 
- verifier: ['The source (arXiv 2503.22459) directly supports the claim. The paper explicitly states that loop-closure constraints increase computational cost and limit their use in control/learning, describes their exact analytic non-linear transmission model (differentiable to 2nd order), and confirms they tr

### K8. ToddlerBot (arXiv 2502.00893, Stanford) models its parallel linkages in MuJoCo with weld equality constraints (plus joint-equality for gears and fixed tendons for bevel couplings), trains walking with PPO in MJX/Brax at 1,024 envs, and reports a small sim-to-real gap with zero-shot transfer.
- source: https://arxiv.org/html/2502.00893v2
- excerpt: > In MuJoCo, we mitigate this by using joint equality constraints for spur gears, fixed tendons for coupled bevel gears, and weld constraints for parallel linkages.
- relevance: Second independent MJX-based RL platform (after BRUCE) that keeps linkages as constraints during GPU training; reported position-tracking error 0.082 m (sim) vs 0.133 m (real). Shows the MuJoCo recipe: weld/connect for rods, joint-equality or tendon for fixed-ratio couplings.
- verifier: ['The arXiv 2502.00893v2 paper explicitly supports all core claims: it uses weld constraints for parallel linkages in MuJoCo, trains with PPO in MJX/Brax at 1,024 envs, and reports zero-shot sim-to-real transfer with quantified metrics. The cited excerpt is verbatim accurate.']

### K9. Agility's Cassie MJCF in MuJoCo Menagerie models BOTH leg loops (achilles rod and plantar/ankle rod) as real closed chains using <equality><connect> point constraints, with a soft default solref of 5 ms; DeepMind's only changes were cosmetic plus switching the solver from PGS to Newton (the loops were kept).
- source: https://github.com/google-deepmind/mujoco_menagerie/blob/main/agility_cassie/cassie.xml
- excerpt: > <equality solref="0.005 1"/> ... <equality> <connect body1="left-plantar-rod" body2="left-foot" anchor="0.35012 0 0"/> <connect body1="left-achilles-rod" body2="left-heel-spring" anchor="0.5012 0 0"/> ... README: "This package contains a simplified robot description (MJCF) of the Cassie bipedal robot." ... "Changed solver from PGS to Newton"
- relevance: The canonical public precedent for training with loop closure in MuJoCo: a vendor-authored biped whose ankle push-rod loop is closed by <connect> on the rod tip, not approximated serially. Shows the minimal recipe (one connect per rod, soft solref, Newton solver) that a 2-RSU ankle could copy — one <connect> per push rod.
- verifier: ['The source code and documentation directly confirm all core technical claims: (1) Both leg loops (achilles rod and plantar rod) are modeled as real closed chains; (2) They use <equality><connect> point constraints; (3) The default solref is 0.005 (5 milliseconds); (4) The solver was changed from P

### K10. The Menagerie Unitree G1 (copied from Unitree's own g1_29dof_rev_1_0.xml) has NO <equality> or <tendon> section: the ankle is a plain serial pitch->roll pair with position actuators, despite the hardware ankle being a parallel mechanism. Unitree H1 likewise has no equality and a single ankle pitch joint.
- source: https://github.com/google-deepmind/mujoco_menagerie/blob/main/unitree_g1/g1.xml
- excerpt: > <joint name="left_ankle_pitch_joint" axis="0 1 0" range="-0.87267 0.5236" actuatorfrcrange="-50 50"/> ... <joint name="left_ankle_roll_joint" axis="1 0 0" range="-0.2618 0.2618" actuatorfrcrange="-50 50"/> ... <position class="g1" name="left_ankle_pitch_joint" joint="left_ankle_pitch_joint"/> README: "Copied the MJCF description from g1_description ... g1_29dof_rev_1_0.xml"
- relevance: Shows the dominant industry practice: the vendor's official training model is serial at the ankle with per-joint torque limits (actuatorfrcrange 50 N·m) standing in for the motor-space limits. H1 (https://github.com/google-deepmind/mujoco_menagerie/blob/main/unitree_h1/h1.xml): <joint name="left_ankle" axis="0 1 0" range="-0.87 0.52"/> with <motor ... ctrlrange="-40 40"/>, no equality. Pygmalion's
- verifier: ['WebFetch of GitHub Menagerie sources confirms: (1) G1 has no <equality> or <tendon> sections; (2) G1 ankle is defined as two serial joints (pitch then roll) with position actuators; (3) H1 likewise has no equality/tendon sections and uses a single ankle pitch joint. All claims verified against pri

### K11. The Booster Gym paper (2025) states the design rationale directly: train on a 'virtual serial structure' because GPU physics engines support it, and convert to the parallel ankle in the SDK with a transposed Jacobian plus PD.
- source: https://arxiv.org/html/2506.15132v1
- excerpt: > "handling closed kinematic chains in parallel ankle mechanisms by implementing a series-parallel conversion module in the SDK" ... "train policies using a virtual serial structure, which is more computationally efficient and widely supported by current GPU-based physics engines" ... "converts the policy outputs from the series model to the parallel structure using a transposed Jacobian matrix and a PD controller."
- relevance: Gives the published justification for the serial approximation and the exact deployment recipe (J^T torque mapping + PD in motor space). Also notes 'Parallel structures are a common design choice for humanoid robot ankles, as they help reduce the inertia of the foot'. Directly answers 'what have others done' for rotary-crank parallel ankles trained with Isaac Gym.
- verifier: ['The Booster Gym paper (arxiv 2506.15132v1, Section III-D) explicitly supports the claim. All three excerpts are accurately quoted from consecutive paragraphs discussing the series-parallel conversion module. The paper directly states the design rationale: train with a virtual serial structure beca

### K12. LiPS (2025, Tien Kung robot) characterises the serial-in-sim approach as 'the currently widely used method', attributes it to GPU engines lacking closed-loop support, and its own contribution still keeps a SERIAL ankle in the simulator — it only moves the Jacobian-transpose motor<->joint torque mapping inside the training loop so the policy acts in parallel (motor) coordinates.
- source: https://arxiv.org/pdf/2503.08349
- excerpt: > "The currently widely used method treats the robot as a serial structure during the reinforcement learning training stage and only performs serial-to-parallel conversion when deploying to the physical robot." ... "1 Initialize robot model with series ankle mechanism; ... 7 Jtranspose ← ComputeTransposedJacobian; 8 τsd ← ComputeSeriesDesiredTorque(Jtranspose τpd); 9 Apply τSeries to the environment"
- relevance: Strongest recent evidence that even groups explicitly targeting parallel-ankle fidelity did NOT simulate the loop; they trained in Isaac Gym (4096 envs) with a serial model and a per-step J^T mapping. Fig. 1 lists Wukong, Optimus, Fourier GR-1 and Tien Kung as parallel-ankle robots. Also reports that a passive-ankle shortcut (kp=0) 'caused the robot to tilt severely, even leading to damage'. Offer
- verifier: ['The source (arxiv.org/html/2503.08349) supports all major claims: (1) characterizes serial-in-sim as "currently" standard in humanoid RL ("typically employ open-loop topologies during training"), (2) explicitly attributes this to GPU engines lacking closed-loop support, (3) LiPS itself trains with

### K13. ToddlerBot (2025) is a public MJCF that keeps real loop closures in its MJX RL-training model: its neck-pitch parallel linkage is closed with paired site-to-site <connect> constraints (two per rod to emulate a weld) with tight solref/solimp, gear couplings use <joint> equality polynomials, and the MJX variant retains the same <equality> block with only 1 solver iteration. Its ankle, however, is serial pitch->roll.
- source: https://github.com/hshi74/toddlerbot/blob/main/toddlerbot/descriptions/toddlerbot_2xc/toddlerbot_2xc_mjx.xml
- excerpt: > <connect site1="closing_neck_pitch_front_1" site2="closing_neck_pitch_front_2" solref="0.004 1" solimp="0.9999 0.9999 0.001 0.5 2"/> ... <joint joint1="left_hip_yaw_driven" joint2="left_hip_yaw_drive" polycoef="0 -0.8571428571 0 0 0"/> ... <option iterations="1" ls_iterations="4"> Paper: "In MuJoCo, we mitigate this by using joint equality constraints for spur gears, fixed tendons for coupled bevel gears, and weld constraints for parallel linkages."
- relevance: Proof that connect-closed linkages survive in a GPU-batched MJX training model (sim2real 'empirically shown a small sim2real gap', arXiv 2502.00893). Relevant to mjlab/MuJoCo-Warp feasibility: equality constraints are compatible with batched training, with the solver cranked down to iterations=1. The site1/site2 form (MuJoCo >= 3.x) is the modern way to anchor a rod end.
- verifier: ['The sources fully support the claim as stated. The GitHub XML file is publicly accessible and contains: (1) four paired site-to-site <connect> constraints for the neck pitch parallel linkage with the exact solref/solimp values cited, (2) nine <joint> equality constraints with the exact polycoef va

### K14. Menagerie's own four-bar example (Robotiq 2F-85 gripper) closes each linkage with <connect>, couples the two driver joints with a <joint> equality, and adds a fixed tendon purely to stabilise the constraint solve — with an in-file comment explaining why.
- source: https://github.com/google-deepmind/mujoco_menagerie/blob/main/robotiq_2f85/2f85.xml
- excerpt: > "This adds stability to the model by having a tendon that distributes the forces between both joints, such that the equality constraint doesn't have to do that much work in order to equalize both joints." <connect anchor="0 0 0" body1="right_follower" body2="right_coupler" solimp="0.95 0.99 0.001" solref="0.005 1"/>
- relevance: DeepMind-curated pattern for four-bar loops: connect + solimp/solref tuning + a helper tendon so the equality solver is not doing the heavy lifting. The comment is a practical warning that connect constraints alone can be numerically fragile under actuation — relevant if the 2-RSU cranks are actuated through the rods at 8k-16k envs.
- verifier: ["The source code fully supports the claim. The Robotiq 2F-85 model in MuJoCo Menagerie uses <connect> elements to close the four-bar linkages, a <joint> equality constraint to couple the driver joints, and a <tendon><fixed> element with explicit XML comment explaining that the tendon adds stability

### K15. Fourier's public GR1T1 description (a robot LiPS explicitly lists as parallel-ankle with rotary actuators) is a plain serial URDF: ankle_pitch then ankle_roll revolute joints with per-joint effort limits, no <mimic>, no loop, no rod/crank links. Berkeley Humanoid's Menagerie model is likewise serial with no <equality>.
- source: https://github.com/FFTAI/Wiki-GRx-Models/blob/master/GRX/GR1/gr1t1/basic_urdf/gr1t1.urdf
- excerpt: > <joint name="left_ankle_pitch_joint" type="revolute"> ... <axis xyz="0 1 0" /> <limit lower="-1.05" upper="0.52" effort="15" velocity="20.32" /> ... <joint name="left_ankle_roll_joint" type="revolute"> <axis xyz="1 0 0" /> <limit lower="-0.44" upper="0.44" effort="30" velocity="20.32" /> (LiPS Fig.1:) "Fourier GR-1 and (d) Tien Kung use rotary actuators but have different structural approaches at the ankle connection"
- relevance: Completes the vendor survey: of the parallel-ankle humanoids with public files (G1, H1, T1, GR1, Berkeley Humanoid), every vendor ships a serial ankle; only Agility (Cassie/Digit) ships loop closure, and Agility's are the only ones authored as MJCF rather than converted from URDF. Note GR1's asymmetric effort limits (15 pitch / 30 roll) are serial-space caps analogous to Pygmalion's lever-ratio to
- verifier: ['The claim is not refuted. All components are verified by the sources examined: (1) The LiPS paper (arXiv:2503.08349) Figure 1 caption explicitly states that Fourier GR-1 is one of four robots that "all use parallel ankle mechanisms" and that it "use rotary actuators"; (2) The GR1T1 URDF is confirm

### K16. MuJoCo (C) does not allow kinematic loops in the tree; loops must be closed with equality constraints, which are soft and solved together with contacts. The docs explicitly warn that representing joints this way is slower and less accurate than tree joints, so only the loop-closing joint (not the whole mechanism) should be an equality.
- source: https://mujoco.readthedocs.io/en/stable/computation/index.html (Equality section) and https://mujoco.readthedocs.io/en/stable/overview.html
- excerpt: > Kinematic loops are not allowed; if loop joints are needed they should be modeled with equality constraints. [...] equality constraints can be used to create "loop joints" [...] The same can be done in MuJoCo but is not recommended – because it leads to both slower and less accurate simulation, effectively turning MuJoCo into a gaming engine.
- relevance: Defines the only native path for the 2-RSU ankle in MuJoCo: keep cranks/rods as tree bodies (hinge + ball joints) and close each rod at the foot with a 3-row `connect`. This is exactly what tools/robot_model/loop_ankle_test.py and pygmalion_v21_loop.xml already do (site-site connect, solref 0.002, dt 1 ms).
- verifier: ['The MuJoCo documentation sources confirm the core claims: (1) kinematic loops are not allowed in the tree; (2) equality constraints must be used instead; (3) the docs explicitly warn this approach is "not recommended" and leads to "slower and less accurate simulation, effectively turning MuJoCo in

### K17. MuJoCo 3.7.0 (April 14, 2026) added the centripetal/Coriolis Jdot·v bias term for connect and weld constraints specifically to stabilize four-bar-type closed mechanisms; the commit measured ~75% reduction in connect-constraint violation. MuJoCo Warp ported it in PR #1294 (merged 2026-05-06), and the mjlab venv's mujoco-warp 3.10.0.1 build contains the Jdotv code.
- source: https://mujoco.readthedocs.io/en/stable/changelog.html (Version 3.7.0) ; https://github.com/google-deepmind/mujoco/commit/412cee20596353aededb991a0896ee295a6ca25f ; https://github.com/google-deepmind/mujoco_warp/pull/1294
- excerpt: > 412cee20 Added the centripetal/Coriolis acceleration term J̇v to the constraint solver bias for connect and weld equality constaints. This significantly improves the stability of constrained mechanisms like four-bar linkages. See Dual problem for details. | jdotv_connect_3d.xml | 1.399e-3 | 5.493e-3 | 74.5% |
- relevance: Any prior negative experience with closed loops drifting at speed in MuJoCo/MJWarp predates this fix. The project's installed stack (mujoco 3.10.0 + mujoco-warp 3.10.0.1, verified in mujoco-sim/mjlab/.venv) already has it, so the loop ankle benefits without an upgrade.
- verifier: ['All major claims are supported by primary sources: (1) MuJoCo 3.7.0 released April 14, 2026, added Jdot·v term for connect/weld constraints per changelog; (2) GitHub commit 412cee20 shows 74.5% reduction for jdotv_connect_3d.xml, matching the claim; (3) MuJoCo Warp PR #1294 merged May 6, 2026; (4)

### K18. MuJoCo Warp's current README claims full MuJoCo feature parity except PGS/noslip solvers, the IMPLICITFAST midpoint feature, PLUGIN actuators/sensors and experimental flex; equality constraints are NOT on the exception list. The MJX feature table lists MJX-Warp Equality = "All" and Solver = "All except PGS, noslip" (i.e. Newton and CG both work with equality rows); MJX-JAX supports CONNECT, WELD, JOINT, TENDON with CG/NEWTON.
- source: https://github.com/google-deepmind/mujoco_warp (README, fetched 2026-08-23) and https://mujoco.readthedocs.io/en/stable/mjx.html (Feature Parity table)
- excerpt: > MuJoCo Warp supports the same features as MuJoCo with the following exceptions: - Integrator: IMPLICITFAST midpoint integrator feature is not supported - Solver: PGS and noslip not yet supported - Actuator / Sensors: PLUGIN types not yet supported - Flex: experimental [...] Equality | All | CONNECT, WELD, JOINT, TENDON [...] Solver | All except PGS, noslip | CG, NEWTON
- relevance: Closed-loop training on mjlab (MJWarp backend, Newton solver default in mjlab MujocoCfg) is officially supported; no feature gap blocks it. Differentiability is the only listed MJWarp hole and is irrelevant for PPO.
- verifier: ['The researcher\'s claim is fully supported by both cited sources. The MuJoCo Warp README confirms equality constraints are not listed as unsupported (exceptions are only IMPLICITFAST, PGS/noslip, PLUGIN, and experimental flex), and the MJX feature parity table explicitly confirms MJX-Warp Equality

### K19. In MJWarp the per-world constraint budget njmax is a strict limit (unlike nconmax), is not parsed from XML, must be passed to make_data/put_data, and memory/compute scale with it; the dense constraint Jacobian is padded to (njmax_pad, nv_pad). Each closed rod adds 3 permanent equality rows (ne) to every world, on top of limits and contacts.
- source: https://mujoco.readthedocs.io/en/latest/mjwarp/index.html (Batch sizes / Performance Tuning) ; mjlab src/mjlab/sim/sim.py SimulationCfg
- excerpt: > It is possible for the number of contacts per world to exceed nconmax if the total number of contacts for all worlds does not exceed nworld x nconmax. However, the number of constraints per world is strictly limited by njmax. [...] Values for nconmax and njmax are not parsed from size/nconmax and size/njmax (these parameters are deprecated).
- relevance: mjlab's Pygmalion config sets cfg.sim.njmax=300 (env_cfgs.py:405); four connects add 12 equality rows per env plus extra joint-limit rows for the new crank/ball joints, so njmax must be re-tuned (mjwarp-testspeed --measure_alloc) or rows silently overflow with NaNs (issue #1510 reporter: "the silent-NaN version of that").
- verifier: ['The claim is supported by the MuJoCo Warp documentation (https://mujoco.readthedocs.io/en/latest/mjwarp/index.html) and mjlab source code. All five major assertions are verified: (1) njmax is strictly per-world limited, (2) not parsed from XML, (3) must be passed to make_data/put_data, (4) memory/

### K20. MJWarp switches its inertia-matrix representation to sparse when nv > 32 under jacobian=AUTO (MuJoCo/MJX use nv >= 60). The closed-loop ankle raises the tree DOF count from 18 (6 free + 12) to 34 (each loop adds a 1-DOF crank hinge and a 3-DOF rod ball joint; two loops per ankle), crossing that threshold.
- source: https://mujoco.readthedocs.io/en/latest/mjwarp/index.html (FAQ: Why is inertia matrix qM sparsity not consistent) ; mujoco-sim/mjlab/src/mjlab/asset_zoo/robots/pygmalion/xmls/pygmalion_v21_loop.xml
- excerpt: > The automatic sparsity threshold utilized by MJWarp for AUTO is optimized for GPU and set to nv > 32, unlike MuJoCo and MJX which use nv >= 60. Dense DENSE and sparse SPARSE settings are consistent with MuJoCo and MJX. This feature is likely to change in the future.
- relevance: A non-obvious performance/determinism consequence of adding the loops: the GPU pipeline changes code path (sparse qM, sparse JTDAJ). Benchmark jacobian=dense vs auto explicitly (mjlab MujocoCfg.jacobian) before comparing throughput to the serial model.
- verifier: ['Both sources fully support the claim. (1) WebFetch of the official MuJoCo documentation confirms: "The automatic sparsity threshold utilized by MJWarp for AUTO is optimized for GPU and set to nv > 32, unlike MuJoCo and MJX which use nv >= 60." (2) The pygmalion_v21_loop.xml file demonstrates the m

### K21. MJWarp computes in float32 and applies the refsafe clamp timeconst >= 2*timestep in its constraint kernels; the docs warn solver behaviour may differ from double-precision MuJoCo and that the solver tolerance is clamped to 1e-6. At mjlab's training timestep of 5 ms the loop's solref=0.002 would be clamped to a 10 ms time constant (unless refsafe is disabled), i.e. a much softer loop than the 1 ms/2 ms standalone test.
- source: https://mujoco.readthedocs.io/en/latest/mjwarp/index.html (Differences from MuJoCo; FAQ) ; mujoco_warp/_src/constraint.py (lines ~112-132) ; https://mujoco.readthedocs.io/en/stable/modeling.html (Solver parameters)
- excerpt: > MJWarp utilizes floats in contrast to MuJoCo's default double representation for mjtNum. Solver settings, including iterations, collision detection, and small friction values may be sensitive to differences in floating point representation. [...] The constraint solver setting tolerance is clamped to a minimum value of 1e-6. [...] if not (opt_disableflags & DisableBit.REFSAFE): timeconst = wp.max(timeconst, 2.0 * timestep)
- relevance: The project note that loops need dt=1 ms and armature means training cost rises ~5x over the current 5 ms step, or the loop must be accepted as a 10 ms-timeconstant spring. Either choice must be validated on the GPU (fp32) path, not just in CPU MuJoCo.
- verifier: ["All three parts of the researcher's claim are directly supported by the opened sources. (1) MJWarp float32 vs MuJoCo double precision is documented in the MuJoCo docs. (2) The refsafe clamp enforcing timeconst >= 2*timestep is implemented in mujoco_warp/_src/constraint.py lines 93-94. (3) The solv

### K22. An open MJWarp issue (July 2026) documents that networks of soft weld equality constraints coupled through contacts oscillate in MJWarp (fp32) where C MuJoCo is quiet (8.6 mm vs 0.04 mm drift), with batched identical worlds diverging; maintainers reproduced it, traced part of it to the convex (CCD) collider, and recommend disabling NATIVECCD for box-box as a mitigation. A related weld quaternion bug was fixed in PR #1513 (2026-07-10).
- source: https://github.com/google-deepmind/mujoco_warp/issues/1510 ; https://github.com/google-deepmind/mujoco_warp/pull/1513
- excerpt: > classic mujoco (fp64/euler): max item drift 0.040 mm / mujoco_warp (fp32/euler): max item drift 8.600 mm [...] Batched worlds aren't just noisy, they diverge: 72 identical worlds of a knife-edge scene spread ~2x in my failure metric. [...] (kbayes) I can repro in Mujoco C as well with nativeccd and libccd.
- relevance: Evidence that equality-constraint behaviour in MJWarp can deviate from C MuJoCo when the loop interacts with contacts (the foot does). Validate the loop ankle on GPU with a CPU-vs-GPU closure-error comparison under foot contact, and keep mujoco-warp >= 3.10.0.2 (post-#1513).
- verifier: ['All major claims are verified: Issue #1510 (July 2026) documents soft weld constraints oscillating in MJWarp (fp32) vs C MuJoCo with exact measurements (8.6 mm vs 0.04 mm drift), batched worlds diverging (~2x spread), maintainer kbayes reproduced it with nativeccd/libccd, traced it to CCD collider

### K23. Body-anchored connect/weld constraints depend on eq_data computed at qpos0; MJWarp's set_const did not recompute eq_data until PR #1487 (merged 2026-07-06, fixing #1270), and MuJoCo itself ignores joint ref values when deriving the second anchor (issue still open). The site1/site2 form avoids eq_data entirely, snaps the sites together at start, and tracks runtime site_pos changes.
- source: https://github.com/google-deepmind/mujoco_warp/pull/1487 ; https://github.com/google-deepmind/mujoco_warp/issues/1270 ; https://mujoco.readthedocs.io/en/stable/XMLreference.html (equality/connect)
- excerpt: > the sites are not required to overlap at the default configuration; if they do not overlap then the sites will "snap together" at the beginning of the simulation. Second, changing the site positions in mjModel.site_pos at runtime will correctly change the position of the constraint (i.e. the content of mjModel.eq_data has no effect when this semantic is used).
- relevance: Confirms the project's site-site connect choice is the robust one for MJWarp (immune to #1270 and to the pre-#1487 set_const bug) and is the right handle for randomizing rod length/anchor geometry via site_pos.
- verifier: ["All components of the claim are verified: (1) PR #1487 merged 2026-07-06 confirms MJWarp's set_const now recomputes eq_data for connect/weld constraints; (2) Issue #1270 confirms body-anchored constraints depend on eq_data computed at qpos0; (3) The exact excerpt about site1/site2 snapping behavio

### K24. PhysX/Isaac Sim articulations must be trees; loops are closed by excluding one joint from the articulation so it becomes a maximal-coordinate joint, which the solver treats with lower priority and which accumulates the most error. Isaac Lab maintainers (2026) advise RL users to reduce the timestep first and use ~8-16 position / 2-4 velocity iterations; a user reported needing 64/32 iterations for good loop behaviour, which is impractical for many parallel envs.
- source: https://docs.isaacsim.omniverse.nvidia.com/6.0.0/robot_setup_tutorials/rig_closed_loop_structures.html ; https://nvidia-omniverse.github.io/PhysX/physx/5.6.0/docs/Articulations.html ; https://github.com/isaac-sim/IsaacLab/issues/4593
- excerpt: > Articulations must be kinematic trees, but there is no need to delete any joints. To eliminate those warnings, you must choose one joint to exclude from the Articulation and have it be treated as a maximal coordinate joint. Because maximal coordinate joints are treated with a lower priority by the solver, it is the joint that accumulates the most error in simulation.
- relevance: Explains why the Isaac Lab Cassie asset (UMich simplified URDF, direct ankle/toe actuation) and most Isaac Lab humanoids drop the closed loops entirely: PhysX can close loops but at an accuracy/iteration cost that scales badly for RL. MuJoCo's equality path is comparatively first-class.
- verifier: ['The core technical claims about Isaac Sim closed-loop handling ARE supported by source 1 (Isaac Sim documentation): articulations must be kinematic trees, one joint is excluded to become maximal-coordinate, and maximal-coordinate joints are treated with lower priority and accumulate the most error

### K25. mjlab's maintainers (Kevin Zakka, Apr 2026) refuse ball joints as a primitive and state linkages (G1 ankle four-bar) can be modeled accurately without them; they list four supported routes — spatial tendons, equality connect, precomputed torque tables, serial-chain approximation — and promised a G1 linkage training example/writeup for v1.5.0 (not yet found in docs as of v1.6.0, Aug 2026).
- source: https://github.com/mujocolab/mjlab/issues/918
- excerpt: > We do not plan to support ball joints as a general primitive... a free joint is not required to model linkages accurately. I have a writeup covering the motivation, the Jacobian analysis, and several modeling approaches (spatial tendons, equality connect, precomputed torque tables, serial-chain approximation) ... how to train a policy on a robot with linkages
- relevance: Direct statement from the framework you train in: all four options (including true loop closure via connect) are considered viable in mjlab today; the rod-end ankle maps naturally onto 'spatial tendon' (rod as massless tendon) or 'equality connect' (rod as body). Watch this issue / releases for the promised G1 example before building your own.
- verifier: ["GitHub issue #918 (opened by Kevin Zakka on April 16, 2026) directly supports all elements of the claim: it explicitly states mjlab will not support ball joints as a general primitive, confirms that linkages can be modeled accurately without them, lists all four named approaches (spatial tendons, 

### K26. mjlab's current documented practice for the G1's dual-motor parallel ankle is the serial approximation: model ankle_pitch/ankle_roll as hinge joints and lump both motors into each joint (stiffness, damping, effort_limit and armature all ×2) — essentially what Pygmalion does today with its lever-ratio mapping.
- source: https://github.com/mujocolab/mjlab/blob/main/docs/source/actuators.rst
- excerpt: > For joints driven by parallel linkages (like the G1's ankles with dual motors), the effective armature in the nominal configuration can be approximated as the sum of the individual motor armatures: ... stiffness=STIFFNESS_5020 * 2, damping=DAMPING_5020 * 2, effort_limit=ACTUATOR_5020.effort_limit * 2, armature=ACTUATOR_5020.reflected_inertia * 2
- relevance: Shows the mainstream mjlab/G1 baseline is serial with summed motor properties at the nominal pose; the doc itself flags 'parallel linkages' as the case where per-joint overrides are awkward. Your lever-ratio approach is consistent with this; the known gap is that ×2 is only valid at the nominal configuration (no configuration-dependent Jacobian).
- verifier: ['The mjlab documentation at /home/syaro/MikuchanRemote/Human-Pygmalion/mujoco-sim/mjlab/docs/source/actuators.rst (lines 414–429) explicitly supports the claim. The "Parallel linkage approximation" section states: "For joints driven by parallel linkages (like the G1\'s ankles with dual motors), the

### K27. A PhD student simulating a biped with connect-constraint closed chains (spatial 6-bar/5-bar) in MuJoCo 3.2.4 could not stabilize it even at dt=1e-4/1e-5 with RK4/Euler; MuJoCo lead Yuval Tassa replied that stabilizing linkages is hard, the inertias were wrong, and recommended replacing light non-colliding link bodies with spatial tendons, which are cheaper and much more stable than connect constraints.
- source: https://github.com/google-deepmind/mujoco/discussions/2268
- excerpt: > stabilizing linkages is hard ... your inertias are all wrong (visualize them in simulate and you'll see what I mean) ... [spatial tendons are] both cheaper and much more stable than connects
- relevance: Authoritative practitioner guidance for exactly your topology (push rods between crank and foot): model each rod as a spatial tendon with a length equality/limit rather than a rod body + two connect constraints; tiny link inertias are the usual cause of blow-ups. Trade-off: tendons cannot collide.
- verifier: ['The source fully supports the claim. GitHub discussion #2268 contains a PhD student\'s question about simulating a bipedal robot with MuJoCo 3.2.4 using connect-constraint closed chains (spatial 6-bar/5-bar linkages). The student explicitly states attempting stabilization with timesteps 0.0001/0.0

### K28. On GPU MuJoCo (MJX), Cassie's and Digit's equality-connect loop closures were visibly violated when solver iterations/line-search iterations were reduced for speed; an actual MJX equality-solver bug was found and fixed (Oct–Nov 2023), but users still reported drifting loops afterwards.
- source: https://github.com/google-deepmind/mujoco/issues/1129
- excerpt: > I am having the same issue with the agile robotics digit which has closed loop chains in its legs which are implemented using equality connect constraints. The more I bring down the constraint solver's iteration and ls_iteration, the more these constraints are broken.
- relevance: Practitioner evidence that loop closure quality on GPU backends is directly tied to solver iteration budget, which RL pipelines (mjlab default: few Newton iterations) deliberately keep low. If you add loops, re-tune iterations/ls_iterations and monitor constraint violation as a training metric.
- verifier: ['The GitHub issue #1129 fully supports the claim. Evidence: (1) HyunyoungJung\'s comment of Oct 26, 2023 mentions Digit with closed-loop chains and explicitly states "The more I bring down the constraint solver\'s iteration and ls_iteration, the more these constraints are broken"; (2) erikfrey conf

### K29. In MuJoCo-Warp (fp32), networks of equality constraints coupled through contacts exhibit sustained mm-scale oscillation that classic fp64 MuJoCo does not (0.04 mm vs 8.6 mm drift), non-monotonic response to solref softening, and world-to-world divergence of identical envs (Jul 2026); maintainers confirmed and offered only partial mitigations. A separate open issue (#1270, Mar 2026) shows connect-constraint second anchors are computed ignoring joint ref positions, with Tassa advising site-semantic connects.
- source: https://github.com/google-deepmind/mujoco_warp/issues/1510
- excerpt: > classic mujoco (fp64/euler): max item drift 0.040 mm; mujoco_warp (fp32/euler): max item drift 8.600 mm ... softening the welds behaves non-monotonically — timeconst=0.05 rings worse than 0.02 ... Batched worlds aren't just noisy, they diverge: 72 identical worlds ... spread ~2×
- relevance: This is the backend mjlab runs on. An ankle loop that is loaded through foot-ground contact is exactly 'equality constraints coupled through contacts'; expect fp32 ringing, nondeterminism across envs, and solref tuning that does not behave intuitively. Use site-based connects (not body anchors) and verify loop closure at non-zero keyframe poses (see https://github.com/google-deepmind/mujoco_warp/i
- verifier: ['All major factual claims are supported by GitHub sources. Issue #1510 confirms the specific drift measurements (0.04 mm classic MuJoCo vs 8.6 mm MuJoCo-Warp), non-monotonic solref behavior, and batched world divergence (~2×). Maintainers (thowell, kbayes) confirmed and offered partial mitigations 

### K30. BRUCE (Jul 2025) is the clearest success case of training RL with ALL closed chains native in GPU MuJoCo (MJX): hip differential (tendon equality), five-bar and four-bar (connect), soft constraints with a deliberate deadband to model backlash (og_bruce: solref='0.005 1.05', solimp='0.2 0.95 0.002 0.9 6'), actions in motor space, passive joints NOT in the observation, only +3.4% sim time; but they admit no controlled comparison against the serial approximation.
- source: https://arxiv.org/html/2507.00273
- excerpt: > Although we observe qualitative gains from closed-chain modeling, a controlled RL comparison against simplified serial/kinematic models under identical training and domain-randomization settings is deferred to future work to quantify the effect more precisely.
- relevance: Proof-of-feasibility on a GPU MuJoCo backend with concrete constraint parameters (https://github.com/alvister88/og_bruce) and design choices you can copy (motor-space actions, start from a feasible keyframe, deadband for backlash — they found policies trained without backlash were brittle on hardware). Caveat: the benefit over your current serial+lever model is unquantified even by its authors.
- verifier: ['The arxiv paper (2507.00273) fully supports the core claim: BRUCE does train RL with three parallel closed-chain mechanisms (hip differential, five-bar linkage, four-bar linkage) natively in GPU MuJoCo with soft constraints, deadband impedance, motor-space actions, and only 3.4% sim overhead. The 

### K31. Booster Robotics (T1, parallel ankle) deliberately trains in a virtual SERIAL ankle model on GPU (Isaac Gym) and does the series→parallel conversion in the SDK at deploy time: virtual joint pos/vel from forward kinematics, policy output converted by transposed Jacobian plus PD in motor space.
- source: https://arxiv.org/html/2506.15132
- excerpt: > the module calculates position and velocity feedback for the virtual series joints using the robot's kinematic model and dynamically converts the policy outputs from the series model to the parallel structure using a transposed Jacobian matrix and a PD controller. This approach reduces the sim-to-real gap and simplifies deployment
- relevance: Industry-standard answer to 'should we train with loops': a commercial humanoid with a 2-motor parallel ankle ships with serial-in-sim + J^T-at-deploy. Your lever-ratio mapping is the 1-D special case; upgrading it to the full configuration-dependent J(q)^T (pitch/roll to crank A/B) captures most of what the loop would, without any constraint-stability cost.
- verifier: ['The arxiv paper (2506.15132) explicitly confirms the claim. It describes Booster Robotics T1 training with a virtual serial ankle model for computational efficiency, then converting to the parallel structure at deployment using forward kinematics, transposed Jacobian matrix, and PD control—exactly

### K32. A new Aug 2026 paper (Hong et al., 'Simulator-Side Dynamics Normalization') quantifies what the serial surrogate + Jacobian mapping misses for a parallel-link leg — configuration-dependent redistribution of actuator inertia/damping and omitted linkage inertia (inertia term +76% at one pose) — and fixes it by injecting compensation torques into the serial sim, cutting hardware velocity-tracking RMSE 56%; they explicitly avoid constraint-based closed-chain modeling because solver/constraint parameters do not map to hardware properties.
- source: https://arxiv.org/html/2608.01697
- excerpt: > in such a constraint formulation, loop-closure accuracy and dynamic response depend on the constraint and solver settings. Because these numerical settings do not directly correspond to the robot's physical parameters, appropriate values are difficult to determine solely from hardware characteristics
- relevance: Gives you a third option between 'serial+lever' and 'true loops': keep the serial tree but add configuration-dependent armature/damping coupling derived from the crank-rod Jacobian. Also the most recent argument that soft-constraint loops are hard to identify against hardware — relevant since your goal is load measurement for design, where constraint softness directly corrupts rod/crank force esti
- verifier: ['The source paper (Hong et al., arxiv 2608.01697v2) substantially supports all major claims. Verified: paper title and authors; quantification of inertia redistribution and omitted linkage inertia; +76.5% inertia term at 90° pose; compensation torque injection methodology; 55.7% forward-velocity RM

### K33. Berkeley's Digit terrain-walking work (Radosavovic et al., 2024) trained and evaluated directly in CPU MuJoCo with the four-bar/knee loops as equality constraints, accepting MuJoCo's slower speed for fidelity, and treated passive joints (toes) as passive.
- source: https://arxiv.org/html/2410.03654v1
- excerpt: > Digit uses four-bar linkages that introduce loops in the kinematic tree. We use the MuJoCo simulator which is able to simulate this using equality constraints. While MuJoCo is considerably slower than GPU-based simulators, like IsaacGym, we can afford to use a slower simulator ... We model the knees via equality constraints.
- relevance: Classic Cassie/Digit lineage (Agility's own cassie.xml uses connect constraints with equality solref='0.005 1'): loop-closure RL training is routine on CPU MuJoCo at small timesteps. The practitioner pattern is fidelity-on-CPU for fine-tuning/validation and simplified models on GPU for bulk training — a sim-to-sim check you could adopt instead of moving loops into mjlab.
- verifier: ['All key claims are directly supported by the source. The paper explicitly states: (1) they used MuJoCo with equality constraints for knee loops ("We model the knees via equality constraints"), (2) training was CPU-based parallelization, (3) they accepted MuJoCo\'s slower speed relative to IsaacGym

### K34. LAAS (Lutz et al., 2025) trained a policy that outputs serial-joint offsets, then transferred serial-space PD impedance to actuator space analytically; sending the mapped motor torques directly (without the actuator-space impedance) made the robot fall, and they validated the Isaac-trained policy in a MuJoCo model WITH the closed loops before hardware.
- source: https://arxiv.org/html/2503.22459
- excerpt: > To validate our method, we also tested to send the reference torques τm* directly to the actuators, which led to dramatic failure, causing the robot to fall.
- relevance: Warns that the motor-vs-joint action-space question is really about where the PD loop closes: J^T torque mapping alone is not enough — the stiffness/damping must be transformed to motor coordinates (J^T K J), which your lever-ratio currently does only as a scalar. Also endorses keeping a loop-closure MuJoCo model as a sim-to-sim gate rather than as the training sim.
- verifier: ['The source (Lutz et al., 2025 / arxiv.org/html/2503.22459) directly supports all elements of the claim. The paper explicitly states the policy outputs serial-joint offsets, describes analytical impedance transfer from serial to actuator space, reports that sending motor torques directly without im

### K35. Unitree G1 exposes the ankle in two command modes at the SDK level: PR (serial pitch/roll, the default) and AB (raw parallel motors). The same motor indices are aliased to both names, so the onboard firmware performs the serial-to-parallel conversion when mode_pr=PR; the conversion itself is closed-source and not in the SDK.
- source: https://github.com/unitreerobotics/unitree_sdk2/blob/main/example/g1/low_level/g1_ankle_swing_example.cpp
- excerpt: > enum class Mode { PR = 0, // Series Control for Ptich/Roll Joints AB = 1 // Parallel Control for A/B Joints }; ... LeftAnklePitch = 4, LeftAnkleB = 4, LeftAnkleRoll = 5, LeftAnkleA = 5, ... dds_low_command.mode_pr() = static_cast<uint8_t>(mode_pr_);
- relevance: Verified directly from the file (371 lines). This is the industry-default deployment pattern for a serial-trained policy: the policy writes q/kp/kd to 'AnklePitch'/'AnkleRoll' slots and the robot maps to motors A/B internally. For Pygmalion it means we would have to write the equivalent firmware layer ourselves (RobStride motors have no such mode); the code shows the interface shape to imitate (a 
- verifier: ['The GitHub source code (unitreerobotics/unitree_sdk2, g1_ankle_swing_example.cpp) confirms all four points of the claim: (1) dual Mode enum with PR=0 and AB=1, (2) motor index aliasing (LeftAnklePitch/LeftAnkleB both index 4, LeftAnkleRoll/LeftAnkleA both index 5), (3) mode_pr_ passed to firmware 

### K36. Unitree's own documentation (mirrored by Weston Robot) states PR mode corresponds to the URDF serial model and that AB mode requires the user to do the parallel kinematics. The unitree_rl_gym real-robot deploy script therefore simply sets MotorMode.PR and writes policy targets plus kp/kd to the ankle pitch/roll indices with no linkage code at all.
- source: https://docs.westonrobot.com/tutorial/unitree/g1_dev_guide/
- excerpt: > PR Mode: Controls the Pitch (P) and Roll (R) motors of the ankle joint (default mode, corresponding to the URDF model). AB Mode: Directly controls the A and B motors of the ankle joint (requires users to calculate the parallel mechanism kinematics themselves).
- relevance: Confirms the serial URDF/MJCF is the intended training model for G1 and the vendor absorbs the conversion. In unitree_rl_gym/deploy/deploy_real/deploy_real.py the only linkage-related lines are `self.mode_pr_ = MotorMode.PR` and a loop writing `motor_cmd[motor_idx].q/kp/kd` for leg joints. So the published G1 RL repos contain no reusable ankle IK/Jacobian code; the conversion has to come from else
- verifier: ['The source documentation (https://docs.westonrobot.com/tutorial/unitree/g1_dev_guide/) explicitly confirms the claim. PR Mode is documented as controlling "the Pitch (P) and Roll (R) motors of the ankle joint (default mode, corresponding to the URDF model)" and AB Mode is documented as requiring u

### K37. Booster Gym's real-robot deploy loop contains a concrete trick for the ankle: for the parallel-mechanism indices it sets the commanded position equal to the current position, zeroes kp, and sends the serial-space P-term torque kp*(q_target - q) as feedforward tau, so that the SDK maps torque (linear under J^T) rather than a position target (non-linear under the linkage). Only the D-term remains onboard.
- source: https://github.com/BoosterRobotics/booster_gym/blob/main/deploy/deploy.py
- excerpt: > # Use series-parallel conversion for torque to avoid non-linearity
for i in self.cfg["mech"]["parallel_mech_indexes"]:
    motor_cmd[i].q = self.dof_pos_latest[i]
    motor_cmd[i].tau = np.clip((self.filtered_dof_target[i] - self.dof_pos_latest[i]) * self.cfg["common"]["stiffness"][i], -self.cfg["common"]["torque_limit"][i], self.cfg["common"]["torque_limit"][i])
    motor_cmd[i].kp = 0.0
- relevance: Directly usable design for our RobStride-based controller: compute tau_s = Kp_s*(q_s* - q_s) in serial coordinates at the policy/publish rate, map tau_m = J^T(q_s) tau_s, send as feedforward, and keep only damping in the motor loop (T1.yaml: ankle stiffness 50, damping 3, torque limits 24/15 Nm; parallel_mech_indexes [15,16,21,22]; 500 Hz publish with 0.8/0.2 target low-pass). It also shows what B
- verifier: ["The GitHub source code (deploy/deploy.py, lines 213-221) explicitly implements the claimed technique: for parallel mechanism indices, it sets motor_cmd[i].q to current position, zeroes motor_cmd[i].kp to 0.0, and sends feedforward tau computed as (target - actual) * stiffness. This architecture mo

### K38. LiPS (Tien Kung humanoid, Beijing Humanoid Robotics Center, arXiv 2503.08349, Mar 2025) keeps a serial ankle in IsaacGym but places the policy action and PD in parallel (motor) coordinates, converting the motor-space PD torque to serial torque with J^T inside the simulator every step; at deployment the policy output goes straight to the motors with no online IK. The paper gives the analytic vector-loop IK for a two-crank/push-rod ankle and catalogues why the two common deployment-side conversions misbehave.
- source: https://arxiv.org/pdf/2503.08349
- excerpt: > a. Using torque equivalence ... solving the analytical solutions at the same frequency as the joint control on the robot side consumes a lot of computational resources and may result in no solutions. b. Using position equivalence ... if the encoder precision at the joint side is poor, the error will be amplified, causing the robot to jitter.
- relevance: Most relevant geometry match: LiPS eqs (4)-(7) are the vector-loop IK for active arm L1, passive rod L2, hinge points P_i and offsets r1 — i.e., our crank + push-rod + U-joint ankle; eq (12) gives J row-wise as (C_iP_i^T R_Pi)/(B_iC_i x C_iP_i)_y. Their alternative to 'map at deployment' is 'map inside the sim': action = crank targets, PD in crank space, tau_s = J^T tau_pd applied to the serial jo
- verifier: ["The paper fully supports the claim. Algorithm IV.1 and Section V.B confirm: (1) LiPS trains with a serial ankle model in IsaacGym, (2) the policy and PD controller operate in parallel/motor coordinates, (3) the conversion from motor-space torque to serial torque uses J^T inside the simulator at ev

### K39. Lutz et al. (LAAS-CNRS, Bipetto robot, arXiv 2503.22459 v2, Oct 2025) give the full closed-form deployment layer for an 'intricate four-bar' 2-DoF ankle (two shin motors, rods, foot U-joint, 'as in H1, G1, GR1, Digit, T1'): torque transfer tau_m* = J_A(q_s)^-T tau_s*, gain transfer K_Pm = A_Pm + J_A^-T K_Ps J_A^-1 + C_Pm, K_Dm = J_A^-T K_Ds J_A^-1, a consistent motor reference q_m* = K_Pm^-1[tau_m* + K_Dm qdot_m + K_Pm q_m], and serial-state estimation q_s = argmin ||f(q_s) - q_m||^2 with gradient 2 J_A^T (f(q_s) - q_m). Policy 30 Hz, gains updated 100 Hz, motor loop ~1 kHz, under 10 ms in pure Python on a Raspberry Pi 5.
- source: https://arxiv.org/pdf/2503.22459
- excerpt: > To validate our method, we also tested to send the reference torques τm* directly to the actuators, which led to dramatic failure, causing the robot to fall. ... Previous studies [20] adopted a similar approach, while neglecting the Apm and Cpm terms by assuming that (qs* − qs) remains small. However, this assumption is not valid in the context of reinforcement learning.
- relevance: This is the most complete published recipe for our exact situation: serial model in Isaac Lab, policy outputs serial position offsets, constant serial Kp/Kd in training, and an impedance-transfer layer on hardware. Two lessons transfer directly to Pygmalion: (1) pure feedforward tau_m without re-derived motor-space gains falls over, so a lever-ratio torque map alone is not a deployment plan; (2) t
- verifier: ['The paper fully supports the claim across all technical and factual elements: (1) Authorship (Lutz et al. at LAAS-CNRS, Bipetto robot), date (arXiv 2503.22459 v2, 8 Oct 2025), and mechanism (intricate four-bar 2-DoF ankle matching H1, G1, GR1, Digit, T1) are correct. (2) Torque transfer τ_m* = J_A

### K40. Hong et al. (arXiv 2608.01697, Aug 2026) formalize the baseline 'serial-tree surrogate + Jacobian mapping' (q^p = J q^s, qdot^p = J qdot^s, tau^s = J^T tau^p) and show what it omits: the coordinate-induced redistribution of actuator inertia/damping and the linkage inertia dropped by the serial reduction. Their fix (S3N) keeps the serial tree but adds these terms to the simulator; on a 2-DoF parallel-link leg it cut joint-position and torque RMSE by 80.9% and 82.1% versus the Jacobian-only baseline.
- source: https://arxiv.org/abs/2608.01697
- excerpt: > Conventional Jacobian-based state and torque mappings preserve consistency with the kinematic and virtual-work relations but do not account for the coordinate-induced redistribution of actuator inertia and damping and the linkage inertia omitted during serial-tree reduction. ... S3N-Full reduced the joint-position and torque RMSEs by 80.9% and 82.1%, respectively, relative to the Jacobian-mapping baseline.
- relevance: Quantifies the residual sim-to-real gap of the 'keep serial, map at deployment' strategy even when the Jacobian is exact: the serial ankle in mjlab sees a constant armature/damping, whereas the real reflected inertia is J^T M_act J(q_s), which varies with pitch/roll. For Pygmalion this argues for (a) setting the serial ankle joints' armature and damping from J^T diag(I_RS03, b_RS03) J at the nomin
- verifier: ['The paper Hong et al. (arXiv 2608.01697, Aug 2026) fully supports the claim. It formalizes the serial-tree + Jacobian baseline, identifies precisely the omitted terms (coordinate-induced redistribution of inertia/damping and linkage inertia), proposes S3N as the solution, and reports exactly the c

### K41. The classic Cassie MuJoCo model (OSU DRL, used by Berkeley Hybrid Robotics' cassie_rl_walking) does not use a serial surrogate: it models the plantar rod and achilles rod bodies and closes the loops with `<equality><connect>` constraints. Notably the foot actuator is attached to the output foot hinge (gear 50), not to the foot-crank that the real motor drives, so even this 'closed-loop' reference model places torque on the serial output joint and leaves the crank/rod loop as a passive kinematic coupling.
- source: https://github.com/osudrl/cassie-mujoco-sim/blob/master/model/cassie.xml
- excerpt: > <equality>
<connect name = 'left-plantar-rod-eq' body1='left-plantar-rod' body2='left-foot' anchor='0.35012 0 0'/>
<connect name = 'left-achilles-rod-eq' body1='left-achilles-rod' body2='left-heel-spring' anchor='0.5012 0 0'/>
...
<motor name='left-foot' joint='left-foot' gear='50' ctrllimited='true' ctrlrange='-0.9 0.9' user='5500'/>
- relevance: Shows the hybrid option available in MuJoCo-family simulators (MJX/Warp list CONNECT and WELD equality types): keep the real rods and cranks in the model for correct kinematic coupling and rod loads, but actuate the serial output joint so the policy action space stays pitch/roll. Cassie's 1-DoF toe is simpler than our 2-DoF ankle, and the `solref='0.005 1'` soft constraint is a known source of com
- verifier: ['The cassie.xml source file from OSU DRL confirms all three aspects of the claim: (1) plantar and achilles rod bodies are connected via equality constraints, (2) foot motors attach to the output foot joint with gear 50, not to any crank, and (3) the crank/rod/foot loop is therefore a passive kinema

### K42. Fourier abandoned the parallel ankle for GR-2: the joint structure was changed from parallel to serial, explicitly citing simpler control, lower maintenance, and easier transfer from simulation to the real robot.
- source: https://interestingengineering.com/innovation/fouriers-gr-2-upgraded-humanoid-robot
- excerpt: > Fourier changed the robot's joint structure from parallel to serial, streamlining the control mechanism, reducing maintenance expenses, and enhancing its capacity to go from AI simulations to practical applications.
- relevance: A design-direction data point rather than a conversion method: one vendor that shipped a parallel ankle (GR-1, listed among parallel-ankle humanoids in both LiPS Fig. 1 and the LAAS paper's Fig. 1) judged the serial-model/parallel-hardware mismatch costly enough to redesign. Berkeley Humanoid likewise chose unitary transmissions to avoid a conversion layer (cited as [14] in arXiv 2503.22459). For 
- verifier: ["The interestingengineering.com article directly supports the claim. It explicitly states that Fourier changed GR-2's joint structure from parallel to serial, and cites the exact three reasons: streamlined control mechanism, reduced maintenance expenses, and improved simulation-to-reality transfer 

### K43. The repo already has a working closed-loop 2-RSU ankle model in plain MuJoCo: tools/robot_model/loop_ankle_test.py builds, on the LEFT leg of pygmalion_v2.xml, a crank hinge per RS03 axis, a ball-jointed push rod on each crank pin, and closes each loop with a site-to-site `equality/connect` between the rod-end site and a foot ball site (written to pygmalion_v21_loop.xml).
- source: /home/syaro/MikuchanRemote/Human-Pygmalion/tools/robot_model/loop_ankle_test.py
- excerpt: > DOF accounting: each loop adds 1 (crank hinge) + 3 (rod ball) and removes 3 (connect), so the two loops add 2 DOF - the same two the serial pitch/roll joints already have. With the cranks driven, the serial joints become passive and follow the linkage.
- relevance: Answers 'can we' at the MuJoCo-CPU level: the mechanism is modelled with mjEQ_CONNECT (lines 70-76, solref 0.002) and the serial pitch/roll joints are kept as passive joints, so the existing pitch/roll sensors/rewards could in principle still read them. Note the +2 surplus DOF are the passive rod spins about their own axes (damped 0.02).
- verifier: ['Source file /home/syaro/MikuchanRemote/Human-Pygmalion/tools/robot_model/loop_ankle_test.py directly supports all claim elements: (1) constructs crank hinges on L_shin_link (lines 49–54, hinge joints axis=[0,1,0]); (2) adds ball-jointed rods as children of cranks (lines 58–66, mjJNT_BALL); (3) clo

### K44. Two hard-won solver lessons from the loop feasibility test: the light intermediate body (ankle cross 0.067 kg) plus the closed loop needs joint `armature` (0.005) and dt = 1 ms or the solver jumps to the other IK branch; and MjSpec actuators must set `biastype=AFFINE` explicitly, otherwise biasprm is ignored and the 'servo' is a pure torque motor (the first attempt's runaway).
- source: /home/syaro/MikuchanRemote/Human-Pygmalion/docs/87_robot_model_v2.md
- excerpt: > ⚠교훈 둘: ① 가벼운 중간체(십자 0.067 kg)+폐루프는 `armature`(0.005)와 dt 1 ms 없이는 솔버가 반대 분기로 넘어간다. ② **MjSpec 액추에이터는 `biastype=AFFINE`을 명시해야** 위치서보가 된다
- relevance: Directly constrains a training integration: the RL sim currently runs at a coarser physics step than 1 ms; a loop ankle may force dt=1 ms (throughput cost) and branch-flip robustness must be checked under contact/push, not only in the welded-shin test. pygmalion_v21_loop.xml line 4 hard-codes `<option timestep="0.001"/>`.
- verifier: ['Both claims are explicitly supported by the source. The document states (§5, lines 119-121) that the light intermediate body (ankle cross 0.067 kg) plus closed loop requires `armature` (0.005) and dt 1 ms to prevent the solver from jumping to the alternate IK branch, and that MjSpec actuators must

### K45. docs/87 explicitly defers RL integration of the loop (v2.1) as a separate stage because it changes the action space (ankle pitch/roll -> crank A/B) and breaks every task regex/reward that references the ankle joints; the stated payoff is measuring rod axial force and stopper loads directly in sim. It also names the structural limitation of the serial model: JS6 cone, crank dead-centre and stop pads are absent, so a policy can learn poses the mechanism cannot reach.
- source: /home/syaro/MikuchanRemote/Human-Pygmalion/docs/87_robot_model_v2.md
- excerpt: > RL 통합(v2.1)은 액션 공간 변경(발목 pitch/roll → 크랭크 A/B)과 태스크 정규식·보상의 발목 참조 수정이 필요해 **별도 단계**. 이득은 로드 힘·스토퍼 하중을 시뮬에서 직접 재는 것.
- relevance: This is the repo's current position on 'should we': yes as a deliberate later step, with a known cost (action space/reward rewrite) and a known benefit (rod/stopper loads, kinematic infeasibility pruned). §4 item 4: '발목 2-RSU의 JS6 콘·크랭크 사점·스톱 패드는 직렬 모델에 없다 — 정책이 기구가 못 가는 자세를 배울 수 있는 구조적 한계, v2.1(§5)이 답.'
- verifier: ['The source document /home/syaro/MikuchanRemote/Human-Pygmalion/docs/87_robot_model_v2.md explicitly supports all claims: (1) §5 line 123 defers RL integration to v2.1 as a "별도 단계" (separate stage); (2) line 123 names the action space change (ankle pitch/roll → crank A/B); (3) line 123 states task 

### K46. The serial stand-in used for training today maps the two RS03 motors onto the pitch/roll hinges with G1-class gains (Kp 28.5 / Kd 1.81), per-DOF effort caps 90 (pitch, co-actuation derated from ~120) / 50 (roll, differential), and under PYG_V2 a reflected armature of 2x0.005/ratio^2 using the measured lever ratios 1.25 (pitch) / 1.68 (roll) = 0.0064 / 0.0035.
- source: /home/syaro/MikuchanRemote/Human-Pygmalion/mujoco-sim/mjlab/src/mjlab/asset_zoo/robots/pygmalion/pygmalion_constants.py
- excerpt: > # v2 (2026-08-20 red team): both ankle DOF are driven by the two RS03 through the 2-RSU # with measured lever ratios pitch 1.25 / roll 1.68 (docs/87 s5), so the reflected rotor # inertia at the joint is 2 x 0.005 / ratio^2 = 0.0064 (pitch) / 0.0035 (roll)
- relevance: This is exactly what a closed-loop model would replace: constant lever ratios and constant per-DOF effort caps, which are pose-dependent in the real 2-RSU. docs/87 §4 item 5 also flags that the task's thermal_effort ratings (ankle 20/5) are still the old serial split and should be ~32/24 for 2xRS03.
- verifier: ['The source file completely supports the claim. All numeric parameters (Kp 28.5, Kd 1.81, effort caps 90/50, lever ratios 1.25/1.68, reflected armature 0.0064/0.0035) are present in the code with correct calculations. The serial model uses two RS03 motors (via ACTUATOR_03 with 0.005 armature) mappe

### K47. docs/37 found that Agibot's open-source X1 MJCF abstracts its push-rod ankle linkage to direct hinges (no equality/connect), and used that as justification for the same serial approximation here; it also listed what the serial hinge cannot capture: variable transmission ratio, linkage ROM/singularities, rod structural loads, compliance/backlash.
- source: /home/syaro/MikuchanRemote/Human-Pygmalion/docs/37_ankle_linkage_fidelity.md
- excerpt: > ⚠ 단 X1 **오픈소스 sim 모델(`xyber_x1_serial.xml` MJCF)은 링크를 직결 hinge로 추상화**(equality/connect 미정의, ankle motor ctrlrange ±18Nm). → **Agibot 자신도 sim에선 우리와 똑같이 "링크→직결 hinge 근사"**를 씀.
- relevance: Repo-internal precedent for the serial approximation; docs/37 also offered the upgrade path explicitly: '실제 4절을 MJCF equality constraint로 모델링' as an optional fidelity upgrade.
- verifier: ['The source document (docs/37_ankle_linkage_fidelity.md) fully supports the claim. It: (1) references the actual X1 MJCF file (`xyber_x1_serial.xml`), confirming it abstracts the push-rod ankle linkage to direct hinges with no equality/connect constraints (verified by fetching the actual file); (2)

### K48. The installed mujoco_warp (mujoco-warp 3.10.0.1, on mujoco 3.10.0 / warp 1.14.0) implements five equality types — CONNECT, WELD, JOINT, TENDON, FLEX — and explicitly does not support DISTANCE; io.py raises NotImplementedError for any eq_type outside that enum.
- source: /home/syaro/MikuchanRemote/Human-Pygmalion/mujoco-sim/mjlab/.venv/lib/python3.13/site-packages/mujoco_warp/_src/types.py
- excerpt: > CONNECT = mujoco.mjtEq.mjEQ_CONNECT
  WELD = mujoco.mjtEq.mjEQ_WELD
  JOINT = mujoco.mjtEq.mjEQ_JOINT
  TENDON = mujoco.mjtEq.mjEQ_TENDON
  FLEX = mujoco.mjtEq.mjEQ_FLEX
  # unsupported: DISTANCE
- relevance: types.py:631-648 (EqType). The support check is io.py:300-314: `(mjm.eq_type, types.EqType, mujoco.mjtEq)` in the loop that ends `raise NotImplementedError(f"{names} not supported.")`. Per-type address arrays are built at io.py:716-720 (eq_connect_adr, eq_wld_adr, eq_jnt_adr, eq_ten_adr, eq_flex_adr). The loop ankle uses CONNECT only, which is supported.
- verifier: ['The claim is verified against the source code at /home/syaro/MikuchanRemote/Human-Pygmalion/mujoco-sim/mjlab/.venv/lib/python3.13/site-packages/mujoco_warp/_src/types.py (lines 631-648) and io.py (lines 305-314). The EqType enum defines exactly the five types claimed (CONNECT, WELD, JOINT, TENDON,

### K49. The GPU connect kernel in mujoco_warp handles the exact form the loop model uses — site-to-site connect (objtype SITE) — as well as body-anchor connect, and each connect adds 3 constraint rows; kernels exist for connect (constraint.py:129), joint (473), tendon (615), flex (804) and weld (933), launched at lines 3668/3730/3794/3838/3885 unless DisableBit.EQUALITY is set. The package's own test data includes a site-site connect (`c_site`) and site-site weld.
- source: /home/syaro/MikuchanRemote/Human-Pygmalion/mujoco-sim/mjlab/.venv/lib/python3.13/site-packages/mujoco_warp/_src/constraint.py
- excerpt: > if nsite > 0 and eq_objtype[eqid] == types.ObjType.SITE:
      body1 = site_bodyid[obj1id]
      body2 = site_bodyid[obj2id]
      pos1 = site_xpos_in[worldid, obj1id]
      pos2 = site_xpos_in[worldid, obj2id]
- relevance: constraint.py:220-224; row count at 196-197 (`wp.atomic_add(ne_out, worldid, 3)`). Four loops (both legs) = 12 equality rows per world, well inside the njmax=300 set in tasks/velocity/config/pygmalion/env_cfgs.py:405. Test fixture: mujoco_warp/test_data/constraints.xml:92 `<connect name="c_site" site1="site0" site2="site1"/>`. The Newton solver (sim/sim.py:99 default) path is covered (`newton` fla
- verifier: ['The source code directly confirms all technical claims: site-to-site connect (objtype SITE) is explicitly handled at lines 220-224; each connect constraint adds exactly 3 rows (line 198); all five kernel functions exist at the specified line numbers (129, 473, 615, 804, 933); they are all launched

### K50. mjlab integration gotcha: the loop model's rod BALL joints (qpos width 4, dof width 3) break mjlab's per-joint indexing. Entity builds joint_q_adr by expanding each joint to qpos_width entries, but `find_joints` returns one index per joint name and `joint_pos` / `write_joint_position` index `joint_q_adr[joint_ids]`, so every joint after a ball joint in spec order (including the whole right leg) would silently read/write the wrong qpos column.
- source: /home/syaro/MikuchanRemote/Human-Pygmalion/mujoco-sim/mjlab/src/mjlab/entity/entity.py
- excerpt: > joint_v_adr.extend(range(vadr, vadr + dof_width(jnt_type)))
        joint_q_adr.extend(range(qadr, qadr + qpos_width(jnt_type)))
- relevance: entity.py:1220-1221 with data.py:159 (`q_slice = self.indexing.joint_q_adr[joint_ids]`) and data.py:364-366 (`joint_pos` = qpos[:, joint_q_adr]); utils/mujoco.py:89-93 gives ball qpos width 4. Implication from the test script's own DOF accounting: replacing each rod ball joint with two hinges (a universal joint, no rod spin) makes the count 1 + 2 - 3 = 0 per loop, removes the passive spin DOFs, ke
- verifier: ['The source code fully supports the claim. The indexing bug exists exactly as described: (1) joint_q_adr is built as a flat list (entity.py lines 1207-1221), (2) find_joints returns one index per joint (entity.py lines 515-523), and (3) write_joint_position/write_joint_velocity use joint_q_adr[join

## Dropped claims (refuted or unsupported)

- D1. S3N (arXiv 2608.01697, Aug 2026, DGIST/UMass, Isaac Sim 5.1 serial-tree surrogate of a four-bar parallel-link leg) formalizes the two options as CBCM (constraint-based) vs TBCM (transformation-based) and shows the kinematics-only serial mapping (what Pygmalion does today) misses actuator inertia/dam  
  why: ['The claim states "S3N formalizes the two options as CBCM vs TBCM," but the source paper explicitly states it "focuses on improving the dynamic fidelity of the serial-tree simulator while preserving the TBCM framework." CBCM is only briefly mentioned and characterized, not formalized. The paper dev
- D2. For Cassie, the standard RL pipelines (OSU DRL, Berkeley Hybrid Robotics; Xie 2018, Li ICRA 2021, IJRR 2024) use Agility's MuJoCo model with the achilles and plantar rods closed by equality constraints; the maintainers state MuJoCo is the only simulator that handles the closed loop plus the spring-l  
  why: ['The source does not support the claim as stated. Specifically: (1) The source says MuJoCo is "best supported" for closed-loop kinematic chain simulation, NOT "the only simulator" — these are materially different claims. (2) The source does not cite Xie 2018. (3) The source does not explicitly stat
- D3. Berkeley Humanoid (arXiv 2407.21781) took the opposite route: the hardware was designed to have no closed chains or springs specifically to keep Isaac Lab simulation cheap (>90k steps/s on an A4500), with the single linkage-driven ankle FFE joint chosen so its actuator-joint mapping is linear.  
  why: ['The claim misrepresents the design philosophy. While Berkeley Humanoid did remove closed kinematic chains and achieve >90k steps/s, and did use a linkage-driven FFE ankle with linear mapping, the paper explicitly identifies the linkage ankle as an "exception" to their general principle of avoiding
- D4. The MuJoCo-Warp build installed in this repo's mjlab venv (mujoco_warp 3.10.0.1 / mujoco 3.10.0) supports CONNECT, WELD, JOINT and TENDON equality constraints on GPU, so closed-loop push rods can be simulated in mjlab without changing engines; the only listed unsupported items are PGS/noslip solvers  
  why: ['The claim correctly identifies that CONNECT, WELD, JOINT, and TENDON equality constraints are supported on GPU in mujoco_warp 3.10.0.1, with correct version numbers. However, it incompletely characterizes unsupported features by stating "the only listed unsupported items are PGS/noslip solvers, pl
- D5. In the Cassie model the rod loop is purely kinematic/passive: the rods carry ball (achilles) and hinge (plantar) joints, the heel spring and shin are stiff hinges, and the ankle MOTOR is placed on the output foot joint (left-foot), not on the crank — the loop does not transmit actuator torque. Origi  
  why: ['The fetched Cassie XML model source confirms most claims: motor placement on foot joint (not crank), rod joint types (ball achilles, hinge plantar), passive loop mechanics, and solver configuration (PGS, 50 iterations, 0.5 ms). However, the specific claim that "the shin" is a "stiff hinge" is not 
- D6. The public Digit MJCF (repackaged from Agility's official XML) models Digit's 2-crank/2-rod toe (a 2-DoF parallel ankle topologically identical to Pygmalion's 2-RSU) as a true closed loop: motors on the two crank joints toe_A/toe_B, each rod hanging on a ball joint, each rod tip welded-by-point to t  
  why: ['The claim states there is "a serial toe_pitch->toe_roll universal joint on the foot," but the actual Digit XML defines both joints with identical axis="0 0 1" (Z-axis rotation). A universal joint requires two perpendicular axes; what\'s implemented here is two sequential hinge joints rotating arou
- D7. Unitree resolves the serial-model-vs-parallel-hardware gap in the SDK, not the simulator: the G1 low-level command has a mode_pr flag; PR mode exposes virtual serial pitch/roll joints 'corresponding to the URDF model', AB mode exposes the two real motors and leaves the parallel kinematics to the use  
  why: ['The GitHub source file (g1_ankle_swing_example.cpp) contains only a brief Mode enum with minimal comments: "PR = 0 // Series Control for Ptich/Roll Joints" and "AB = 1 // Parallel Control for A/B Joints". It does NOT contain the specific phrases in the claim such as "corresponding to the URDF mode
- D8. Booster Robotics publishes two T1 MJCF/URDF variants (T1_serial and T1_locomotion) and BOTH are serial at the ankle — neither contains <equality> loop closure, tendons, or rod/crank bodies; the only difference is joint count (23 vs 18). The Menagerie T1 was derived from t1_serial.urdf.  
  why: ['The claim contains a factual error. While the source correctly shows T1_serial and T1_locomotion both exist and both are serial at the ankle with no equality constraints/tendons/rod-crank bodies, and the Menagerie derivation claim is correct, the joint count claim is incorrect. The claim states "t
- D9. The og_bruce MJCF guide (BRUCE biped) documents two explicit options for four-bar/five-bar leg loops in MuJoCo — polynomial tendon equality (approximate) vs <connect> closure (exact, captures singularities) — and uses connect with solref/solimp deliberately softened to represent rod-end backlash.  
  why: ['The claim states the guide documents "two explicit options for four-bar/five-bar leg loops," but the source only documents two explicit options for the four-bar linkage. The five-bar linkage is only shown with the connect-based approach. This is a factual misrepresentation of what the source docum
- D10. MJWarp's source implements connect, weld, joint, tendon, flex and flexstrain equality kernels with Newton-solver-specific sparse JTDAJ blocks; eq_active is a per-world Data array (nworld, neq) and eq_solref/eq_solimp/eq_data are batchable Model arrays (*, neq), so loop stiffness and anchors can be d  
  why: ['The claim contains a factually incorrect statement about GitHub issue closure dates. It asserts that issues #54, #55, and #56 "were closed on 2025-03-31", but they were actually opened on that date and closed 10-22 days later: #54 on April 12, #55 on April 22, and #56 on April 10, 2025. All other 
- D11. Newton (the new Isaac Lab physics backend wrapping MJWarp) converts MJCF equality constraints to loop joints/mimic constraints by default since v1.2.0 (May 2026); SolverMuJoCo accepts only FIXED, REVOLUTE and BALL loop-closing joints (mapped to weld / 5-DOF / connect couplings) and drops any drive,   
  why: ['The technical content about loop closure handling is accurate and well-supported by the Newton documentation. However, the version attribution is incorrect: the claim states the feature was introduced "since v1.2.0 (May 2026)" when it was actually introduced in v1.3.0 (June 2026). The v1.2.0 relea
- D12. Reference closed-loop MJCFs that are trained/benchmarked on these backends: Agility's own Cassie model in MuJoCo Menagerie closes the achilles and plantar rods with four `connect` constraints (equality solref 0.005, timestep 0.0005, Newton solver), and MJWarp's nightly ALOHA benchmark scenes carry `  
  why: ['The sources confirm equality constraints exist in Cassie and ALOHA XML files and are exercised in MJWarp benchmarks. However, the benchmarks are simulator performance tools (mjwarp-testspeed) running pre-recorded control sequences (--replay), not RL training. The nightly dashboard tracks throughpu
- D13. Four-bar linkages in MuJoCo must be built from equality constraints (no native loop primitive); users report the closed-loop joint 'disengages' under fast actuator motion even at dt=2e-4, and the maintainer's answer is to tune solref/solimp of the equality. MuJoCo also enforces solref timeconst >= 2  
  why: ['The cited GitHub issue #172 (google-deepmind/mujoco) does not contain the claimed content. Issue #172 is a simple March 2022 question asking for four-bar linkage simulation examples, with no description and no visible comments or technical discussion about equality constraint disengagement, solver
- D14. Unitree G1's firmware exposes the parallel ankle in two modes: PR ('Series Control for Pitch/Roll Joints', default, matches the URDF) and AB ('Parallel Control for A/B Joints'); RL policies (unitree_rl_gym/lab/mjlab, MuJoCo Playground) all command PR mode, so the kinematic conversion lives on the ro  
  why: ['The source demonstrates that PR and AB modes exist with aliased joint indices, but does not support the specific claims that: (1) PR is the default mode, (2) RL policies "all command PR mode," or (3) kinematic conversion lives on the robot rather than the simulator. The example file contains no di
- D15. Booster T1's open SDK exposes the same two-level interface but names it explicitly: LowCmd has `CmdType { PARALLEL, SERIAL }` (default PARALLEL), LowState carries both `motor_state_serial` and `motor_state_parallel`, and the parallel ankle actuators are enumerated as cranks (kCrankUpLeft=15, kCrankD  
  why: ['The source attribution is incorrect. While the SDK does contain CmdType enum with PARALLEL/SERIAL values and the exact crank indices claimed (kCrankUpLeft=15, kCrankDownLeft=16, kCrankUpRight=21, kCrankDownRight=22), the crank indices are NOT in the cited LowCmd.h file. They are defined in a diffe
- D16. Booster Gym (arXiv 2506.15132, Jun 2025) documents how the T1 SDK's conversion works: forward kinematics of the linkage produce position/velocity feedback for the virtual serial joints, and the policy's serial-space PD output is mapped to the two parallel motors through the transposed Jacobian. Trai  
  why: ['The source paper does not document the specific MJCF file structure, file path (resources/T1/T1_serial.xml), or joint naming (Ankle_Pitch/Ankle_Roll) claimed. While the paper confirms use of a "virtual serial structure" in Isaac Gym and transposed Jacobian/PD controller for series-parallel convers
- D17. Verified numbers for the loop model (docs/87 §5, and re-run today with the current v3 base): closure residual 0.000 mm at the CAD pose for both loops (rod lengths 287.0/194.5 mm, crank radius 65.0 mm); with the shin welded and crank position servos (kp 100), crank ±11.5 deg gives pitch -14.2/+14.1 d  
  why: ['The source document §5 contains loop closure data, but the claimed numerical values do not match. Specific discrepancies: (1) pitch values—claim says -14.2/+14.1° but source says ∓14.4° or -14.8/+14.1° depending on test condition; (2) roll differential—claim says -19.7° but source says -19.3°; (3)
- D18. The loop XML is a feasibility artifact, not a trainable asset: only the left leg has the loop (R_ankle_* joints remain serial hinges at lines 168/171), the file defines only the two crank `general` position actuators (no serial ankle actuators, no other leg actuators — those come from mjlab's actuat  
  why: ['The XML source supports the architectural claims (left-only loop, only two crank actuators defined), but does NOT support the key quantitative assertion about "0.33 kg double-counted" masses. The crank and rod bodies in the XML have no explicit inertial masses defined, and verifying double-countin
- D19. docs/38 (2026-06-22) already surveyed the two sim2real camps and recommended Camp A (policy in joint space; parallel IK + Jacobian-transpose in the deployment SDK, as Booster T1 / Unitree G1 PR mode / LiPS do) over Camp B (policy in motor space with the closed chain in the training sim, as BRUCE and  
  why: ["The source (docs/38, 2026-06-22) does recommend Camp A and does cite IsaacLab's weakness with closed-chains as a reason. However, it explicitly assumes IsaacLab as the training framework and does not address the scenario where training runs in mjlab/MuJoCo-Warp. The document's recommendation is Is
- D20. docs/71 and docs/74 chose an OFFLINE replay instead of in-sim loops: measured ankle (theta, omega, tau) time series from 14 policies (v41 pool, 237,702 samples) are replayed through the 2-RSU IK / Jacobian (tau_m = J^-T tau_j) quasi-statically, ignoring rod/crank inertia and friction. That replay fo  
  why: ['The source document docs/71 does verify that a tB=67.6 > 60 N·m violation at 0.006% was found via offline replay and that lowering sim effort limit to ~81 N·m was recommended. However, the claim incorrectly attributes this finding to "14 policies (v41 pool, 237,702 samples)." In fact, the violatio