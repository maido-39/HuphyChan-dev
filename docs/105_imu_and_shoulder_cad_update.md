# 105. IMU 위치 정정과 어깨 모델 갱신 — 라이브 Fusion 실측 (2026-08-26)

> *한 줄*: 사용자 지시 두 건("IMU 위치가 바뀌어야 해 — Hip Yaw 모터 표면기준 −Z 187 mm" / "어깨 모델 수정했다,
> 베어링 빼고는 출력물")을 **켜져 있는 Fusion 문서에서 직접 실측**해 확인했다. 둘 다 지시와 CAD가 어긋나는 곳이 있었고,
> 그 어긋남이 이 노트의 알맹이다.

| | |
|---|---|
| 측정 대상 | 라이브 Fusion 문서 `260819_HumanMesh_wUpper_OMAKASE_RrecoverFromCrash v22` |
| 경로 | Windows CAD PC → SSH 역터널 → `127.0.0.1:27182/mcp` (MCP Server Adapter, Fusion 2704.1.53) |
| 도구 | `tools/fusion/mcp_client.py` + `tools/fusion/fx.py` (**읽기 전용**, 저장·수정·undo 일절 없음) |
| 주의 | 리포의 `refs/Huphy_1.0_STEP/`(08-14)는 **라이브 문서보다 낡았다**. 실측은 STEP이 아니라 Fusion에서 했다 |

---

## 1. IMU — "Hip Yaw"가 아니라 **Waist Yaw**였다

### 1a. 지시를 그대로 따르면 IMU가 허벅지 안으로 들어간다
Hip Yaw 모터(RS03 Hip_Y)는 루트 좌표 z = **−97.00 … −40.40 mm**에 있다. 여기서 월드 −Z로 187 mm 내려가면
z = −227 … −284 mm이고, 이 구간은 전부 **허벅지 안**(고관절과 무릎 모터 사이)이다.
베이스 자세추정용 IMU를 흔들리는 허벅지로 옮기는 것은 위치 조정이 아니라 기능 변경이다.

### 1b. 면 쌍 전수 대조 — 지시가 가리키는 모터를 좁힌다
모든 Yaw/Hip 계열 모터의 ±Z 평면 면과 IMU 조립체의 ±Z 평면 면을 짝지어 간격 187 mm ± 1 mm인 쌍을 열거했다
(43쌍). **43쌍 전부 `Robstride RS04 - Waist_Yaw`였고 Hip Yaw는 한 쌍도 없다.** 최상위 쌍은 오차 0.00 mm다:

| 순위 | 모터 면 z (mm) | IMU 면 z (mm) | 간격 (mm) | 오차 |
|---|---|---|---|---|
| 1 | **177.50** (Waist_Yaw) | **−9.50** | **187.00** | **0.00** |
| 2 | 180.50 (Waist_Yaw) | −6.51 | 187.01 | 0.01 |
| 3 | 175.79 (Waist_Yaw) | −11.29 | 187.08 | 0.08 |

> ⚠ **이 증거의 힘을 과장하지 말 것 (적대적 검증에서 지적됨).** 오프셋을 140–214 mm로 훑으면
> 오프셋당 평균 **114쌍**이 걸리고, **153·181·140·142·145·147·151·154 mm는 187 mm보다 "정확히 0.00" 쌍이 더 많다.**
> 즉 **0.00 mm 일치 자체는 우연으로 충분히 나온다.** 또 "Hip Yaw 쌍이 없다"는 것도 동어반복에 가깝다 —
> Hip Yaw 면(−97.0…−40.4)과 IMU 면(−26.7…+4.48)의 간격은 **13.7…101.5 mm로 유계**여서
> 187 mm는 애초에 **기하학적으로 불가능**하기 때문이다.
>
> **이 대조가 실제로 증명하는 것은 하나뿐이다: 지시를 문자 그대로 Hip Yaw로 읽으면 성립하지 않는다.**
> Waist Yaw로 확정한 근거는 **사용자 확인(2026-08-26)**이고, 그것이 유일한 실질 증거다.

### 1c. CAD 루트 → URDF `base_link` 좌표 변환 (독립 검증 완료)
```
URDF_x = (70.0 − CAD_y) mm
URDF_y =  CAD_x          mm
URDF_z = (CAD_z − 60.0)  mm      # = Rz(+90°) 후 평행이동, 우수좌표계 유지
```
관절 두 개로 교차검증했고 **둘 다 0.00 mm로 일치**한다:

| 검증점 | CAD (mm) | 변환 결과 | URDF 실제값 | 판정 |
|---|---|---|---|---|
| 고관절 중심 | (−123.70, 70.00, 60.00) | (0, −123.70, 0) | `L_hip_pitch_joint` origin `0 −0.1237 0` | ✅ |
| 무릎 축 | (−123.70, 115.00, −310.00) | (−45, −123.70, −370) | `L_knee_joint` 누적 `−0.045 −0.1237 −0.37` | ✅ |

### 1d. 결론 — 현재 모델의 IMU는 **310.6 mm 틀렸다**
| | base_link 좌표 (m) | 출처 |
|---|---|---|
| 현재 생성값 | `0.004, 0, **0.241**` | `build_robot.py:530` — 구모델에서 물려받은 값("old model geometry re-expressed", 실측 아님) |
| **실측 (사용자 스펙)** | `−0.000001, **0.007078**, **−0.06950**` | Waist_Yaw 면 177.50 − 187.00 = CAD z −9.50 |
| 참고: 센서 기판 중심 | `−0.000001, 0.007078, −0.076086` | EBIMU-9DOFv5 COM (CAD −16.086) — 스펙 면보다 6.59 mm 아래 |

**보정 벡터 Δr = (−0.004, +0.0071, −0.3105) m, 크기 310.6 mm.**

IMU 자세: EBIMU 기판의 `transform2`가 **단위행렬**이고 면 법선이 정확히 ±Z/±Y다 ⇒ 기판이 골반과 **축 정렬**되어 있다.
따라서 site에 **추가 회전은 필요 없다**(`rpy = 0 0 0`).

### 1e. 이 정정이 정책에 미치는 영향 — **행위자(actor)에는 영향 0**
관측 구성을 런타임에서 직접 읽어 확인했다(설정 파일을 믿지 않고 `observation_manager`에서 덤프):

```
GROUP actor : [0:3] base_ang_vel   [3:6] projected_gravity  [6:22] joint_pos … 
GROUP critic: [0:3] base_lin_vel   [3:6] base_ang_vel       …
```
- `base_lin_vel`(= site velocimeter, **위치 의존**)은 **크리틱 전용**이다 → 배포되는 정책은 이 신호를 안 본다.
- 행위자가 보는 `base_ang_vel`은 자이로다. **강체의 각속도는 위치와 무관**하므로 site를 옮겨도 값이 같다.
- `projected_gravity`는 `mdp.projected_gravity`(베이스 자세)로 계산되며 site를 거치지 않는다. 자세도 동일.

크리틱이 받는 변화량을 학습된 정책(`bundleD1_AB` 32798)으로 실측했다
(`tools/robot_model/loop_tests/imu_site_delta.py`, 16 env × 500 step, 전진 1.6 m/s):

| | 값 |
|---|---|
| \|ω\| (중앙값 / p95) | 0.106 / 0.202 rad/s |
| **\|ω × Δr\|** (중앙값 / p95 / 최대) | **0.021 / 0.047 / 0.082 m/s** |
| 추종 중인 속도 대비 | **1.3 %** |
| `base_lin_vel`에 이미 주입되는 노이즈(±0.5 m/s) 대비 p95 | **0.093** |

⇒ **이미 넣고 있는 관측 노이즈의 1/10 수준**이다. 기존 정책을 무효화하지 않고,
재학습 없이도 안전하게 반영할 수 있다. 가치는 **sim2real 충실도**에 있다:
실기에서 속도계·가속도계는 진짜 IMU 위치에서 읽히므로, 나중에 가속도계를 관측에 넣을 때
레버암이 맞아야 한다([[103_v2_training_plan]] V10).

---

## 2. 어깨 — 재작업된 부품이 전부 **일반 Steel(7.85 g/cm³)** 로 들어와 있다

### 2a. 실측
활성 가지는 `Arm_R / ArmR_Dummy`이며(`ArmR_fullDoF`는 `light=False` 비활성 변형), 활성 바디 98개 기준:

| 분류 | CAD 그대로 (kg) | 비고 |
|---|---|---|
| 출력물(printed) | **3.5812** | 10개 구조 바디 — **전부 "Steel" 7.850 g/cm³** |
| 베어링 | 0.0500 | 6810ZZ-Roll2Yaw |
| 체결류 | 0.1928 | ISO 4762 / 10642 |
| 모터 | 1.7600 | RS03 ×2 (CAD 880 g/개) |
| 기타 | 1.0575 | DR2020 압출재(Al 6061, 정상) + DummyHand |
| **합계** | **6.6415** | COM (−178.51, 88.31, 434.60) mm |

### 2b. 왜 Steel이 오류인가 (두 가지 독립 근거)
1. **직전 리비전에서 같은 그룹이 Aluminum 6061(2.70)이었다** — 새 바디가 문서 기본 재질로 들어온 것이다.
2. **형상이 명백히 출력 브래킷이다**: `Shoulder-Pitch2Roll-BearingCap`은 3 mm 캡, `ShoulderR_holderB`·
   `Arm_ConnectorB_alpha`는 6 mm 판, `Shoulder_Stopper_P`는 4 mm 스토퍼다. 758 g짜리 강재 `Arm_ConnectorA`를
   깎아 쓰는 설계가 아니다.

### 2c. "이전 측정값 고려해" = docs/89의 출력물 밀도
[[89_printed_density_survey]]: 실측 11개 부품의 출력물/알루미늄 질량비 **0.329 ± 0.033**(전체 19개는 0.344),
권장 출력물 밀도 **0.887–0.89 g/cm³**(유효 인필 ≈ 74 %). 이 값은 **밀도**이므로 원 재질이 Al이든 Steel이든
그대로 적용된다.

| 시나리오 | 출력물 질량 | 어깨 합계 | COM (mm) |
|---|---|---|---|
| A: CAD 그대로 | 3.5812 | 6.6415 | (−178.5, 88.3, 434.6) |
| **B: 출력물만 0.887 재유도 (권장)** | **0.4047** | **3.4650** | (−175.9, 86.9, 364.7) |
| C: B + DummyHand까지 출력물 취급 | — | 2.7934 | (−170.0, 87.4, 474.1) |

**시나리오 B 채택 시 어깨 질량 −3.177 kg.**

### 2d. 계측 함정 두 개 (기록해 둘 것)
1. **`Arm_R.physicalProperties`를 그룹 질량으로 읽으면 안 된다.** 13.1599 kg = `ArmR_fullDoF` 6.4965(숨김)
   + `ArmR_Dummy` 6.6133 + 베어링 0.05 — **숨긴 가지가 전부 합산**되어 실제의 약 2배다.
   산술로 확인했다(합이 정확히 일치).
2. `Shoulder-Roll2Yaw-dummy`(1.782 kg CAD / 0.201 kg @0.887)는 **08-22에는 보였는데 지금 숨김**이다.
   가시 체인이 `Shoulder-Pitch2Roll → DR2020 압출재 → DummyHand`로 이어지므로 압출재가 대체한 부품으로 보여
   **합계에서 제외**했다. 다만 라이트 전구는 뷰 상태일 뿐이라([[88]] §4b) 확정은 사람 판단이 필요하다.

### 2e. 모터 질량 불일치
CAD의 RS03 어깨 모터는 **880 g/개**인데, 이 프로젝트의 **실측 RS03는 919.5 g**이다([[reference-robstride-motor-specs]]).
어깨 2개 기준 **+39 g/개 = +79 g**. 반올림 오차가 아니라 카탈로그값과 실측값의 차이이므로,
`massprops_fusion.py`가 모터 질량을 실측값으로 덮어쓰는 기존 규약이 어깨에도 적용되는지 확인이 필요하다.

---

### 2f. 적대적 검증에서 뒤집힌 두 가지 (중요)
1. **`Shoulder-Roll2Yaw-dummy`는 제외하면 안 된다.** 처음엔 "압출재가 대체한 부품"으로 보고 제외했는데,
   바운딩박스가 반대를 말한다: 두 바디는 y[49.5, 75]와 y[95, 137.5]의 **측판**이고 DR2020 압출재가 그 사이
   y[75, 95] 틈에 정확히 들어가며 z로 73 mm 겹친다. 즉 **압출재를 어깨 롤 출력에 물리고 6810ZZ를 받는 요크**다.
   게다가 리포 규약(`massprops_fusion.py`)이 "라이트 오프는 뷰 상태일 뿐"이라고 명시하고,
   **현행 모델이 이미 포함하고 있다**(`arm` 2.8414 kg 중 1.7818 kg = 62.7 %). ⇒ **포함**.
2. **RS03 모터 질량**: CAD 880 g은 카탈로그값이고 이 프로젝트 **실측은 919.5 g**이다(−39.5 g/개, 쌍으로 −79 g).
   아래 표는 "어깨만 변인"으로 두기 위해 **카탈로그 880 g을 유지**한 값이다. 실측 반영은 별도 항목으로 처리한다.

### 2g. 링크별 변화 (한쪽 팔 기준, 출력물 0.887 g/cm³ 적용)
| 링크 | 현행 모델 | v22 CAD 보정 | 변화 |
|---|---|---|---|
| `shoulder_pitch_link` 질량 | 1.1776 kg | **1.40268 kg** | **+0.2251 kg** |
| COM (링크 좌표) | (−0.00184, +0.00690, +0.00003) | (−0.003799, +0.002131, −0.012522) | 12.6 mm 하강 |
| 주관성 (자체 COM 기준) | 0.00200 / 0.00152 / 0.00132 | 0.001906 / 0.003581 / 0.004085 | 약 2배 |
| `arm_link` 질량 | 2.8430 kg | **0.65071 kg** | **−2.1923 kg (−77 %)** |
| COM (링크 좌표) | (−0.00595, −0.00013, −0.25605) | (−0.006391, −0.000064, −0.280914) | 24.9 mm 멀어짐 |
| 주관성 | 0.17676 / 0.17518 / 0.00246 | 0.000389 / 0.033638 / 0.033862 | **−81 %** |

**로봇 전체: 35.3475 → 31.4131 kg (−3.9338 kg, −11.1 %).**

새 `arm_link`(9바디, 650.7 g) = DR2020 압출재 300.4 g(**진짜 Al 6061, 재유도 금지**) + Roll2Yaw 요크 201.4 g
+ DummyHand 셸 85.6 g + 6810ZZ 50.0 g + 나사 13.5 g.
새 `shoulder_pitch_link`(89바디, 1402.7 g) = 롤 RS03 880.0 g + 출력물 9바디 343.3 g(387.07 cm³ × 0.887) + 나사 79개 179.3 g.

⚠ `arm_link` 관성이 **−81 %**다. 팔이 용접되어 있어도 상체 관성은 베이스 링크에 합쳐지므로, 이 크기의 변화는
**보행 정책에 실제로 영향을 준다**(−3.93 kg는 전체의 11 %다). v3에서 학습한 정책을 v4로 그대로 가져갈 수 없고,
**최소한 warm-start 재학습이 필요**하다.

### 2h. 재빌드가 조용히 망가질 뻔한 지점 두 개 (수정 완료)
`massprops_fusion.classify`에서:
- **M1**: v22에서 `Shoulder-Pitch2Roll`이 `ArmR_Dummy` 아래로 재부모화되었는데 `ArmR_Dummy` 검사가 먼저였다
  → **어깨 브래킷 3.218 kg이 어깨-롤 관절을 넘어 `arm_link`로 이주**하고 `shoulder_pitch_link`는 모터만 남은 0.880 kg이 된다.
- **M2**: `6810ZZ-Roll2Yaw`가 `/Arm_R:1/` 바로 아래로 승격되어 **어느 분기에도 안 걸리고 50 g이 조용히 사라진다**.
두 건 모두 수정했고, `Torso2ShoulderP`는 **명시적 `return None`**으로 바꿔 "무시"가 사고가 아니라 기록된 결정이 되게 했다.

### 2i. "왜 질량이 줄었나" — 사용자 질의에 대한 정산 (2026-08-26)
사용자 지적: *"하체는 손 안 댔는데 왜 줄지? DF2020은 알루미늄이야. 나사도 고려했나?"* — 셋 다 정당한 확인이고,
현행 v3 모델을 만든 덤프(`bodies_v3_printed.json`)를 직접 열어 정산했다.

**① 하체는 정말 안 건드렸다.** −3.94 kg **전부가 상체**(`arm_link` + `shoulder_pitch_link`)에서 나온다.

**② DF2020·DR2020은 이미 알루미늄이고 그대로 둔다.** 덤프에서 실제 밀도를 확인했다 —
`DF2020-160L/85L`, `DR2020-375L` 전부 **ρ = 2.700**. `set_printed_density.py`의 `SKIP_OCC`가
`DR2020`·`DF2020`을 이미 제외하므로 출력물 밀도가 적용되지 않는다.

**③ 나사는 계산에 들어가 있다.** 강재 7.75로 센다. 오히려 **재작업이 나사 질량을 늘렸다**:
`shoulder_pitch_link`에 나사 79개 **+179 g**, `arm_link`에 **+13.5 g**.

**④ 그럼 왜 줄었나 — 두 바디가 처음으로 출력물 밀도를 받기 때문이다.**

| 항목 (한쪽 팔) | 현행 | 신규 | 변화 | 이유 |
|---|---|---|---|---|
| Roll2Yaw 요크 (2바디) | 1781.8 g | 201.4 g | **−1580.4 g** | 지금까지 **강재 밀도**로 들어가 있었다 |
| DummyHand | 757.1 g | 85.6 g | **−671.5 g** | 위와 같음 — **단, §2j 확인 필요** |
| DR2020 압출재 | 300.4 g | 300.4 g | ±0 | Al 6061 유지 |
| 베어링 6900ZZ→6810ZZ | 9.0 g | 50.0 g | **+41.0 g** | 재작업이 키운 것 |
| 팔 나사 | 0 g | 13.5 g | **+13.5 g** | 재작업이 늘린 것 |
| `shoulder_pitch_link` | 1177.6 g | 1402.7 g | **+225.1 g** | 나사 79개 + 출력물 브래킷 |

⇒ **−11 %는 CAD 재작업의 결과가 아니다.** 재작업 자체는 오히려 **질량을 늘린다**(+41 g 베어링, +193 g 나사).
줄어드는 것은 **[[89_printed_density_survey]]의 출력물 밀도 보정이 어깨에 처음 적용되기 때문**이다 —
그 조사는 **다리만** 다뤘고 상체는 "실측한 적 없다"는 이유로 알루미늄/강재로 남아 있었다
(`set_printed_density.py` 원래 독스트링). 사용자의 "베어링 빼고는 출력물"이 그 전제를 바꾼 것이다.

### 2j. ⚠ 미해결 — DummyHand는 출력물인가, 페이로드 자리표시자인가
`DummyHand`는 어깨가 아니라 **손** 자리다. 사용자 지시("어깨는 출력물")가 여기까지 미치는지 불분명하다.

| 해석 | `DummyHand` | 로봇 전체 |
|---|---|---|
| 출력물 셸로 본다 (형상: 77면, 96.45 cm³ / bbox 191.37 cm³ = 채움 50.4 %) | 85.6 g | **31.40 kg** |
| 실제 손 질량을 대신하는 **페이로드 자리표시자**로 본다 → 재유도 금지 | 757.1 g | **32.75 kg** |

**차이 1.34 kg**(전체의 4 %)이므로 확정 전에는 재빌드하지 않는다.

## 3. 지금까지 반영한 것 / 막혀 있는 것

### 3a. 반영 완료
| 항목 | 상태 |
|---|---|
| IMU 위치를 `build_robot.py`의 실측값으로 | ✅ (`IMU_POS`, `PYG_IMU_LEGACY`로 구값 복원 가능) |
| v3 모델 원상복구 | ✅ **되돌림**. v3는 현행 15개 정책이 학습한 모델이므로 그 의미를 유지해야 하고, 내가 AB(loop)만 재생성해 **AB/RP가 IMU 310.6 mm 차이로 어긋나 있었다**(md5 614f4fce로 복귀 확인) |
| `PYG_MODEL_V4` 게이트 | ✅ 기본 OFF. loop 분기엔 애초에 `PYG_V2_XML` 오버라이드가 없어 AB는 v3 외 모델을 고를 수조차 없었다 |
| `massprops_fusion.classify` M1·M2 수정 | ✅ |
| `set_printed_density.py` 어깨까지 확장 + `Steel` 수용 | ✅ (재질 필터가 `Alumin`만 받으면 **어깨에 아무 일도 안 일어난다**) |
| 상체 관절 검증 | ✅ 자유 시 30관절/17액추에이터·5관절 전부 구동·최대 20.5° 유계 정착, 용접 시 25관절/12액추에이터 **동일 질량 35.347 kg**(용접이 질량 보존) |

### 3b. 막혀 있는 것 — **URDF-export 사본이 어깨 재작업 이전 상태다**
파이프라인(`make_printed_robot.sh`)은 활성 문서가 `260819_HumanMesh_wUpper_URDFexport*`일 것을 요구하고,
`set_printed_density.py`도 마스터 문서를 건드리지 않도록 **거부**한다(확인함). 그런데 Fusion에 있는 사본은
`260819_HumanMesh_wUpper_URDFexport_printedDensity`로 **재작업 전 리비전**이다.
⇒ v4 재빌드 전에 **export 사본을 v22에서 새로 뜨는 작업이 선행**되어야 하며, 이는 사용자의 CAD 문서를 쓰는 일이라 임의로 하지 않았다.

### 3c. 상체 관절: 지금 "살리면" 깨지는 곳
`PYG_UPPER_DOF=1`로 자유화하면 액션 차원이 12 → 17이 되는데, 속도 태스크의 일부 리워드 항이
**관절 12개짜리 벡터를 하드코딩**하고 있다(`variable_posture`가
`size of tensor a (17) must match tensor b (12)`로 예외). 모델 결함이 아니라 **태스크 측 미비**다.
사용자 지시대로 **기본은 용접 유지**이며, 나중에 팔을 학습에 넣으려면 이 항들을 먼저 일반화해야 한다.

### 3d. ⚠ 플래그 파싱 함정 (신규 발견)
`bool(os.environ.get("PYG_X"))`는 문자열 `"0"`에도 **참**이다. 즉 **`PYG_UPPER_DOF=0`은 상체를 오히려 푼다.**
이 방식으로 읽는 PYG 플래그가 **21개**다(`PYG_V2`·`PYG_SOFT_LANDING`·`PYG_INERTIAL_DR`·`PYG_NO_DR`·`PYG_INIT_MID` 등).
**과거 런에는 영향 없음**을 확인했다: 기록된 런 설정에서 `=0`인 것은 `PYG_DR_START_ITER=0`뿐이고 이것은 정수 파싱이다.
새로 추가한 `PYG_IMU_LEGACY`·`PYG_MODEL_V4`는 함정 없이 파싱한다.

## §R 참조
[[103_v2_training_plan]] · [[100_observation_design_sim2real]] · [[102_rebuild_handover]] · [[89_printed_density_survey]] · [[reference-robstride-motor-specs]]
