# 학습 리포트 — 2026-06-22_17-28-08_g1vanilla (⚠ INVALID — 분석 안 함)

- **task/run**: `2026-06-22_17-28-08_g1vanilla`
- ⚠ **INVALID 런**: G1 vanilla 베이스라인 첫 시도였으나 `BipedG1VanillaEnvCfg`가 `BipedRoughEnvCfg`를 상속해 **rough terrain**으로 학습됨 (gaitfix는 전부 flat → terrain confound로 비교 무효). iter 750서 중단.
- **대체**: flat로 수정(부모 → `BipedFlatEnvCfg`, 커밋 `3784cf2`) → **`g1van_flat`로 재학습**. 이 리포트는 무시 (`.audit_skip` 등록).
- 무효 런이라 정량/정성/§7 분석 없음 (템플릿 stub 제거). 진짜 베이스라인 = `g1van_flat` 리포트 참조.
