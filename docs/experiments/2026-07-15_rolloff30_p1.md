# rolloff30_p1 — hip_roll 축 30mm 외측 오프셋 (P1, flat, feet A/B용)

> **한 줄**: hip_roll 회전축만 외측 30mm 이동(yaw 이하·pitch 원위치, [[68_hip_geometry_variants_viz]])한 기하로 flat 학습. gen21p2_fc(비오프셋 flat 앵커)와 **roll-offset 부하 A/B**용. DR-off P1(gait 형성) 완주, P2·측정은 §후속. 설계논의는 [[67_hip_cant_and_roll_motor_review]] §9.

## §1 재현성
- run: `logs/rsl_rl/pygmalion_velocity/2026-07-15_14-17-23_rolloff30_p1` (최종 model_11999)
- launch: `PYG_ROLLOFF30=1 PYG_NO_DR=1 PYG_INIT_BENT=1 PYG_FRESH_STEPS=1` + actor-only warm-start(flat 앵커 gen21_bent_p2 model_31998), 8192 env, 12k iter, DR-off.
- 기하: `pygmalion_rolloff30.xml`(roll 앵커 ∓30mm 외측·검증 roll dY=∓30.0mm·yaw/knee/ankle 0.0mm·pitch 불변, L/R 미러). 지형 flat.
- 측정소스: (fc 예정, P2 후) — 미측정.

## §1b Reward & Gains
- Gen-2.1 번들 동일([[2026-07-13_gen21_bent_p2]]) — 기하(roll offset)만 변인. Kp/Kd/effort/speed 불변.

## §2 최종 지표 (DR-off P1)
- **fell_over 0.0000** (iter 2000부터 종료까지 유지 — roll offset이 학습을 깨지 않음).
- track_linear reward 1.69·track_angular 1.62·Mean reward 106 (flat DR-off, cant30 P1과 동급 건강).

## §4 부모/변인 비교
- vs 비오프셋 flat 앵커 gen21_bent_p2: **PYG_ROLLOFF30 단일변인**(roll 축 30mm 외측). 나머지 동일.
- vs cant30_p1: 같은 flat·warm-start·DR-off 방법론(기하만 캔트 vs roll-offset). A/B 비교 가능.

## §12 판정 (P1)
- ✅ **roll offset 정책비용 0** (fell 0·추종 건강) — 넓어진 힙 기하도 학습 정상.
- 부하 재분배(§9 예측: hip_roll RMS 59→~80%)의 실측 검증은 **P2+fc A/B**에서.
- 계보: flat 앵커 → rolloff30_p1(roll offset) → P2(DR+push, ★dr override 12k/24k 필수) → fc → **vs gen21p2_fc**.

## 후속 (측정 파이프라인 — 미완)
1. **P2**(DR+push, `PYG_DR_START_ITER=12000 PYG_DR_END_ITER=24000` 필수 — 12k-P1 파생, [[2026-07-14_gen21_rough_uneven2_p1]] §P2-버그 교훈).
2. fc 측정(PYG_ROLLOFF30·INIT_BENT 재지정) → hip_roll RMS/P99 실측 → **A/B vs gen21p2_fc**(docs/67 §9 갱신).
- 등록: [[66_experiment_registry]] Era-9, [[experiment_map.canvas]], INDEX.
