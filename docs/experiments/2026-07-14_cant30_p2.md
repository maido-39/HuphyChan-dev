# cant30 P2 — flat-2.5max progress-reward domain-rand+push-trained **hip-pitch-cant30** (2026-07-14)

> **정책명**: `flat-2.5max progress-reward domain-rand+push-trained hip-cant30 (2026-07-14)` (태그 `cant30_p2`)
> **한 줄 요약**: gen21(비캔트) Gen-2.1 스택을 **hip pitch 축 30° 캔트(CAD 확정 기하, PYG_HIP_CANT30)** 기하로만 바꿔 재학습. **정책 품질·강건성은 비캔트와 무해 동등**(fell 0.0, track_lin 1.33 vs 1.35). 부하 A/B(§5~§11)는 fc 측정으로 검증.
> **계보**: gen21_bent_p1 → **cant30_p1**(fresh Gen-2.1 P1, DR-off) → **cant30_p2**(resume, DR+push 램프 +12000 iter). 비교대상 = gen21_bent_p2(flat 설계앵커).

## §1 재현성
| 항목 | 값 |
|---|---|
| task | Mjlab-Velocity-Flat-Pygmalion |
| checkpoint | `logs/rsl_rl/pygmalion_velocity/2026-07-14_07-40-59_cant30_p2/model_31998.pt` (P1 end 19999 + 12000) |
| config 백업 | `<run>/params/{env,agent}.yaml` |
| 지형 | flat |
| 커리큘럼 | P2: DR + push 램프 `dr_factor` 0→1 (resume from cant30_p1 model_19999) |
| **기하 변인** | **PYG_HIP_CANT30=1** — hip pitch 축 inner-up 30° 캔트 + pitch↔yaw 스큐 0mm 교정 + roll 오프셋 29.67mm (docs/67 §3 CAD 확정, `verify_cant30_geometry.py` PASS) |
| init | PYG_INIT_BENT=1 (bent-knee, gen21과 동일) |
| obs/action | obs 60-dim (critic MLP in_features=60), action 12 |
| 측정소스(부하) | `cant30p2_fc.npz` **완료** — §5·§7 A/B 채움. §8c TN·§10 wrench 6관절은 미작성(사유 §후속) |
| seed | (env.yaml 기록) |
| ★토글 재지정 | 측정·play·렌더 전부 `PYG_HIP_CANT30=1 PYG_INIT_BENT=1` (기하+init default 일치, 누락=무효) |

## §1b Reward & Gains
- **보상 구조·Kp/Kd/effort는 gen21_bent_p2와 동일** — 이 실험의 유일 변인은 hip pitch 축 기하(캔트)이며 보상항·게인·effort는 일절 변경 없음. 전체 보상 표·게인 표·PD 회귀(R²)는 [[2026-07-13_gen21_bent_p2]] §1b를 그대로 승계(단일변인 통제).
- 캔트가 게인에 주는 영향: pitch 관절의 **반영관성 1/cos²30 = +33%**(축 기울임의 기구학적 귀결) → 유효 대역폭이 소폭 감소하나, P2가 fell 0으로 수렴한 것으로 보아 기존 Kp/Kd로 안정. (정량은 §7 모터활용 + q/qtarget에서 확인)

<!-- SPEC-TABLES:BEGIN (analysis/run_spec_tables.py) -->

**§1b-1. 리워드 가중치·관절 게인** (이 런의 `params/env.yaml` 파싱 — 위 §1b 서술의 정량 원본)

**Reward 항목** (weight·왜·어떻게):

| reward | weight | 왜 | 어떻게 |
|---|--:|---|---|
| foot_clearance | **-2** | 스윙발 지면 이격(발끌림 방지) | 목표 높이 오차×발 수평속도 |
| track_angular_velocity | **+2** | 명령 회전속도 추종 | exp(-err²/std²) |
| track_linear_velocity | **+2** | 명령 전진/측방 속도 추종 | exp(-err²/std²) |
| air_time | **+1** | 체공시간 보상(질질끌기 억제) | 0.05~0.5 s 체공 발 수; \|command\|>0.5에서 활성 |
| dof_pos_limits | **-1** | 관절범위 한계 벌점 | 한계초과 L1 |
| pose | **+1** | 기본 관절자세 정규화(기괴자세 억제) | default-pose L2 |
| self_collisions | **-1** | 자기충돌 벌점 | -접촉수 |
| stand_still_penalty | **-1** | 이동 명령을 무시하고 서는 stall 방지 | 명령 대비 진행률<30%이면 flat cost |
| track_lin_vel_progress | **+1** | 고속 명령에서 정지하는 local optimum 방지 | 명령방향 실제속도 투영값, 명령크기에서 cap |
| upright | **+1** | 몸통 직립 유지(넘어짐 방지) | exp 자세 |
| knee_overspeed | **-0.5** | 실측 RS04 무부하 속도를 넘는 보행 억제 | relu(\|knee velocity\|-19.9)^2 |
| foot_swing_height | -0.25 | 스윙발 높이 성형 | 스윙 중 목표 높이 오차 |
| action_rate_l2 | -0.1 | 액션 급변 벌점 | -\|Δa\|² |
| foot_slip | -0.1 | 접지발 미끄러짐 벌점 | -\|v_contact\| |
| body_ang_vel | -0.05 | 몸통 각속도 벌점(흔들림 억제) | -\|ω\|² |
| angular_momentum | -0.02 | 전신 각운동량 벌점(회전 낭비 억제) | -\|L\|² |
| thermal_effort | -0.02 | ★열분배: Σ(τ/rated)² 정규화(관절 균등화) | -Σ(τ/rated)² |
| contact_force_cap | -0.01 | ★충격 cap: 발 GRF 역치초과분 벌점(사뿐착지) | -min(max(F-600,0),800) |
| soft_landing | -1e-05 | 착지 첫접촉 충격 벌점(약) | -첫접촉 GRF |
| torque_limit | -0 | commanded 토크 한계초과 벌점 | off(0) |

**관절별 Kp/Kd** (position-PD, effort=관절측 peak):

| 관절 | 모터 | Kp(stiffness) | Kd(damping) | effort [N·m] |
|---|---|--:|--:|--:|
| hip_pitch | RS04 | 150 | 6 | 120 |
| hip_roll | RS04 | 150 | 6 | 120 |
| hip_yaw | RS03 | 150 | 6 | 60 |
| knee | RS04 | 220 | 6 | 120 |
| ankle_pitch | RS03 | 28.5 | 1.81 | 90 |
| ankle_roll | RS00 | 28.5 | 1.81 | 50 |


**§1b-2. 액추에이터 동역학·한계** (이 런의 `params/env.yaml` 파싱 — `2026-07-14_07-40-59_cant30_p2`)

| 관절 그룹 | 모터 | Kp [N·m/rad] | Kd [N·m·s/rad] | effort 한계 [N·m] | 무부하 속도 [rad/s] | 로터 관성 armature [kg·m²] | 쿨롱 마찰 [N·m] | 점성 [N·m·s/rad] | T-N 곡선 |
|---|---|--:|--:|--:|--:|--:|--:|--:|---|
| ankle_roll | RS00 | 28.5 | 1.81 | 50 | — | 0.0005 | — | — | 미사용 (effort_limit 상수 클램프) |
| hip_yaw | RS03 | 150 | 6 | 60 | — | 0.005 | — | — | 미사용 (effort_limit 상수 클램프) |
| ankle_pitch | RS03 | 28.5 | 1.81 | 90 | — | 0.005 | — | — | 미사용 (effort_limit 상수 클램프) |
| hip_pitch | RS04 | 150 | 6 | 120 | — | 0.007 | — | — | 미사용 (effort_limit 상수 클램프) |
| knee | RS04 | 220 | 6 | 120 | — | 0.007 | — | — | 미사용 (effort_limit 상수 클램프) |

토크는 `effort_limit`과 (있으면) 실측 T-N 곡선의 속도의존 상한 중 **작은 값**으로 클램프된다. armature/쿨롱/점성은 모터 실측값(`PYG_MOTOR_MEAS=1`)이면 실측, 아니면 카탈로그 추정치다.

**§1b-3. ROM 한계·액션 창** (모델 XML range · soft 한계 = 중심±0.5·range×0.9 (mjlab `Entity` 규약) · 액션 clip = env.yaml `actions.joint_pos.clip` · 창 = clip 폭 · default = 액션 0 자세)

**soft 한계와 액션 clip은 같은 공식이다** — `Entity.soft_joint_pos_limits`와 `pygmalion_constants.safe_target_clip()`이 둘 다 *중심 ± 0.5·range·factor*를 쓴다(각 경계에 factor를 곱하는 것이 아니다: 비대칭 관절에서 두 식이 갈린다 — knee `[0,120]`은 `[6,114]`이지 `[0,108]`이 아니다). 그래서 `PYG_SAFE_TARGET_CLIP=1`인 런에서는 두 열이 정확히 일치하고, 정책이 통과하는 클램프와 시뮬레이터가 강제하는 클램프가 하나의 계약이 된다.

모델 출처: 노트 선언 `PYG_HIP_CANT30=1`; 이 시기 `pygmalion_constants._XML_NAME` 기본 분기 — `PYG_V2`/`PYG_HIP_CANT*`/`PYG_ROLLOFF30` 미설정 시 `pygmalion.xml`. 노트의 hip_roll 하드스톱 진술(외전 −45° / 내전 +25°)과 이 파일의 range가 일치 — `pygmalion_cant30.xml`

| 관절 | XML range [°] | soft 한계 [°] | 액션 clip [°] | 사용가능 창 [°] | default [°] | 구동 |
|---|---|---|---|--:|--:|---|
| L/R_hip_pitch_joint | [-125, 30] | [-117.2, 22.3] | n/a (구 설정: clip 없음) | 139.5 | -18.33 | 액션 |
| L/R_hip_roll_joint | [-45, 25] | [-41.5, 21.5] | n/a (구 설정: clip 없음) | 63 | 0 | 액션 |
| L/R_hip_yaw_joint | [-50, 50] | [-45, 45] | n/a (구 설정: clip 없음) | 90 | 0 | 액션 |
| L/R_knee_joint | [-140, 0] | [-133, -7] | n/a (구 설정: clip 없음) | 126 | -38.39 | 액션 |
| L/R_ankle_pitch_joint | [-50, 40] | [-45.5, 35.5] | n/a (구 설정: clip 없음) | 81 | 20.63 | 액션 |
| L/R_ankle_roll_joint | [-20, 20] | [-18, 18] | n/a (구 설정: clip 없음) | 36 | 0 | 액션 |
| L/R_toe_joint | [-50, 0] | [-47.5, -2.5] | — (수동) | — | 0 | 수동 |

액션 스케일 0.25 rad/단위, 오프셋 = default (`use_default_offset`). clip이 없는 구 설정에서는 정책 목표각을 시뮬레이터의 soft 한계가 사후에 잡는다 — 창은 soft 한계 폭으로 읽는다.

**§1b-4. 이 런의 스택 플래그 (`PYG_*`)**

출처: 이 노트의 실행 명령/본문에서 추출 (런처 매니페스트 미기록 — 값 없는 항목은 노트가 이름만 언급)

| 플래그 | 값 |
|---|---|
| `PYG_HIP_CANT` | (값 미기재) |
| `PYG_HIP_CANT30` | 1 |
| `PYG_INIT_BENT` | 1 |
| `PYG_MOTOR_MEAS` | 1 |
| `PYG_ROLLOFF30` | (값 미기재) |
| `PYG_SAFE_TARGET_CLIP` | 1 |
| `PYG_V2` | (값 미기재) |

§1b의 리워드 가중치 표가 정본이다 — 플래그는 그 가중치가 어떻게 조립됐는지의 기록이다.

<!-- SPEC-TABLES:END -->

## §2 최종 지표 (full DR+push, dr_factor=1.0)
| 지표 | cant30_p2 | gen21_bent_p2 (앵커) | 판정 |
|---|---|---|---|
| Episode_Termination/fell_over | **0.0000** | 0.0000 | 동일 — 캔트가 강건성 훼손 없음 |
| Episode_Reward/track_linear_velocity | 1.33 | 1.35 | 노이즈 내 동일 |
| Episode_Reward/track_angular_velocity | 1.25 | ~1.25 | 동일 |
| Mean reward | 72–74 | ~74 | 동일권 |

### §2c 학습 건강도
- P1(DR-off): 게이트 통과 — vx 2.5→2.24(**90%**), 1.5→1.14(76%), 0.75→0.72(**95%**). 비캔트 gen21 P1(2.5→85%)과 동등/상회.
- P2(DR+push 램프 완주): dr_factor 1.0 도달, **낙상 0**. 캔트 기하가 DR·push 하에서도 정책 붕괴를 일으키지 않음이 확정.
- ★핵심: **hip pitch 30° 캔트는 정책 학습·강건성에 무해**(§5/§6 재투영 해석의 "학습 불파괴" 예측이 실측으로 성립).

## §9 DR/push 램프
- P2 resume: `dr_factor` 0→1 선형 램프, `push_by_setting_velocity` 이벤트 활성(push_max 범위 env.yaml). 최종 dr_factor=1.0에서 fell 0 수렴.

---
## §5·§7 부하 A/B — cant30p2_fc ↔ gen21p2_fc (적응정책 실측, 2026-07-14 완료)
측정: 동일 프로토콜(box −2.0~2.5·grid 0.25·15s dwell·121 blocks·90750 steps, CPU). 실모터 대비 사용률:

![[cant30_vs_baseline_ab.png]]

| 관절 (모터) | RMS/rated base→cant | P99/peak base→cant | 방향 |
|---|---|---|---|
| hip_pitch (RS04) | 69→**66%** (−3) | 76→73% (−3) | ✓ 감소(켄트 대상축) |
| hip_yaw (RS04) | 32→35% (+3) | 27→**34%** (+7) | ⚠ 커플링 비용(예측대로) — 단 RS04 여유 커서 무해 |
| hip_roll (RS04) | 59→**67%** (+8) | 48→51% (+2) | ⚠ 오프셋+재분배 |
| **knee (RS04)** | **114→101%** (−13) | 94→**87%** (−7) | ✓✓ **만성 최악 열부하 완화** |
| **ankle_pitch (RS03)** | 68→**79%** (+11) | 91→**97%** (+6) | ✗ **약체 모터를 peak로** |
| **ankle_roll (RS00)** | 58→**68%** (+10) | 73→**89%** (+15) | ✗ **최약체 모터를 peak로** |
| GRF (BW) | P99 1.20→1.27 (+6%) · peak 4.91→4.42 (−10%) | | ≈ 중립 |

### 핵심 해석 — 캔트는 "무료 이득"이 아니라 **하중 재분배**
- **완화**: knee(RMS 114→101% rated — 만성 최악 열제약)·hip_pitch(−3). knee는 RS04(peak 여유)라 상시열이 유일 병목이었는데 이를 직접 낮춤 = 실질 가치.
- **가중**: **ankle_pitch(P99 91→97% RS03 peak)·ankle_roll(73→89% RS00 peak)** — 즉 **가장 약한 모터(RS03/RS00)를 peak 한계로 밀어붙임**. hip_roll RMS +8.
- ★**재투영(§5/§6 docs/67)이 놓친 것**: 단순 재투영은 "pitch −12%·yaw +비용"만 예측했으나, 적응정책은 실제로 **(a) knee를 offload(재투영 미예측)** 하고 **(b) 하중을 발목으로 이전(미예측)**. 동일모션 가정의 재투영이 재분배를 못 봄 → **적응정책 A/B가 필수였음이 입증**. pitch 실측 −4%(재투영 −12%보다 작음), yaw 커플링 비용은 방향 확증.
- **설계 판단**: 순수 부하균형 관점에선 캔트가 **약체 발목을 더 싣고 강체(RS04) knee를 던다** = 방향이 다소 불리(약한 곳을 더 약하게). 단 knee 상시열 완화는 진짜 이득. ∴ **캔트의 정당성은 부하절감이 아니라 패키징이어야** 함(§7 docs/67 결론과 일치). 발목이 이미 peak 근접(baseline서도 ankle_pitch 91%)이라 캔트 채택 시 **발목 사이징 재검토 필수**.

## §12 판정
- **정책 비용 = 0** (fell 0·추종 비캔트 동등) — 캔트가 학습을 깨지 않음 실측 확정.
- **부하: 재분배(knee↓ / 발목↑)** — 무료 이득 아님. knee 열완화 vs 발목 peak 가중의 트레이드. 약체 모터(발목)를 미는 방향이라 부하논리만으론 캔트 비권장; **패키징 필요성이 있을 때만 채택**, 채택 시 발목(RS03/RS00) 사이징·2-RSU 재검토 동반.
- **α=20 vs 30**: 발목 가중이 sinα로 자라므로(재분배 강도) **20°가 발목 부담을 덜 실으면서 knee 완화 일부 유지 가능성** — cant20 학습으로 확인 가치 있음(발목 P99가 30°의 97/89%보다 낮아지는지). 단 패키징이 각을 강제하지 않는 한 기본은 α0.
- 계보: flat 앵커 [[2026-07-13_gen21_bent_p2]] 유지. 캔트 데이터는 하드웨어 기하 co-design(docs/67 §5) 근거로 편입.

> **§8c TN·§10 wrench·§11 떨림·ab_sidebyside 미작성 사유(2026-07-15 명시)**: cant30은 발 ±9° toe-out이 발견돼 **cant30fp(feet-parallel)로 대체**됨([[2026-07-15_cant30fp_p1]]). 이 리포트의 **A/B(§5·§7)가 cant30의 확정 산출물**(캔트=하중 재분배, 발목 가중)이고, 앵커급 전체 wrench 시각화는 **후속 cant30fp의 fc에서 완성**한다(구 cant30에 중복 작업 안 함). 즉 본 리포트는 "미완"이 아니라 **A/B 완결 + 계보상 대체**.
