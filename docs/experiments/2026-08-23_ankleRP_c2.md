# ankleRP_c2 — flat-2.5max gen21-bundle curriculum-dr+push ankle-RP-serial (2026-08-23)

> 학습 중 (2026-08-23 21:02 착수 — c1(20:45)은 iter 172에서 중단: 상체 팔을 15° 외전 고정(docs/92 §7)하고 c2로 재시작, 32 000 iter 목표, 16384 env, RTX 5060 Ti 16 GB에서 ankleAB_c2과 동시 실행). 완주 후 §2~§12를 이 파일에 채운다 (fc/fcp 측정 → 추종·부하·모터활용·wrench). A/B 상대 arm: [[2026-08-23_ankleAB_c2]]. 비교노트는 완주 후 별도.

## §1 재현성
- run dir: `2026-08-23_21-02-18_ankleRP_c2` (`mujoco-sim/mjlab/logs/rsl_rl/pygmalion_velocity/`), config 백업 `params/env.yaml`, `params/agent.yaml`; wandb project pygmalion.
- launch: `PYG_V2=1 PYG_INIT_BENT=1 PYG_INERTIAL_DR=1 PYG_DR_START_ITER=10000 PYG_DR_END_ITER=20000 PYG_ARM_ABD_DEG=15 PYG_ANKLE_MODE=RP .venv/bin/python3 analysis/train_wandb_video.py Mjlab-Velocity-Flat-Pygmalion --video True --video-interval 8000 --video-length 500 --env.scene.num-envs 16384 --agent.max-iterations 32000 --agent.run-name ankleRP_c2 --agent.logger wandb` (PYG_TN·PYG_MOTOR_MEAS 기본 ON).
- ★측정·play·렌더 시 동일 토글 필수: `PYG_V2=1 PYG_INIT_BENT=1 PYG_ARM_ABD_DEG=15 PYG_ANKLE_MODE=RP`.
- 상체: waist/shoulder 관절 weld(PYG_UPPER_DOF 미설정), **어깨 roll −15°(외전 15°)로 고정** — 팔 캡슐이 영점에서 hip_roll 링크와 14 mm 겹치던 것을 109 mm 여유로(arm_clearance.py, docs/92 §7). 목/머리는 CAD에 없음. arm–hip 충돌제외 제거(자기충돌 페널티 대상).
- 모델: 3D 프린트 하체 35.35 kg ([[89_printed_parts_density_ratio]]), 관절 범위 = CAD 스윕 하드웨어 한계(hip_pitch −120/+25, hip_roll −85/+25, hip_yaw ±45, knee −120/0, ankle pitch −50/+30, roll ±20 — 발목 위치한계는 AB/RP 무관하게 roll/pitch 각으로).
- 발목 기구: 직렬 pitch→roll (pygmalion_v3_printed.xml, 질량·발바닥 박스·캡슐은 AB와 동일). AnkleRpTnActuator: 관절공간 PD(Kp 28.5 / Kd 1.81) → 루프 자코비안으로 크랭크 토크 환산 → 크랭크당 ±60 N·m + T-N 클램프 → 역환산 (자세별 선형화 토크 한계, docs/92 §2). 관절 armature/damping/friction = 루프 반영값(pitch .0209/.0306/.472, roll .0153/.0223/.403).
- 모터 파라미터(실측 벤치 시스템ID 2026-08-23, 출력축): RS04 J 0.016333 / b 0.009492 / tc 0.269456, RS03 J 0.015265 / b 0.022342 / tc 0.285370 → hip/knee(RS04), hip_yaw(RS03), 발목 모터(RS03). T-N 곡선 액추에이터(Motor_Spec 48 V 실측, 구동 사분면 속도별 상한, 제동 사분면 피크).
- OBS/action: actor 45 / critic 60 / action 12 (scale 0.25 rad).
- init: bent (PYG_INIT_BENT=1, hip −0.32 / knee −0.67 / ankle +0.36; AB는 루프 정합해 `pygmalion_v3_printed_loop_bent.json`).
- 커리큘럼(단일 런): 명령 vx 단계 0.8→1.2→1.6→2.0→2.5 (iter 4k/8k/12k/16k, ang 0.5→1.0), **DR 램프 dr_levels 0→1 iter 10k→20k** (push ±0.7 m/s·friction 0.3–1.2·encoder ±0.015·CoM — friction/encoder/CoM은 리셋마다 재샘플로 바꿔 램프가 실제 적용됨), 관성 DR PYG_INERTIAL_DR=1(측정 질량 불확실성, startup 고정 샘플).
- 보상: gen21 번들 그대로 ([[2026-08-23_ankleAB_c2]]와 동일; env.yaml diff = 발목 항목만, launch 직후 확인 — docs/92 §5). thermal rated: ankle pitch 32.7 / roll 27.9 N·m (루프 중심자세 extent × 20/60).
- seed: agent.yaml 기본.

## §1b Reward & Gains
- gen21 번들 전 항 표는 완주 후 env.yaml 파싱으로 채움(변경 없음 — [[2026-07-13_gen21_bent_p2]] §1b와 동일). 신규: 액추에이터 타입(TnPdActuator/AnkleRp)·모터 파라미터·발목 기구.

## §2–§12
- 완주 후 기입 (fc/fcp 15 s dwell, 영상 2종 실시간, §7 모터활용은 T-N 엔벨로프 대비, §8c TN 설계선도, §10 wrench, §11 대칭/떨림, §12 판정: AB vs RP 부하·추종·낙상).

## §9 DR
- dr_factor 램프 10k→20k (로그 grep으로 완주 후 궤적 첨부). push는 dr_levels의 push_max 스케일, friction/encoder/CoM은 reset-mode 재샘플.
