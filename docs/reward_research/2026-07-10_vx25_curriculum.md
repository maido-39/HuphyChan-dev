# 2026-07-10 · vx 2.5 커리큘럼 (독트린 복귀) — 갭 확대·장기 수렴

**왜**: 액추에이터 선정 독트린의 최악조건 = vx 2.5 · yaw 1.0 · rough. 현 계보(±1.5 G1-like)로는 2.5 측정이 OOD(in-DR 원칙 위반, [[62]] §3). 사용자 지시: "2.5 커리큘럼, iter 늘리고, 커리큘럼 간 갭 늘려 잘 수렴하게, flat→rough, 기존 최적방법으로."

**근거(전수검사 확정 원칙)**: 2단계 커리큘럼(P1 DR-off→P2 램프)만이 flat·rough 모두 재현 성공([[62]] 원칙12); 갭 부족 시 ramp 직후 track 급락(P1 3000 계단응답 관측); 2.5 시대 실패는 "달성불가 목표 추종"이 아니라 커리큘럼·게인 미비가 주인(g1 modest 1.5는 임시 조치였음).

**변경(env_cfgs.py, reward 가중치 불변)**:
- command_vel stages: 0:±0.8 → 4000:±1.2 → 8000:±1.6(yaw 1.0) → 12000:(−2.0,+2.5) → 16000:(−2.0,+2.5) 유지 확인 구간 — 갭 3000→4000 iter, 최종 전진 2.5/후진 2.0(과거 worst-case 코너와 일치).
- Phase1(flat, PYG_NO_DR) 20000 iter(기존 10000의 2배), Phase2 DR 램프 20000→32000.
- ⚠미결(별도 결정): velocity_limit 19.9 실측 정합 재학습은 이번에 미포함 — 2.5 보행이 재차 ω>20 수요를 만들면 그때 병합.

**게이트**: 각 stage 도달 후 track_linear 회복(≥직전 stage의 85%), 완주 시 방향별 추종([[61]] 방법) + wide-DR 2.5 캠페인.
