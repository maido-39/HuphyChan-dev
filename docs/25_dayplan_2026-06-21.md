# 오늘 연구 계획 — 2026-06-21 (12:00 → 21:00)

## 0. 최종 목적 재확인 (왜 이걸 하나)
> **RL이 만든 자연스럽고 효율적인 보행 → 관절·연결부의 실제 하중(토크·속도·구조 |F|/|M|) 측정 → "어느 모터/감속비/연결부를 얼마나 강하게 설계할지" 결정.** 
> 제약 = 물리적 **SIZE**(모터 선택). 보행 품질 = **인간형 + 에너지효율**. 즉 *학습은 수단, 산출물은 HW 사이징 근거*.

지금까지: 평지 보행 확립·발목offload·rough 전이. 측정으로 **ankle_roll 토크+속도 동시 포화 / knee 속도병목(1:3 과함)** 확인. 미해결: **toe 미적재**(간접 CoP보상으로 전환), **rough 미수렴**, **HW 사이징 확정**, **sim 하중 신뢰도**.

## 1. 오늘의 가설 (검증가능 / 반증조건)
| | 가설 | 측정 | 반증 → 대안 |
|---|---|---|---|
| **H-A** toe | **간접** CoP/앞발진행 + power-CoT 보상이 종말기 **앞발 GRF비율↑(toe 적재)**을, static-curl 없이 달성하고 ankle_pitch/knee peak 토크·CoT↓ | 앞발 GRF비율, ankle/knee peak τ vs stage-3, CoT, **적대적 probe**(적재가 CoP 이동+전진과 일치?) | 비율 안 오르거나 정적curl → H3 heel-rise(능동발목) / AMP |
| **H-B** rough | **지형 커리큘럼(쉬움→어려움) + flat warm-start**이 rough 수렴(noise_std↓·낙상<10%·error_vel<0.7) | noise_std 추세·낙상률·error_vel | 여전히 std↑/낙상>15% → 로봇 지형능력(발목 가동범위) 문제, HW로 |
| **H-C** 사이징 | 측정 worst-case가 **ankle_roll=binding**(τ+속도 ≥100%)·**knee 속도병목(1:3→1:2)** 확정, 재사이징 시 ~20% 여유로 포락선 적합 | peak τ·속도 vs RobStride 카탈로그, 여유% | 다른 관절도 포화 → 재검토 |
| **H-D** 효율=자연 | 에너지(CoT) 최소화가 **heel-to-toe 굴림·toe-off** 등 자연 특징 + peak 토크↓를 *동시에* 유도(효율↔자연 일치) | 보행 굴림 패턴·peak τ·CoT, 문헌 | 충돌하면 AMP로 자연성 별도 부여 |

## 2. 시간 계획 (GPU 직렬 ║ CPU·읽기·분석 병렬)
> 원칙: **GPU는 항상 한 학습을 돌리고**, 그 사이 CPU로 읽기·분석·문서·다음 config 준비. 응답 대기로 막히지 않게 **자율 진행**.

| 시간 | GPU 트랙 (직렬) | CPU/읽기/분석 트랙 (병렬) |
|---|---|---|
| **12:00–13:35** | stage-5 rough 마무리(iter 1238→2500) | ① 우선순위 논문 읽기(wu807crtd) ② **HW 사이징 스프레드시트** 골격(측정 peak τ·속도 vs RobStride 카탈로그) ③ forefoot config-test 명령·`forefoot_cop` L/R 순서 점검 |
| **13:35–14:15** | stage-5 완료 → **측정+리포트**(rough loads, 새 시각화) | rough 측정 분석(발목 포락선), H-B 기준선 기록 |
| **14:15–16:30** | ★ **FOREFOOT CoP 실험**(Flat-Forefoot-v0, warm-start stage-3, ~2500it) | HW 스프레드시트 완성(H-C) · 효율/toe 논문 · toe-적재 분석 초안 |
| **16:30–17:15** | forefoot 완료 → **측정+리포트** | **H-A 검증**: 앞발 GRF비율·ankle/knee τ vs stage-3·CoT + **static-curl 적대적 probe** → 판정 |
| **17:15–19:45** | ★ **ROUGH 재학습**(지형 커리큘럼+warm-start, H-B) | sim2real 하중신뢰도·SOTA 논문 · **HW 사이징 결정 메모** · 무게/페이로드 민감도(측정 슬롯서 +10kg sweep) |
| **19:45–21:00** | rough 완료 → 측정 | **H-B 판정** + **하루 마감 리포트**(H-A~D 결론·HW 결정·읽은논문 노트·다음단계) + GitHub push |

## 3. 산출물 체크리스트 (오늘 끝까지)
- [ ] stage-5 rough 측정+리포트(시각화 포함)
- [ ] forefoot CoP 실험 → 측정+리포트 + **H-A 판정**(+static-curl probe)
- [ ] rough 재학습(커리큘럼+warm-start) → **H-B 판정**
- [ ] **HW 사이징 결정 메모**(H-C): ankle_roll 모터 후보·knee 1:2 검증·연결부, ~20% 여유
- [ ] 읽은 논문 노트(wu807crtd 리스트) + **H-D**(효율↔자연) 문헌 근거
- [ ] 무게/페이로드 민감도(가능 시)
- [ ] 하루 마감 리포트 + push

> ⚠️ stage-5가 fresh(미수렴 가능)면 16:30 이후 rough 재학습이 본 수렴 시도. GPU 1장이라 학습은 직렬 — **읽기/분석/문서는 학습 중 병렬로 소화**해 시간 낭비 0.

## 4. 실험 완료 시 검토·수정 프로토콜 (★ 능동 훅)
> 매 실험을 **`run_in_background`(클린 종료/크래시 통지) + `scripts/watch_run.sh <log> <maxiter> <sleep_s>`(주기형 메트릭 스냅샷, ~25분, 로그감시·자기참조 없음)** 둘로 감시. 통지 오면 아래를 **면밀히** 수행하고 *계획을 수정해야 하면 수정*한다.
>
> **★ 중간 모니터링(~25분 주기, 짧게·규칙적으로)**: watcher 스냅샷마다 — ① `analyze_tensorboard.py`로 전체 메트릭 + [[27_training_review_loop]] 체크리스트로 **정량/정성 검토**, ② **`docs/experiments/<run>_monitor.md`에 한 줄(정량 표) + 정성 분석을 append**(기록 누적), ③ continue/refine/stop 판정(보수적), ④ watcher 재무장. 완료 시 `make_run_report`가 이 모니터링 로그를 본 리포트에 포함한다.

**완료(DONE) 시:**
1. **측정** — `measure.py`로 clip/unclip npz (Isaac, sandbox off).
2. **분석(시각화)** — `bash scripts/analyze_run.sh <tag> <clip> [unclip]` (토크/속도 바·**시계열**·포화%·구조하중) + `analyze_tensorboard.py` (noise_std·낙상·loss).
3. **가설 판정** — 해당 실험 가설. *forefoot=H-A*: 앞발 GRF비율↑? ankle_pitch/knee peak τ↓? CoT↓? **+ static-curl 적대적 probe**(적재가 실제 CoP 이동+전진과 일치하나, 정적 curl 아닌가).
4. **부모 대비 비교** — vs stage-3 (plots + 정량).
5. **리포트** — `make_run_report.py --measure_npz <clip>` (모터 viz·학습건강도·누적영상 자동 임베드) → docs/experiments + INDEX + push.
6. **★ 계획 수정** — 결과로 다음 실험·전체 계획 재검토. 예: H-A 성공→toe **강성 sweep**(Cho 0.98 Nm/deg 검증)·HW offset 정량 / H-A 실패(또는 static-curl)→**H3 heel-rise**(능동 ankle, 합법) 또는 **AMP**. rough 최대토크 필요 시→**terrain curriculum(Rudin)**으로 재학습.
7. **다음 실험 launch** + watcher 재무장.

**문제(STALL/ERROR/DIVERGE) 시:** 원인 진단(OOM/유령프로세스 GPU점유/보상가중/LR) → 수정 후 재실행. (오늘 사례: stage-5가 SIGTERM 무시·4.6GB 점유 hang → SIGKILL로 해제 후 forefoot 진행.)

## 5. 계획 refine (연구 wyilgvpyj 반영 — 상세 [[28_reward_actuator_fidelity]])
- **즉시 반영됨**: RS03 속도 220→**200**(YAML+분석도구, 데이터시트 오류 수정), **p95** 토크 메트릭, `joint_overrating_penalty`(rewards.py, 준비).
- **다음 forefoot 후속**(완주 후): (a) **H-A**: forefoot_cop 0.5→~2.5. (b) **열/부하분산**: `joint_overrating_penalty`(rated=[ankle_roll 5·hip 40·knee 120·RS03 20]) 음가중 + power_cot 병행, w를 0서 ramp → ankle_roll RMS%rated<100 목표. (★ power_cot이 열과부하 못 막은 이유=POWER이지 HEAT 아님·14관절 가림 → 정지토크 ankle_roll 놓침.)
- **sim 충실도**: flat effort_limit → **속도의존 T-N 포락 clip**(Shin 2312.17507; 그들 ablation 최대기여 +2.0m/s) — YAML에 T_peak·ω_noload + custom actuator. 측정 하중을 sim2real-grade로.
- **HW resize fork**: T1/T2/clip로도 ankle_roll RMS<5N·m 안 되면 → **RS01/RS02 물리 교체**(rated 6·peak 17·속도↑).
- **식별 과제**: RS00 열시상수·권선 R·마찰(Kt만 있음) — bench 식별(T1 시상수·T3 Joule용, [[26_reading_list]] Hwangbo actuator-net 연계).

관련: [[MIDREPORT_2026-06-21_1100]] · [[28_reward_actuator_fidelity]] · [[24_training_health_analysis]] · [[18_research_roadmap]] · [[23_toe_use_methods]] · [[21_motor_power_weight]]
