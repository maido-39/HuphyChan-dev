# === Isaac Sim install / conda (multi-GB) ===
sim/

# === Training outputs: checkpoints, videos, tensorboard, measurement npz (large/regenerable) ===
**/logs/

# === Generated USD (rebuilt from MJCF via convert_asset.py) ===
**/usd/
pygmalion_locomotion/outputs/

# === Python cache / backups ===
**/__pycache__/
*.pyc
*.pyo
*.egg-info/
*.bak

# === Copyrighted / personal-study figures (paper/press images) — 절대 커밋/공개 금지.
#     Self-made SVG diagrams + matplotlib plots in docs/assets/ ARE tracked;
#     only docs/assets/refs/ (external figures) is excluded for copyright safety. ===
docs/assets/refs/

# === Stray root artifacts (backup zips / model dumps / screenshots) ===
*.zip
MJMODEL.TXT
screenshot.png

# === Private / local / secrets ===
*.private
*.private.*
.claude/
.vscode/
.DS_Store

# Obsidian local UI state (noisy)
**/.obsidian/workspace*.json
docs/assets/*.mp4
docs/assets/*official*.png

# 153 MB - over GitHub's 100 MB hard limit, so it can never be pushed. Kept on disk and
# backed up at ~/pyg_fea/big_media_backup/; removed from history 2026-08-22 to unblock the
# push. If it needs to live on GitHub, move the demo videos to Git LFS instead.
docs/mujoco/assets/p2r_v2_fc_demo_loadviz.mp4

# assembly viewer working copies - regenerated by tools/assembly_viewer/build_data.py
tools/assembly_viewer/meshes/
tools/assembly_viewer/vendor/
